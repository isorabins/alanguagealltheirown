{"hash":"69709f67e430e0d5ce5d37a8e770e2194a734c48986e05976a9271850312a520","rules":[{"id":"rule-439","text_en":"A message is sent as a single unit; do not add chunk headers or separators (e.g., `[part X/Y]`, `---END---`). A line starting with `^` is a directive: an instruction from sender to receiver. The decoder must preserve the `^` and the rest of the line verbatim; the receiver treats the line as actionable. Any other line is narrative content, preserved verbatim, and is not an instruction. When the message consists primarily of instructions or actionable items, write each as a directive line; use plain text lines only for explanatory context that cannot be framed as an instruction."},{"id":"rule-440","text_en":"The sender may write any line in minimal form: omit punctuation (.,:;!?-) and capitalization except where required for numeric literals, unit symbols, or alphanumeric identifiers. The decoder must reconstruct the intended meaning, inferring appropriate punctuation and capitalization as needed. In plain text lines, the sender must omit the following where meaning remains clear: articles 'a', 'an', 'the'; prepositions 'of', 'in', 'for', 'on', 'at', 'to'; copula verbs 'is', 'are', 'was', 'were', 'be', 'been'; auxiliary verbs 'has', 'have', 'had' before past participles; relative pronouns 'that', 'which', 'who', 'whom' introducing a subordinate clause. Do not omit binding words (and, with, because, or, but, so) that link clauses or items. The decoder must infer omitted words."},{"id":"rule-441","text_en":"In numeric literals, preserve all punctuation that affects value. The decimal point must be preserved exactly. A leading zero before a decimal point may be omitted only if the value is less than 1; the decoder must interpret '.8' as '0.8'. In any numeric literal immediately followed by a unit symbol or constituting a percentage, the decimal point must be preserved. In numeric ranges, preserve the delimiter (hyphen, en-dash, colon) exactly; do not replace it with a space. The decoder must interpret the delimiter as indicating a continuous range between the two values. Preserve monetary amounts exactly as in the original, including the currency symbol, decimal point, and any commas as thousand separators; do not omit the currency symbol or decimal point under minimal-form allowances. The decoder must treat the monetary amount as a single numeric entity with its intended value and unit."},{"id":"rule-442","text_en":"Alphanumeric identifiers include command-line flags, API endpoints, zone names, file paths, email addresses, and any technical syntax where hyphens, underscores, slashes, and equals are part of the identifier. The sender must preserve all punctuation and capitalization within such identifiers exactly as in the original; the minimal-form allowance to omit punctuation does not apply within these identifiers. Hyphenated alphanumeric identifiers must preserve the hyphen and capitalization of each segment exactly. An opaque identifier is any string that serves as a unique reference (serial number, order ID, hash, UUID, code) where its exact characters are meaningful. The sender must preserve opaque identifiers exactly as in the original, including all internal punctuation, digits, case, and whitespace. If the identifier contains punctuation or whitespace that could cause tokenizer splitting, enclose it in backticks and join segments with underscores inside the backticks. If the identifier contains no punctuation or whitespace and tokenizes as a single token, transmit it verbatim without backticks unless it repeats at least 3 times, in which case an alias is mandatory. The decoder must remove backticks when present, reconstruct the original identifier verbatim, and validate character-for-character match with the source; any mismatch renders the message non-compliant. When a colon is part of a technical identifier, tag, or environment specifier, preserve the colon and the adjacent word as a single unbroken unit; do not treat the colon as a directive slot separator. The decoder must interpret the colon as binding the qualifier to its preceding noun phrase."},{"id":"rule-443","text_en":"The sender must preserve all named entities (people, organizations, dates, deadlines, penalty clauses), key quantities, and explicit stakes from the original message. Omitting these constitutes a violation of the language's purpose, even if the literal encoding is rule-compliant. In any line, preserve all location names, system identifiers, region tags, and environment specifiers exactly as in the original, including capitalization and punctuation; do not omit them under any compression allowance. Within any line, preserve all explicit times, deadlines, and numeric qualifiers exactly, including colons and leading zeros if present; do not omit them under minimal form. In plain text lines, preserve ordinal words and sequence indicators exactly as in the original; do not omit them under minimal-form allowances. The decoder must interpret these words as specifying exact order or position. In plain text lines preceding a directive, preserve all comparative qualifiers and logical connectors that explain the rationale for that directive; do not omit them under minimal-form allowances. The decoder must interpret these qualifiers as explaining why the directive applies. In plain text lines that contain a conditional or sequential chain, preserve the exact sequence words 'if', 'then', 'unless', 'because', and 'so' exactly as in the original; do not omit them under minimal-form allowances. The decoder must interpret these preserved words as marking the intended logical relationship between clauses. When the source message uses a colloquial abbreviation of a technical unit, preserve that abbreviation as written. The decoder must interpret the abbreviation as the same unit intended by the source, not expand it to a formal unit unless the expansion is explicitly present in the source."},{"id":"rule-444","text_en":"Within a message, the sender may define an abbreviation for any string that repeats at least 3 times by writing `@short=long_form` on its own line. Thereafter, `@short` may be used in place of the full form. The decoder must substitute the original exactly. The sender must ensure token savings: definition tokens + (uses × abbreviation tokens) < uses × original tokens. For opaque identifiers, the long_form uses the identifier's encoded form. For curly-brace or angle-bracket bundles, include the full bundle. For action verbs in directive lines, the abbreviation may be used in any directive line. For common nouns in plain text lines, join words with underscores."},{"id":"rule-445","text_en":"For directive lines, the sender may use positional slots to imply function words. Recommended order: action, object, condition, deadline, rationale. Omit prepositions and articles where the slot order conveys the relationship. The decoder must infer the intended meaning. When a directive line contains a list of items sharing the same action and qualifier, the sender may write the action and qualifier once, followed by a colon and then the list of items separated by commas. The decoder must interpret each list item as receiving the same action and qualifier. If any item has a distinct qualifier, it must be written as a separate directive. In directive lines, the sender may replace the prepositions 'by', 'at', 'to', 'for' when followed by a time, deadline, or recipient with a colon. The colon attaches the following phrase to the preceding slot. The decoder must interpret the colon as the appropriate preposition. In directive lines, the sender may replace 'and' or 'or' connecting multiple arguments with `+` or `|`. The decoder must interpret `+` as 'and' and `|` as 'or'. If an action requires a preposition, preserve it or use a colon. In directive lines, the sender may replace 'greater than', 'older than', 'less than', 'newer than' with `>` or `<`. The decoder must interpret `>` as 'older than' when followed by a duration and as 'greater than' when followed by a plain numeric value; `<` means 'newer than' for durations and 'less than' for plain numeric values."},{"id":"rule-446","text_en":"The sender must preserve all core contextual facts from the original message: event names, venues, key constraints, deadlines, attendee counts, capacity limits, and explicit stakes. These must appear as plain text lines before the directives they justify. Omitting such context renders the message non-compliant."},{"id":"rule-449","text_en":"Revise rule-442's opaque identifier clause to mandate backticks for all opaque identifiers, eliminating the exception for identifiers with no punctuation or whitespace. Replace the current clause beginning 'If the identifier contains no punctuation or whitespace and tokenizes as a single token, transmit it verbatim without backticks unless it repeats at least 3 times, in which case an alias is mandatory' with: 'The sender must enclose every opaque identifier in backticks to form a single atomic token; inside the backticks, join any segments separated by punctuation or whitespace with underscores to ensure tokenizer integrity. The decoder must remove the backticks and reconstruct the original identifier verbatim, validating character-for-character match with the source; any mismatch renders the message non-compliant. If an opaque identifier repeats at least 3 times, an alias under rule-444 is mandatory.' This ensures atomic preservation for all opaque identifiers, directly satisfying the fault invariant."},{"id":"rule-452","text_en":"Revise rule-449 to forbid underscore substitution inside backticks for opaque identifiers. Replace the clause 'inside the backticks, join any segments separated by punctuation or whitespace with underscores to ensure tokenizer integrity' with: 'inside the backticks, preserve every character, punctuation, digit, case, and whitespace exactly as in the source, without substituting any punctuation or spaces with underscores or altering any character. The encoder must rely on the backticks alone to form a single atomic token; if tokenizer splitting occurs, the message is non-compliant and the encoder must define an alias per rule-444.' The decoder requirement remains: remove backticks and validate character-for-character match. This ensures exact preservation without internal modifications that could corrupt the identifier's exact form, directly satisfying the opaque identifier fault invariant."},{"id":"rule-453","text_en":"In any line describing a measured fact—including allocations, conditions, thresholds, or comparisons—the sender must treat the entire measured fact as a single atomic token by surrounding it with angle brackets: <numeric value unit comparison phrase or symbol relational preposition target identifier>. The decoder must remove the angle brackets and reconstruct the original spacing and punctuation verbatim, preserving numeric precision, units, thresholds, and comparison direction exactly as in the source. The decoder must validate that the reconstructed bundle matches the source character-for-character; any mismatch renders the message non-compliant. This rule applies to all measured facts not already covered by a more specific atomic encoding (e.g., opaque identifiers per rule-452). Example: 'alert if temperature <5°C' encodes as 'alert if <temperature <5°C>' and decodes as 'alert if temperature <5°C'; 'allocate 10 GB to node-7' encodes as 'allocate <10 GB to node-7>' and decodes as 'allocate 10 GB to node-7'."},{"id":"rule-454","text_en":"In any line describing a measured fact—including allocations, conditions, thresholds, or comparisons—the sender must treat the entire measured fact as a single atomic token by surrounding it with angle brackets: <numeric value unit comparison phrase or symbol relational preposition target identifier>. The decoder must remove the angle brackets and reconstruct the original spacing and punctuation verbatim, preserving numeric precision, units, thresholds, and comparison direction exactly as in the source. The decoder must validate both: (1) character‑for‑character match between the reconstructed bundle and the source; (2) that the reconstructed bundle contains every original component—value, unit, comparison phrase or symbol, relational preposition (if any), and target identifier (if any)—without omission, alteration, or splitting. If either validation fails, the message is non‑compliant. This rule applies to all measured facts not already covered by a more specific atomic encoding (e.g., opaque identifiers per rule‑452). Example: 'alert if temperature <5°C' encodes as 'alert if <temperature <5°C>' and decodes as 'alert if temperature <5°C'; 'allocate 10 GB to node‑7' encodes as 'allocate <10 GB to node‑7>' and decodes as 'allocate 10 GB to node‑7'."},{"id":"rule-455","text_en":"In any line describing a temporal constraint—including absolute times (e.g., '04:30 PM EST'), relative deadlines (e.g., 'by tomorrow'), date ranges (e.g., '2025‑03‑15 to 2025‑03‑17'), durations (e.g., 'for 30 days'), and ordering relationships (e.g., 'before event start', 'after step 3')—the sender must treat the entire temporal phrase as a single atomic token by surrounding it with angle brackets: <temporal phrase>. The decoder must remove the angle brackets and reconstruct the original wording, spacing, punctuation, and relational ordering exactly as in the source, without normalizing, abbreviating, omitting, or altering any component. The decoder must validate that the reconstructed phrase matches the source character‑for‑character; if any component differs, the message is non‑compliant. This rule applies to all temporal constraints not already covered by a more specific atomic encoding (e.g., opaque identifiers per rule‑452). Example: 'submit report <by 04:30 PM EST>' decodes as 'submit report by 04:30 PM EST'; 'maintenance window <from 2025‑03‑15 to 2025‑03‑17>' decodes as 'maintenance window from 2025‑03‑15 to 2025‑03‑17'."},{"id":"rule-457","text_en":"Revise rule‑455 to explicitly forbid decoder normalization and require verbatim literal output. Replace the sentence 'The decoder must remove the angle brackets and reconstruct the original wording, spacing, punctuation, and relational ordering exactly as in the source, without normalizing, abbreviating, omitting, or altering any component.' with: 'The decoder must treat the content inside the angle brackets as a literal string; it must remove the angle brackets and output that string verbatim, without parsing, normalizing, abbreviating, reordering, omitting, or altering any character. The decoder must then validate that the verbatim output matches the source character‑for‑character; if any component differs, the message is non‑compliant.' This ensures the temporal phrase is preserved exactly as written, satisfying the fault invariant."},{"id":"rule-459","text_en":"In any line containing a temporal expression—including times, deadlines, ranges, durations, and ordering relationships—the sender must identify the complete contiguous sequence of words that constitute the temporal expression. The sequence begins with the first relational preposition (e.g., 'by', 'at', 'from', 'to', 'before', 'after', 'until', 'for') or subordinating conjunction (e.g., 'when', 'once', 'as soon as') that links the temporal element to the surrounding context and includes every subsequent word up to and including the last temporal element (time, date, range, duration, or event reference). The sender must treat this entire sequence as a single atomic token by surrounding it with angle brackets: <complete temporal phrase>. The encoder must verify that the enclosed phrase contains every original component—times, dates, ranges, durations, ordering words, and all linking prepositions/conjunctions—without omission, normalization, abbreviation, or alteration. The decoder must remove the angle brackets and output the enclosed string verbatim, without parsing, normalizing, abbreviating, reordering, omitting, or altering any character. The decoder must validate that the verbatim output matches the source character‑for‑character; if any component differs, the message is non‑compliant. This rule applies to all temporal constraints not already covered by a more specific atomic encoding (e.g., opaque identifiers per rule‑452). Example: 'submit report by 04:30 PM EST' encodes as 'submit report <by 04:30 PM EST>'; 'maintenance window from 2025‑03‑15 to 2025‑03‑17' encodes as 'maintenance window <from 2025‑03‑15 to 2025‑03‑17>'; 'wait until the start of event' encodes as 'wait <until the start of event>'."},{"id":"rule-461","text_en":"In any line containing a temporal expression as defined in rule-459, the decoder must treat the content inside the angle brackets as a literal string; it must remove the angle brackets and output that string verbatim, without parsing, normalizing, abbreviating, reordering, omitting, or altering any character. The decoder must then validate that the verbatim output matches the source character-for-character; if any component differs, the message is non-compliant. This rule reinforces the exact preservation required by the fault invariant."},{"id":"rule-465","text_en":"Before sending, the encoder must verify that the content placed inside the angle brackets, after the angle brackets are removed, exactly matches the contiguous substring of the source that constitutes the complete temporal phrase as defined in rule‑459. The verification must be a character‑for‑character comparison; if any character differs, the encoder must correct the encoding before transmitting the message. This encoder‑side validation is mandatory and ensures the atomic bundle captures the complete temporal constraint and its relational anchors, preventing normalization or omission that would violate the temporal‑constraint fault invariant."},{"id":"rule-466","text_en":"Revise rule-459 to add a precise terminal condition for the temporal phrase sequence. Insert after 'any immediate qualifier attached to that element (e.g., 'EST' in '04:30 PM EST', 'local' in 'by tomorrow local time').' the following sentence: 'The sequence must end before the first word that is a conjunction (e.g., 'and', 'or', 'but', 'because', 'so', 'if', 'unless', 'since', 'while') that does not serve as the temporal link itself, or before a word that begins a new independent clause not modifying the temporal element.' Provide concrete worked examples: (1) 'submit report by 04:30 PM EST because manager leaves early' encodes as 'submit report <by 04:30 PM EST> because manager leaves early' – the bundle stops before 'because', which starts a rationale clause. (2) 'wait until 3 PM and then proceed' encodes as 'wait <until 3 PM> and then proceed' – 'and' begins a coordinated action, not part of the temporal link. (3) 'maintenance from 9 AM to 11 AM local time unless outage occurs' encodes as 'maintenance <from 9 AM to 11 AM local time> unless outage occurs' – 'unless' starts a conditional clause. (4) 'alert before start of event if temperature rises' encodes as 'alert <before start of event> if temperature rises' – 'if' begins a condition. (5) 'meet at 2 PM in conference room' encodes as 'meet <at 2 PM in conference room>' – 'in conference room' is a locative qualifier attached to the temporal phrase, not a new clause, so it remains inside. These examples illustrate the stop rule prevents over-inclusion while preserving linking words, ensuring the complete temporal constraint is captured without absorbing unrelated rationale or instructions."},{"id":"rule-467","text_en":"Revise rule-452 to mandate an alias for opaque identifiers that contain punctuation or whitespace, eliminating reliance on tokenizer detection. Replace the clause beginning 'If the identifier contains punctuation or whitespace that could cause tokenizer splitting, enclose it in backticks and join segments with underscores inside the backticks.' with: 'If the identifier contains any punctuation (including hyphens, slashes, colons, dots, underscores) or whitespace, the sender must define an alias for it under rule-444 and use the alias in the message; the alias definition line must store the identifier exactly as in the source, enclosed in backticks with every character, punctuation, digit, case, and whitespace preserved verbatim, without any internal underscore substitution. The decoder must substitute the stored identifier exactly, removing the backticks, and must validate that the reconstructed identifier matches the original source character‑for‑character; any mismatch renders the message non‑compliant.' Remove the subsequent sentence about tokenizer splitting and alias mandate, as it is superseded."},{"id":"rule-470","text_en":"In any line where a descriptive label (any noun or noun phrase that names, classifies, or describes an entity, location, concept, or attribute) directly precedes and attaches to a following identifier, name, or description, the sender must treat the entire label-entity pair as a single atomic token by surrounding it with curly braces: {label attached entity}. The decoder must remove the curly braces and reconstruct the original spacing and punctuation verbatim, preserving the exact label wording and its attachment without any inference, alteration, detachment, or splitting. The decoder must validate that the reconstructed pair matches the source character-for-character; if any component differs, the message is non-compliant. Example: original 'allocate to rack A-12' encodes as 'allocate to {rack A-12}' and decodes as 'allocate to rack A-12'; original 'meet at West Gate security desk' encodes as 'meet at {West Gate security desk}' and decodes as 'meet at West Gate security desk'."},{"id":"rule-477","text_en":"In any line where a complete critical fact—a phrase linking at least two distinct semantic categories (such as an action and a condition, a cause and an effect, or a qualification and an instruction) via a subordinating or coordinating conjunction (e.g., 'because', 'so', 'if', 'unless', 'since', 'while') or relational preposition (e.g., 'for', 'with', 'by')—must be preserved as one unit, the sender must treat the entire critical fact as a single atomic token by surrounding it with angle brackets: <complete critical fact>. The bundle must include every original word, punctuation, spacing, and linking conjunction from the source. The decoder must remove the angle brackets and output the enclosed string verbatim, without parsing, normalizing, abbreviating, reordering, omitting, or altering any character. The decoder must validate that the verbatim output matches the source character-for-character; if any component differs, the message is non-compliant. This rule applies only when the linked categories are not already fully captured by an existing atomic bundle (e.g., a measured fact per rule-454, a temporal constraint per rule-459, or a label-entity pair per rule-470). Example: 'alert if temperature <5°C because sensor reading exceeds threshold' encodes as 'alert <if temperature <5°C because sensor reading exceeds threshold>' and decodes verbatim, preserving the 'if' and 'because' links."},{"id":"rule-478","text_en":"Revise rule-467 to mandate backticks for all opaque identifiers, removing the alias requirement for punctuation. Replace the clause 'If the identifier contains any punctuation (including hyphens, slashes, colons, dots, underscores) or whitespace, the sender must define an alias for it under rule-444 and use the alias in the message; the alias definition line must store the identifier exactly as in the source, enclosed in backticks with every character, punctuation, digit, case, and whitespace preserved verbatim, without any internal underscore substitution.' with: 'For every opaque identifier, the sender must enclose the entire identifier in backticks to form a single atomic token, preserving every character, punctuation, digit, case, and whitespace exactly as in the source, without substituting any punctuation or spaces with underscores or altering any character. The decoder must remove the backticks and reconstruct the original identifier verbatim, validating character-for-character match with the source; any mismatch renders the message non-compliant. If an opaque identifier repeats at least 3 times, an alias under rule-444 is mandatory.' This ensures atomic preservation for all opaque identifiers, directly satisfying the fault invariant."},{"id":"rule-486","text_en":"When encoding any opaque identifier as defined in rule-478, the sender must treat the entire identifier as a semantic atom: the backticks create an isolation zone where every character, punctuation mark, digit, and case is preserved exactly as in the source, and no internal symbol (including colons, slashes, hyphens, dots, underscores) may be interpreted by the decoder as a language delimiter, slot separator, list marker, or relational operator. The decoder must remove the backticks and output the identifier verbatim, validating character-for-character match with the source; any mismatch renders the message non-compliant. The decoder must also treat the reconstructed identifier as an opaque reference, not parsing its internal structure for meaning, ensuring its semantic role as a unique identifier is preserved. This rule reinforces rule-478 by explicitly forbidding decoder reinterpretation of internal punctuation, directly satisfying the opaque identifier fault invariant."},{"id":"rule-487","text_en":"Replace rule-454 with: In any line describing a measured fact—including allocations, conditions, thresholds, or comparisons—the sender must treat the entire measured fact as a single atomic token by surrounding it with angle brackets: <numeric value unit comparison phrase or symbol relational preposition target identifier>. A measured fact begins with the first numeric value, unit, comparison phrase or symbol (e.g., 'greater than', '<', 'at least'), or relational preposition (e.g., 'to', 'for', 'at') that introduces the measured quantity or comparison, and includes every subsequent word up to and including the last target identifier or numeric qualifier, but stops before any conjunction (e.g., 'and', 'or', 'because', 'so', 'if', 'unless', 'since', 'while') that begins a new clause not part of the measured relationship; this boundary takes precedence over rule-477 when categories overlap. The decoder must remove the angle brackets and reconstruct the original spacing and punctuation verbatim, preserving numeric precision, units, thresholds, and comparison direction exactly as in the source. The decoder must validate both: (1) character‑for‑character match between the reconstructed bundle and the source; (2) that the reconstructed bundle contains every original component—value, unit, comparison phrase or symbol, relational preposition (if any), and target identifier (if any)—without omission, alteration, or splitting. If either validation fails, the message is non‑compliant. This rule applies to all measured facts not already covered by a more specific atomic encoding (e.g., opaque identifiers per rule‑452). Example: 'pressure at least 2.5 bar and valve closed' encodes as 'pressure <at least 2.5 bar> and valve closed' (stops before 'and'); 'allocate 10 GB to node‑7 for backup' encodes as 'allocate <10 GB to node‑7 for backup>' (includes attached prepositional phrase)."},{"id":"rule-489","text_en":"In any line containing a critical fact—a phrase where at least two distinct semantic categories (such as an action and a condition, a cause and an effect, a qualification and an instruction) are syntactically linked by a subordinating conjunction (e.g., 'because', 'so', 'if', 'unless', 'since', 'while'), coordinating conjunction ('and', 'or'), or relational preposition (e.g., 'for', 'with', 'by') not already part of an inner atomic bundle (angle brackets <...>, curly braces {...}, backticks `...`)—the sender must treat the entire critical fact as a single atomic token by surrounding it with double vertical bars: ||complete critical fact||. The bundle must begin with the first word of the first semantic category and include every subsequent word up to and including the last word of the linked category, preserving all linking conjunctions, prepositions, punctuation, and spacing exactly as in the source. The encoder must verify that the enclosed substring matches the source character-for-character. The decoder must remove the double vertical bars and output the substring verbatim, without parsing, normalizing, omitting, or altering any character, and must validate the character-for-character match; any mismatch renders the message non-compliant. Example: 'alert if temperature <5°C because sensor reading exceeds threshold' encodes as 'alert ||if temperature <5°C because sensor reading exceeds threshold||' and decodes verbatim."},{"id":"rule-493","text_en":"Revise rule-470 to add an encoder-side verification mandate. After the first sentence, insert: 'Before sending, the encoder must verify that the content placed inside the curly braces, after the curly braces are removed, exactly matches the contiguous substring of the source that constitutes the complete label‑entity pair. The verification must be a character‑for‑character comparison; if any character differs, the encoder must correct the encoding before transmitting the message.' The decoder requirement remains unchanged: remove curly braces and reconstruct the original spacing and punctuation verbatim, validating character‑for‑character match. This ensures the atomic bundle captures the exact label wording and its attachment to the entity, preventing omission or alteration that would violate the exact‑label fault invariant."},{"id":"rule-496","text_en":"In any line containing a critical fact—a phrase where at least two distinct semantic categories (such as an action and a condition, a cause and an effect, a qualification and an instruction) are syntactically linked by a subordinating conjunction (e.g., 'because', 'so', 'if', 'unless', 'since', 'while'), coordinating conjunction ('and', 'or'), or relational preposition (e.g., 'for', 'with', 'by') not already part of an inner atomic bundle (angle brackets <...>, curly braces {...}, backticks `...`)—the sender must treat the entire critical fact as a single atomic token by surrounding it with double vertical bars: ||complete critical fact||. The bundle must begin with the first word of the first semantic category and include every subsequent word up to and including the last word of the linked category, preserving all linking conjunctions, prepositions, punctuation, and spacing exactly as in the source. The sequence must end before the first word that is a conjunction (e.g., 'and', 'or', 'but', 'because', 'so', 'if', 'unless', 'since', 'while') that does not serve as the linking word itself, or before a word that begins a new independent clause not part of the critical relationship. Before sending, the encoder must verify that the content placed inside the double vertical bars, after the bars are removed, exactly matches the contiguous substring of the source that constitutes the complete critical fact. The verification must be a character‑for‑character comparison; if any character differs, the encoder must correct the encoding before transmitting the message. The decoder must remove the double vertical bars and output the substring verbatim, without parsing, normalizing, omitting, or altering any character, and must validate the character‑for‑character match; any mismatch renders the message non‑compliant. Example: 'alert if temperature <5°C because sensor reading exceeds threshold' encodes as 'alert ||if temperature <5°C because sensor reading exceeds threshold||' and decodes verbatim; 'proceed unless pre‑check fails and then restart service' encodes as 'proceed ||unless pre‑check fails|| and then restart service' – the bundle stops before 'and', which begins a coordinated action."},{"id":"rule-497","text_en":"Replace all phrase-level atomic bundling delimiters with unified angle brackets. Amend rule-470 (label-entity pairs), rule-489 and rule-496 (critical facts) to use angle brackets instead of curly braces and double vertical bars. Add: 'Any phrase that must be preserved exactly as a single atomic token—including a measured fact (per rule-487), a temporal constraint (per rule-459), a label‑entity pair, a critical fact, or any other contiguous substring requiring verbatim atomic preservation—must be surrounded by angle brackets: <phrase>. The encoder must verify that the enclosed substring, after removing the angle brackets, exactly matches the contiguous substring of the source character‑for‑character. The decoder must remove the angle brackets and output the substring verbatim, without parsing, normalizing, omitting, or altering any character, and must validate character‑for‑character match; any mismatch renders the message non‑compliant. This rule supersedes the delimiter-specific bundling in rules 470, 489, and 496, but does not alter the semantic validation requirements for each category (e.g., label attachment, conjunction preservation).' Retain backticks for opaque identifiers per rule-478 as a separate token-boundary mechanism."},{"id":"rule-502","text_en":"In any line containing two or more temporal constraints (as defined in rule-459) linked by a comparative or sequential conjunction (e.g., 'after', 'before', 'from', 'to', 'between', 'until', 'and', 'but', 'or') that expresses a combined ordering or range, the sender must treat the entire multi-constraint phrase as a single atomic token by surrounding it with double vertical bars: ||complete multi-temporal phrase||. The bundle must begin with the first linking word and include every subsequent word up to and including the last temporal constraint, preserving all conjunctions, prepositions, punctuation, and spacing exactly as in the source. The encoder must verify character-for-character match per rule-465. The decoder must remove the bars and output the substring verbatim, validating the match; any mismatch renders the message non-compliant. Example: 'proceed after 3 PM but before 5 PM' encodes as 'proceed ||after 3 PM but before 5 PM||'; 'maintenance from 9 AM to 11 AM' encodes as 'maintenance ||from 9 AM to 11 AM||' (superseding rule-459's single bundle when multiple constraints are linked)."},{"id":"rule-505","text_en":"Add to rule-496: Before encoding, the encoder must scan each line for any subordinating conjunction ('because', 'so', 'if', 'unless', 'since', 'while'), coordinating conjunction ('and', 'or'), or relational preposition ('for', 'with', 'by') that links two distinct semantic categories not already inside an atomic bundle (angle brackets <...>, curly braces {...}, backticks `...`). If such a linking word is found, the encoder must treat the entire phrase from the first word of the first semantic category through the last word of the linked category as a single atomic token by surrounding it with double vertical bars per rule-496, applying the same boundary rules (stop before a new clause conjunction). This mandatory scan ensures every critical fact is captured and bundled, preserving the complete relationships between entities, actions, and constraints."},{"id":"rule-506","text_en":"Add to rule-496: The encoder's mandatory scan per rule-505 must also verify that every linking conjunction or preposition inside the double vertical bar bundle appears exactly as in the source, with its original spelling, case, and position. No linking word inside a critical fact bundle may be replaced by a symbol, abbreviation, or omitted under minimal-form allowances; the bundle must preserve the complete relational wording verbatim. The decoder validation must check this preservation; if any linking word is missing, altered, or replaced, the message is non-compliant."},{"id":"rule-510","text_en":"Replace rule-459 with a new rule that mandates a complete, unbreakable temporal bundle. The new rule: In any line containing a temporal expression—including absolute times, relative deadlines, date ranges, durations, and ordering relationships—the sender must treat the entire temporal phrase as a single atomic token by surrounding it with angle brackets: <complete temporal phrase>. The phrase must begin with the first relational preposition (e.g., 'by', 'at', 'from', 'to', 'before', 'after', 'until', 'for', 'when', 'once', 'as soon as') or subordinating conjunction that introduces the temporal element, and must include every subsequent word up to and including the last temporal element and any attached qualifier (e.g., time zone, locale, 'local time', 'EST'). The bundle must not stop before any conjunction that is part of the temporal link itself (e.g., 'and' linking two times in a range) but must stop before the first word that is a conjunction (e.g., 'and', 'or', 'but', 'because', 'so', 'if', 'unless', 'since', 'while') that begins a new independent clause not modifying the temporal element. The encoder must verify that the enclosed substring, after angle brackets are removed, exactly matches the contiguous source substring that constitutes the complete temporal phrase as defined above; if any character differs, the encoding is invalid and must be corrected. The decoder must remove the angle brackets and output the enclosed string verbatim, without parsing, normalizing, abbreviating, reordering, omitting, or altering any character, and must validate that the verbatim output matches the source character-for-character; any mismatch renders the message non-compliant. This rule applies to all temporal constraints not already covered by a more specific atomic encoding (e.g., opaque identifiers per rule-452). Example: 'submit report by 04:30 PM EST because manager leaves early' encodes as 'submit report <by 04:30 PM EST> because manager leaves early' (stops before 'because'). 'maintenance from 9 AM to 11 AM local time unless outage occurs' encodes as 'maintenance <from 9 AM to 11 AM local time> unless outage occurs' (includes 'local time', stops before 'unless'). 'wait until 3 PM and then proceed' encodes as 'wait <until 3 PM> and then proceed' (stops before 'and' which begins a coordinated action)."},{"id":"rule-511","text_en":"In any line, when a clause is introduced by a subordinating conjunction ('because', 'so', 'if', 'unless', 'since', 'while'), coordinating conjunction ('and', 'or'), or relational preposition ('for', 'with', 'by', 'to', 'at', 'from', 'before', 'after', 'until') that links it to the preceding context, the sender must treat the entire clause as a single atomic token by surrounding it with parentheses: (complete clause). The clause includes the conjunction or preposition and every subsequent word up to the next punctuation mark that ends the clause (e.g., comma, period, colon, semicolon) or the next independent clause introduced by another conjunction not part of the same linkage. This rule applies specifically when the clause is a descriptive attachment using a preposition like 'with' that modifies a preceding noun but is not part of a measured fact, temporal constraint, label-entity pair, or critical fact already bundled under other rules. Example: 'check logs with detailed timestamp analysis' encodes as 'check logs (with detailed timestamp analysis)' where the prepositional clause is isolated; rule-496's scan for critical facts might not capture this because 'with' links a description, not two distinct semantic categories. The encoder must verify that the enclosed substring, after parentheses are removed, exactly matches the contiguous source substring character-for-character. The decoder must remove the parentheses and output the substring verbatim, without parsing, normalizing, omitting, or altering any character, and must validate the character-for-character match; any mismatch renders the message non-compliant."},{"id":"rule-513","text_en":"Revise rule-496 to expand the encoder's mandatory scan to capture any phrase where a measured fact (as defined in rule-487) or temporal constraint (as defined in rule-510) is linked by a subordinating conjunction ('because', 'so', 'if', 'unless', 'since', 'while'), coordinating conjunction ('and', 'or'), or relational preposition ('for', 'with', 'by', 'to', 'at', 'from', 'before', 'after', 'until') to another semantic category (action, condition, entity, or another fact). Replace the opening clause 'In any line containing a critical fact—a phrase where at least two distinct semantic categories ... are syntactically linked by a subordinating conjunction ...' with: 'In any line containing a phrase where a measured fact (per rule-487), a temporal constraint (per rule-510), or any other semantic category is syntactically linked by a subordinating conjunction ('because', 'so', 'if', 'unless', 'since', 'while'), coordinating conjunction ('and', 'or'), or relational preposition ('for', 'with', 'by', 'to', 'at', 'from', 'before', 'after', 'until') to another distinct semantic category, and the linked categories are not already fully inside a single atomic bundle (angle brackets <...>, curly braces {...}, backticks `...`, parentheses (...)), the sender must treat the entire linked phrase as a single atomic token by surrounding it with double vertical bars: ||complete linked phrase||.' Keep all subsequent bundling, boundary, verification, and decoder validation requirements unchanged. This ensures the encoder's scan per rule-505 captures measured and temporal relationships as critical facts, preserving the complete relational structure."},{"id":"rule-515","text_en":"Repeal the contradictory clause in rule-478. Replace the sentence 'If the identifier contains no punctuation or whitespace and tokenizes as a single token, transmit it verbatim without backticks unless it repeats at least 3 times, in which case an alias is mandatory.' with: 'The sender must enclose every opaque identifier in backticks to form a single atomic token, preserving every character, punctuation, digit, case, and whitespace exactly as in the source, without substituting any punctuation or spaces with underscores or altering any character. The decoder must remove the backticks and reconstruct the original identifier verbatim, validating character-for-character match with the source; any mismatch renders the message non-compliant. If an opaque identifier repeats at least 3 times, an alias under rule-444 is mandatory.' This repeal eliminates the ambiguity and ensures the rule's opening mandate is unconditional and directly satisfies the opaque identifier fault invariant."},{"id":"rule-521","text_en":"Repeal the proposed add rule-521; the decoder's treatment of opaque identifiers is already fully governed by rule-486 and rule-478."},{"id":"rule-523","text_en":"Revise rule-470 to adopt unified angle brackets for label-entity pairs and reinforce exact preservation. Replace the current rule text with: 'In any line where a descriptive label (any noun or noun phrase that names, classifies, or describes an entity, location, concept, or attribute) directly precedes and attaches to a following identifier, name, or description, the sender must treat the entire label-entity pair as a single atomic token by surrounding it with angle brackets: <label attached entity>. The pair must begin with the first word of the label and include every subsequent word up to and including the last word of the attached entity, preserving all internal punctuation and spacing exactly as in the source. The sequence must end before the first word that is a conjunction (e.g., 'and', 'or', 'but', 'because', 'so') that begins a new independent clause not part of the attachment. Before sending, the encoder must verify that the content placed inside the angle brackets, after the brackets are removed, exactly matches the contiguous substring of the source that constitutes the complete label-entity pair. The verification must be a character-for-character comparison; if any character differs, the encoder must correct the encoding before transmitting the message. The decoder must remove the angle brackets and output the enclosed string verbatim, without parsing, normalizing, omitting, or altering any character, and must validate the character-for-character match; any mismatch renders the message non-compliant. This rule applies to all label-entity pairs not already covered by a more specific atomic encoding (e.g., opaque identifiers per rule-478).'"},{"id":"rule-524","text_en":"Add rule-524: In any message, for any phrase that is already wrapped in angle brackets <...> in the source according to an adopted bundling rule (e.g., measured fact per rule-487, temporal constraint per rule-510, label-entity pair per rule-523, critical fact per rule-496, or other phrase requiring exact preservation), and the identical angle‑bracketed substring (including the angle brackets and all internal punctuation, case, digits, and whitespace) appears at least three times, the sender may define a reference token for it by writing `$ref=exact_bundled_phrase` on its own line, where `exact_bundled_phrase` is that angle‑bracketed substring verbatim. The encoder must verify that each occurrence of the bundled phrase in the source matches the defined `exact_bundled_phrase` character‑for‑character. Thereafter, the sender may use `$ref` in place of that bundled phrase anywhere in the message. The decoder must substitute the exact_bundled_phrase verbatim (including its angle brackets) for each occurrence of `$ref`, validating character‑for‑character match with the original bundled phrase in the source. The sender must ensure token savings: definition tokens + (uses * reference token tokens) < uses * original bundled phrase tokens. This mechanism is distinct from rule‑444's @short alias, which defines abbreviations for whole strings that repeat; $ref enables compression of identical atomic phrases that appear inside varied surrounding contexts. Decision procedure: (1) Identify any substring that is already wrapped in angle brackets in the source according to an adopted bundling rule. (2) If the identical angle‑bracketed substring repeats at least three times, consider $ref referencing. (3) If the substring repeats but is not angle‑bracketed, use @short alias if it meets rule‑444 criteria. (4) Choose the mechanism that yields the greatest token savings while preserving exact meaning. Example: original 'alert if <temperature <5°C> because sensor reading exceeds threshold' and 'log when <temperature <5°C> occurs' share the bundled phrase '<temperature <5°C>'. Encode as '$ref=<temperature <5°C>\\nalert if $ref because sensor reading exceeds threshold\\nlog when $ref occurs' and decode verbatim."},{"id":"rule-529","text_en":"Add rule: After reconstructing an opaque identifier per rule-478 and rule-486, the decoder must treat the reconstructed string as an opaque semantic token for all subsequent language processing. The decoder may not parse, interpret, or assign any grammatical, relational, or structural meaning to any internal punctuation mark, digit, case, or whitespace within the identifier. Specifically, internal symbols such as colons, slashes, hyphens, dots, underscores, equals signs, or parentheses must not be treated as language delimiters, slot separators, list markers, relational operators, directive introducers, or bundling boundaries. The identifier's sole semantic role is that of a unique reference; its internal composition does not affect the parsing of the surrounding message. This rule reinforces the prohibition of decoder reinterpretation in rule-486 by explicitly forbidding any post-reconstruction parsing that could alter the identifier's intended semantic role, directly satisfying the opaque identifier fault invariant."},{"id":"rule-531","text_en":"Add rule: For any clause introduced by a subordinating conjunction ('because', 'so', 'if', 'unless', 'since', 'while'), coordinating conjunction ('and', 'or'), or relational preposition ('for', 'with', 'by', 'to', 'at', 'from', 'before', 'after', 'until') that syntactically links two distinct semantic categories, the sender must treat the entire linked phrase as a single atomic token by surrounding it with angle brackets: <complete linked phrase>. The phrase must begin with the linking word and include every subsequent word up to the end of the linked category, stopping before any conjunction that begins a new independent clause not part of the same linkage. Before sending, the encoder must verify character-for-character match with the source substring. The decoder must remove the brackets and output the substring verbatim, validating the match; any mismatch renders the message non-compliant. This rule supersedes and repeals the double vertical bar mechanism in rule-496 and rule-513, consolidating all phrase-level bundling under a single, uniform delimiter while preserving the complete relationships between entities, actions, and constraints."},{"id":"rule-537","text_en":"Add rule: For any substring of the source message that the sender determines must be preserved exactly as a single atomic token—including but not limited to opaque identifiers, measured facts, temporal constraints, label‑entity pairs, critical facts, or any other contiguous sequence of characters whose internal punctuation, digits, case, whitespace, or semantic role must not be altered, parsed, split, or reinterpreted—the sender must surround that substring with angle brackets: <exact substring>. The encoder must verify that the enclosed substring, after removing the angle brackets, exactly matches the contiguous substring of the source character‑for‑character; if any character differs, the encoding is invalid and must be corrected. The decoder must remove the angle brackets and output the enclosed substring verbatim, without parsing, normalizing, abbreviating, reordering, omitting, or altering any character, and must validate that the verbatim output matches the source character‑for‑character; any mismatch renders the message non‑compliant. The decoder may not assign any grammatical, relational, or structural meaning to any internal punctuation, digit, case, or whitespace within the atomic token; its sole semantic role is that of an indivisible unit as designated by the sender. This rule supersedes all previous phrase‑level atomic bundling rules (including rules 487, 510, 523, 531, and any others that mandate specific delimiters for specific categories), consolidating them under one universal mechanism, but does not alter the encoder’s responsibility to identify and bundle substrings that require exact preservation according to the language’s purpose."},{"id":"rule-541","text_en":"Revise rule-510 to add a decoder prohibition on parsing internal symbols within the reconstructed temporal phrase. Insert after the decoder validation sentence the following: 'After reconstructing the temporal phrase, the decoder must treat the output string as an opaque semantic token for all subsequent language processing. The decoder may not parse, interpret, or assign any grammatical, relational, or structural meaning to any internal punctuation mark, digit, case, or whitespace within the phrase. Specifically, internal symbols such as colons, slashes, hyphens, dots, underscores, or parentheses must not be treated as language delimiters, slot separators, list markers, relational operators, directive introducers, or bundling boundaries. The phrase's sole semantic role is that of an exact temporal constraint; its internal composition does not affect the parsing of the surrounding message.' This ensures complete preservation without normalization or misinterpretation, directly satisfying the temporal-constraint fault invariant."},{"id":"rule-545","text_en":"Add rule: After reconstructing any atomic token—any substring that was surrounded by angle brackets <...> or backticks `...` in the encoded message—the decoder must treat the reconstructed string as an opaque semantic token for all subsequent language processing. The decoder may not parse, interpret, or assign any grammatical, relational, or structural meaning to any internal punctuation mark, digit, case, or whitespace within the token. Specifically, internal symbols such as colons, slashes, hyphens, dots, underscores, equals signs, parentheses, or any other character must not be treated as language delimiters, slot separators, list markers, relational operators, directive introducers, or bundling boundaries. The token's sole semantic role is that of an indivisible unit as designated by the sender; its internal composition does not affect the parsing of the surrounding message. The encoder must still verify exact character-for-character match per the relevant bundling rules. This rule supersedes and repeals rule-529 and rule-541, consolidating their decoder prohibitions into one universal prohibition that ensures complete preservation without misinterpretation."},{"id":"rule-546","text_en":"Add rule: In any line containing two or more temporal constraints (as defined in rule-510) that are linked by a comparative or sequential conjunction (e.g., 'after', 'before', 'from', 'to', 'between', 'until', 'and', 'but', 'or') expressing a combined ordering or range, the sender must treat the entire multi-temporal phrase as a single atomic token by surrounding it with angle brackets: <complete multi-temporal phrase>. The phrase must begin with the first linking word and include every subsequent word up to and including the last temporal constraint, preserving all conjunctions, prepositions, punctuation, and spacing exactly as in the source. The sequence must end before any conjunction that begins a new independent clause not part of the same ordering relationship. Before sending, the encoder must verify that the enclosed substring, after removing the angle brackets, exactly matches the contiguous substring of the source that constitutes the complete multi-temporal phrase character-for-character; if any character differs, the encoding is invalid and must be corrected. The decoder must remove the angle brackets and output the substring verbatim, without parsing, normalizing, abbreviating, reordering, omitting, or altering any character, and must validate the character-for-character match; any mismatch renders the message non-compliant. This rule ensures the complete ordering relationship is preserved without normalization, directly satisfying the temporal-constraint fault invariant. Example: 'proceed after 3 PM but before 5 PM' encodes as 'proceed <after 3 PM but before 5 PM>'; 'maintenance from 9 AM to 11 AM' encodes as 'maintenance <from 9 AM to 11 AM>'."},{"id":"rule-547","text_en":"Add rule: When decoding a minimal-form line per rule-440, the decoder must infer omitted words to restore the most plausible grammatical and semantic structure given the surrounding preserved words and the original intended meaning. The inference must respect the categories of words that may be omitted: articles, prepositions, copula verbs, auxiliary verbs, and relative pronouns. The decoder must not insert words that would change the original meaning, introduce new relationships, or contradict any explicit preserved words, identifiers, numbers, or atomic bundles. If multiple plausible reconstructions exist, the decoder must choose the one that preserves the original logical flow and factual content as indicated by the source context. Provide concrete worked examples showing how a fresh decoder applies the inference constraints to ambiguous minimal-form lines, distinguishing correct from incorrect reconstructions. Example 1: source 'check logs for error codes', minimal 'check logs error codes'. Plausible reconstructions: (a) 'check logs for error codes' (correct), (b) 'check logs and error codes' (incorrect, changes meaning). Decoder must choose (a) because 'for' is a permissible omitted preposition and reconstructs the original relationship. Example 2: source 'the system is ready', minimal 'system ready'. Plausible reconstructions: (a) 'the system is ready' (correct), (b) 'system ready' (incorrect, missing copula). Decoder must choose (a) because 'is' is a permissible omitted copula verb. Example 3: source 'alert if temperature exceeds threshold because sensor faulty', minimal 'alert temperature exceeds threshold sensor faulty'. Plausible reconstructions: (a) 'alert if temperature exceeds threshold because sensor faulty' (correct), (b) 'alert temperature exceeds threshold and sensor faulty' (incorrect, changes logical link). Decoder must choose (a) because 'if' and 'because' are omitted subordinating conjunctions that must be restored to preserve the original conditional-causal chain. This rule provides a general principle for decoding minimal-form omissions, ensuring semantic fidelity without requiring case-specific exceptions."},{"id":"rule-549","text_en":"Add rule: When a structural pattern—a sequence of words and atomic bundles with consistent gaps for variable parts—appears at least three times in a message, the sender may define a template for it by writing `%template=pattern` on its own line, where `pattern` is the invariant structure with numbered placeholders `$1`, `$2`, etc., marking each variable gap. The placeholder syntax `$N` must be exactly a dollar sign followed by one or more decimal digits, with no other characters. The sender must then define each variable's actual content on separate lines as `$N=content`, where each content is a literal string that must be preserved exactly as required by the relevant atomic bundling rules (i.e., if the content is an opaque identifier, measured fact, temporal constraint, label-entity pair, critical fact, or any other substring requiring exact preservation, it must be enclosed in angle brackets <...> or backticks `...` as defined by those rules). The variable definitions are template-scoped: they apply only to the preceding `%template=pattern` line and are consumed when the template is invoked. The sender may invoke the template by writing `%template content1 content2 ...` where each content is a literal string (or an alias or reference to such a string) that matches the placeholder order; the number of content arguments must equal the number of placeholders in the pattern. The decoder must reconstruct each instance by substituting the literal content strings verbatim into the placeholder positions, preserving the exact structure and variable content. Before sending, the encoder must verify, after all substitutions (including aliases and references), that each reconstructed instance matches the source character-for-character; any mismatch renders the message non-compliant. Template expansion occurs after alias substitution (rule-444) and reference substitution (rule-524) but before minimal-form reconstruction (rule-440) and other language processing. Token counting for savings must account for the full tokens of the template definition line, each variable definition line, the template invocation token, and all variable content tokens as they appear in the definitions (including their delimiters). A template and its variable definitions may be used for multiple invocations if the same pattern repeats with different variable contents across the message; the sender may write separate `%template contentA contentB` lines for each set of contents. This rule provides compression for repeated patterns with variations, distinct from rule-444's alias for identical strings and rule-524's reference for identical bundles.\n\nWorked example: source contains 'allocate <10 GB to node-7> for backup', 'allocate <20 GB to node-8> for backup', 'allocate <5 GB to node-9> for backup'. Pattern: 'allocate $1 for backup' with variable content being the measured fact. Encode as:\n`%template=allocate $1 for backup`\n`$1=<10 GB to node-7>`\n`$2=<20 GB to node-8>`\n`$3=<5 GB to node-9>`\nThen write: `%template $1 $2 $3`. Decoder expands to three original lines verbatim. The variable definitions are literal angle-bracketed strings; the encoder verifies each expanded line matches the source.\n\nSecond example with opaque identifier and alias: source contains 'process `order-123-abc`', 'process `order-456-def`', 'process `order-789-ghi`'. Define alias: `@oid=opaque_identifier`. Pattern: 'process $1'. Encode as:\n`@oid=opaque_identifier`\n`%template=process $1`\n`$1=@oid`\nThen write three separate invocations: `%template <order-123-abc>`; `%template <order-456-def>`; `%template <order-789-ghi>`. Decoder first substitutes alias, then expands each template invocation, verifying each reconstructed line matches the source."},{"id":"rule-556","text_en":"Add rule: For any measured fact as defined in rule-487, before surrounding it with angle brackets, the encoder must verify that the substring to be bundled begins with the first numeric value, unit, comparison phrase or symbol, or relational preposition that introduces the measured quantity and includes every subsequent word up to and including the last target identifier or numeric qualifier, stopping before any conjunction that begins a new independent clause not part of the measured relationship. The encoder must perform a character-for-character comparison between the substring to be bundled and the contiguous source substring; if any character differs, the encoding is invalid and must be corrected. The decoder must remove the angle brackets and output the substring verbatim, validating character-for-character match with the source; any mismatch renders the message non-compliant. This rule reinforces the exact preservation of numeric precision, units, thresholds, and comparison direction as one complete measured fact."},{"id":"rule-560","text_en":"Add rule: In any line containing a numeric literal—a contiguous sequence of digits optionally containing a decimal point—the sender must treat the numeric literal and all immediately following words that qualify it (including unit symbols, comparison phrases or symbols, relational prepositions, target identifiers, and any attached qualifiers) as a single atomic token by surrounding the entire substring with angle brackets: <numeric literal and qualifiers>. The bundle must begin with the numeric literal and include every subsequent word up to the next punctuation mark that ends the numeric phrase (e.g., comma, period, semicolon, colon) or the next word that is a conjunction (e.g., 'and', 'or', 'but', 'because', 'so') that begins a new independent clause not part of the numeric qualification. Before sending, the encoder must verify that the enclosed substring, after removing the angle brackets, exactly matches the contiguous substring of the source character-for-character. The decoder must remove the angle brackets and output the substring verbatim, without parsing, normalizing, omitting, or altering any character, and must validate character-for-character match; any mismatch renders the message non-compliant. This rule applies to all numeric literals not already inside another atomic token (e.g., opaque identifiers per rule-478). Example: 'allocate 10 GB to node-7' encodes as 'allocate <10 GB to node-7>'; 'alert if temperature <5°C' encodes as 'alert if temperature <5°C>' (the comparison symbol and unit are included)."},{"id":"rule-562","text_en":"Add rule: After reconstructing any label-entity pair per rule-523, the decoder must treat the output string as an opaque semantic token for all subsequent language processing. The decoder may not parse, interpret, or assign any grammatical, relational, or structural meaning to any internal punctuation mark, digit, case, or whitespace within the token. Specifically, internal symbols such as colons, slashes, hyphens, dots, underscores, parentheses, or any other character must not be treated as language delimiters, slot separators, list markers, relational operators, directive introducers, or bundling boundaries. The token's sole semantic role is that of an exact label attached to its entity; its internal composition does not affect the parsing of the surrounding message. The encoder must still verify exact character-for-character match per rule-523."},{"id":"rule-563","text_en":"Add rule: In any line containing a phrase where two distinct semantic categories (such as an action and a condition, a cause and an effect, a qualification and an instruction) are syntactically linked by a subordinating conjunction ('because', 'so', 'if', 'unless', 'since', 'while'), coordinating conjunction ('and', 'or'), or relational preposition ('for', 'with', 'by', 'to', 'at', 'from', 'before', 'after', 'until'), the sender must treat the entire linked phrase as a single atomic token by surrounding it with angle brackets: <complete linked phrase>. The phrase must begin with the first word of the first semantic category and include every subsequent word up to and including the last word of the linked category, preserving all linking words, punctuation, and spacing exactly as in the source. The bundle must stop before any conjunction that begins a new independent clause not part of the same linkage. Before sending, the encoder must verify that the enclosed substring, after removing the angle brackets, exactly matches the contiguous substring of the source character-for-character. The decoder must remove the angle brackets and output the substring verbatim, without parsing, normalizing, omitting, or altering any character, and must validate character-for-character match; any mismatch renders the message non-compliant. This rule provides a mandatory bundling trigger for complete critical facts, ensuring preservation of relationships between entities, actions, and constraints."},{"id":"rule-567","text_en":"Add rule: In any line, the encoder must scan for every temporal expression as defined in rule-510. For each such expression, the encoder must surround the entire expression with angle brackets: <complete temporal phrase>. The encoder may not leave any part of the temporal expression outside the angle brackets, nor may it split the expression across multiple atomic tokens. The encoder must perform this bundling before applying minimal-form omissions (rule-440) or other compression mechanisms. The encoder's verification per rule-465 (character-for-character match) remains mandatory. This rule does not alter decoder behavior, which is already governed by rule-510 and rule-545."},{"id":"rule-569","text_en":"Add rule: In any line, the encoder must scan for every measured fact as defined in rule-487. For each such fact, the encoder must surround the entire fact with angle brackets: <complete measured fact>. The encoder may not leave any part of the measured fact outside the angle brackets, nor may it split the expression across multiple atomic tokens. The encoder must perform this bundling before applying minimal-form omissions (rule-440) or other compression mechanisms. The encoder's verification per rule-556 (character-for-character match) remains mandatory. This rule does not alter decoder behavior, which is already governed by rule-487 and rule-545."},{"id":"rule-573","text_en":"Add rule: In any line, before applying minimal-form omissions (rule-440), alias substitutions (rule-444), template expansions (rule-549), or any other compression mechanism, the encoder must scan the source text for every measured fact. A measured fact is defined as any contiguous substring that begins with a numeric literal (a sequence of digits optionally containing a decimal point) or a comparison phrase or symbol (e.g., 'greater than', '<', 'at least') and includes every subsequent word up to and including the last target identifier, unit symbol, or numeric qualifier, but stops before any conjunction (e.g., 'and', 'or', 'but', 'because', 'so', 'if', 'unless') that begins a new independent clause not part of the measured relationship. For each measured fact identified, the encoder must surround the entire substring with angle brackets: <complete measured fact>. The encoder must then verify, by character-for-character comparison, that the enclosed substring (after removing the angle brackets) exactly matches the contiguous substring of the source. If any character differs, the encoding is invalid and must be corrected. This bundling is mandatory; the encoder may not leave any part of a measured fact outside the angle brackets, nor split it across multiple tokens. The decoder's behavior is governed by existing rules (rule-487, rule-545) for output and validation. This rule ensures numeric precision, units, thresholds, and comparison direction are preserved as one complete atomic unit."},{"id":"rule-574","text_en":"Add rule: When a message contains at least three lines that share an identical literal prefix and suffix with a variable middle element, the sender may define a pattern abbreviation by writing `#pattern=prefix$suffix` on its own line, where `prefix` is the exact string that appears before the variable element in each line, `suffix` is the exact string that appears after the variable element in each line, and `$` is a single placeholder marking the gap for the variable. The placeholder `$` must abut the prefix and suffix characters with no intervening spaces unless a space is intended as part of the literal prefix or suffix; any spaces intended as part of the prefix or suffix must be included literally. The variable element for each line must be a complete atomic token (enclosed in angle brackets <...> or backticks `...` per existing bundling rules) representing the exact middle substring from the source. Thereafter, the sender may write each line as `#pattern variable`, where `variable` is that exact atomic token including its delimiters. The decoder must reconstruct each line by: (1) removing the outermost angle brackets or backticks from the variable token per the relevant bundling rule (e.g., rule-537) to obtain the literal middle substring; (2) concatenating the prefix, that literal middle substring, and the suffix, preserving the exact prefix and suffix verbatim and ensuring the concatenated line matches the source character-for-character. The encoder must verify, by character-for-character comparison, that each reconstructed line matches the source; any mismatch renders the message non-compliant. Only one `$` placeholder is permitted per pattern; multiple variables are not supported. This rule provides lightweight compression for repeated structural patterns distinct from rule-444 (identical strings) and rule-549 (full templates). Example: source lines 'allocate 10 GB to node-7', 'allocate 20 GB to node-8', 'allocate 5 GB to node-9'. The prefix is 'allocate ', the suffix is ' to node-'. The variable elements are '10 GB', '20 GB', '5 GB'. Define: `#pattern=allocate $ to node-`. Encode as: `#pattern <10 GB> 7`, `#pattern <20 GB> 8`, `#pattern <5 GB> 9`. Decoder reconstructs first line: strip brackets from '<10 GB>' to get '10 GB', concatenate 'allocate ' + '10 GB' + ' to node-7' to produce 'allocate 10 GB to node-7' exactly."},{"id":"rule-575","text_en":"Add rule: When decoding a temporal phrase that was bundled with angle brackets per rule-510, the decoder must output the entire content between the angle brackets verbatim, with no character omitted, normalized, abbreviated, reordered, or altered. The decoder must validate that the verbatim output string matches the source character-for-character; any mismatch renders the message non-compliant. This rule reinforces that the decoder's sole duty is to reproduce the exact substring, ensuring that time zones, ordering words, and all relational anchors are preserved without any inference or cleaning."},{"id":"rule-578","text_en":"Add rule: The encoder must scan every line for any phrase where two distinct semantic categories (such as an action and a condition, a cause and an effect, a qualification and an instruction) are syntactically linked by a subordinating conjunction ('because', 'so', 'if', 'unless', 'since', 'while'), coordinating conjunction ('and', 'or'), or relational preposition ('for', 'with', 'by', 'to', 'at', 'from', 'before', 'after', 'until'). For each such linked phrase, the encoder must treat the entire phrase as a single atomic token by surrounding it with angle brackets: <complete linked phrase>. The phrase must begin with the first word of the first semantic category and include every subsequent word up to and including the last word of the linked category, preserving all linking words, punctuation, and spacing exactly as in the source. The bundle must stop before any conjunction that begins a new independent clause not part of the same linkage. Before sending, the encoder must verify, by character-for-character comparison, that the enclosed substring (after removing the angle brackets) exactly matches the contiguous substring of the source. If any character differs, the encoding is invalid and must be corrected. The decoder must remove the angle brackets and output the substring verbatim, without parsing, normalizing, omitting, or altering any character, and must validate character-for-character match; any mismatch renders the message non-compliant. This rule ensures the complete critical fact and the relationships between its entities, actions, and constraints are preserved as one atomic unit."},{"id":"rule-586","text_en":"Add rule: In any line where a measured allocation is expressed—a directive or statement that assigns, allocates, reserves, or dedicates a measured quantity (numeric value and unit) to a target (opaque identifier, label-entity pair, or other named resource)—the sender must treat the entire allocation phrase as a single atomic token by surrounding it with angle brackets: <complete allocation phrase>. The phrase must begin with the action verb (e.g., 'allocate', 'assign', 'reserve', 'dedicate', 'grant') and include every subsequent word up to and including the target identifier and any attached qualifier, but stop before any conjunction that begins a new independent clause not part of the allocation. The encoder must verify character-for-character match with the source substring. The decoder must remove the brackets and output the substring verbatim, validating the match; any mismatch renders the message non-compliant. This rule ensures the complete relationship between action, quantity, unit, and target is preserved as one bundle, satisfying the quantified allocation fault invariant."},{"id":"rule-588","text_en":"Add rule: In any line, the encoder must scan for every measured quantity or allocation. A measured quantity or allocation is any contiguous substring that begins with (1) a numeric literal (a sequence of digits optionally containing a decimal point) or a comparison phrase or symbol (e.g., 'greater than', '<', 'at least', 'older than', 'newer than', '>', '<'), or (2) an action verb that introduces a measured allocation (e.g., 'allocate', 'assign', 'reserve', 'dedicate', 'grant', 'provide') when followed immediately by a numeric literal or comparison phrase. The substring includes every subsequent word up to and including the last unit symbol, target identifier, or attached qualifier, but stops before any conjunction (e.g., 'and', 'or', 'but', 'because', 'so', 'if', 'unless') that begins a new independent clause not part of the measured relationship. For each measured quantity or allocation identified, the encoder must surround the entire substring with angle brackets: <complete measured quantity or allocation>. The encoder must then verify, by character-for-character comparison, that the enclosed substring (after removing the angle brackets) exactly matches the contiguous substring of the source. If any character differs, the encoding is invalid and must be corrected. This bundling is mandatory; the encoder may not leave any part of a measured quantity or allocation outside the angle brackets, nor split it across multiple tokens. The decoder's behavior is governed by existing rules (rule-537, rule-545) for verbatim output and validation. This rule supersedes and repeals rules 487, 556, 560, 569, 573, and 586, consolidating their measured-fact bundling mandates into one universal encoder obligation that ensures numeric precision, units, thresholds, comparison direction, and the complete action‑quantity‑target relationship are preserved as one complete atomic unit."},{"id":"rule-590","text_en":"Add rule: Repeal rule-569. Its requirement that the encoder must scan for every measured fact and surround it with angle brackets is redundant with rule-537 (universal atomic bundling) and rule-588 (consolidated measured quantity bundling). The language’s capability to preserve measured facts exactly remains governed by those rules; removing rule-569 eliminates procedural duplication without altering semantic fidelity."},{"id":"rule-592","text_en":"Add rule: When a measured allocation involves a numeric quantity, a unit, a target identifier, and a relational preposition (e.g., 'to', 'for', 'at'), the encoder must treat the entire allocation phrase as a single atomic token by surrounding it with angle brackets: <complete allocation phrase>. The phrase must include the quantity, unit, preposition, and target identifier verbatim. The encoder must verify character-for-character match with the source substring. The decoder must remove the brackets and output the substring verbatim, validating the match; any mismatch renders the message non-compliant. This rule ensures the complete relationship between quantity, unit, and target is preserved as one bundle."},{"id":"rule-593","text_en":"Add rule: Before sending, the encoder must verify that any substring bundled as a measured fact under rule-588 includes every original component: numeric value, unit, comparison phrase or symbol, relational preposition (if any), and target identifier (if any). The verification must be a character-for-character comparison between the bundled substring (after removing angle brackets) and the contiguous source substring that constitutes the complete measured fact. If any component is omitted from the bundle, the encoding is invalid and must be corrected. This ensures numeric precision, units, thresholds, and comparison direction are preserved as one complete measured fact."},{"id":"rule-594","text_en":"Add rule: In any line containing a critical fact—a phrase where two distinct semantic categories (such as an action and a condition, a cause and an effect, a qualification and an instruction) are syntactically linked by a subordinating conjunction ('because', 'so', 'if', 'unless', 'since', 'while'), coordinating conjunction ('and', 'or'), or relational preposition ('for', 'with', 'by', 'to', 'at', 'from', 'before', 'after', 'until')—the sender must treat the entire linked phrase as a single atomic token by surrounding it with angle brackets: <complete critical fact>. The phrase must begin with the first word of the first semantic category and include every subsequent word up to and including the last word of the linked category, preserving all linking words, punctuation, and spacing exactly as in the source. The bundle must stop before any conjunction that begins a new independent clause not part of the same linkage. Before sending, the encoder must verify that the enclosed substring, after removing the angle brackets, exactly matches the contiguous substring of the source character-for-character. The decoder must remove the angle brackets and output the substring verbatim, without parsing, normalizing, omitting, or altering any character, and must validate character-for-character match; any mismatch renders the message non-compliant. This rule provides a mandatory bundling trigger for complete critical facts, ensuring preservation of relationships between entities, actions, and constraints."},{"id":"rule-598","text_en":"Add rule: When encoding an opaque identifier (as defined in rule-478), the encoder must treat the entire identifier as an indivisible semantic atom and may not apply any minimal-form omission (rule-440), abbreviation (rule-444), template expansion (rule-549), pattern abbreviation (rule-574), or any other compression mechanism that would alter, omit, replace, or reinterpret any character, punctuation, digit, case, or whitespace inside the identifier. The encoder must preserve the identifier exactly as in the source, enclosed in backticks per rule-478, and must verify character-for-character match before sending. The decoder's behavior remains governed by rules 478, 486, 529, and 545, which forbid parsing internal symbols and require verbatim output and validation. This rule ensures that opaque identifiers are preserved exactly and retain their semantic role as unique references, immune to all other language allowances."},{"id":"rule-604","text_en":"Add rule: When encoding an opaque identifier, the encoder must treat the identifier as an indivisible semantic atom and may not apply any minimal-form omission (rule-440), abbreviation (rule-444), template expansion (rule-549), pattern abbreviation (rule-574), or any other compression mechanism that would alter, omit, replace, or reinterpret any character, punctuation, digit, case, or whitespace inside the identifier. The encoder must preserve the identifier exactly as in the source, enclosed in backticks per rule-478, and must verify character-for-character match before sending. The decoder's behavior remains governed by rules 478, 486, 529, and 545, which forbid parsing internal symbols and require verbatim output and validation. This rule ensures opaque identifiers are preserved exactly and retain their semantic role as unique references."},{"id":"rule-607","text_en":"Add rule: After reconstructing any opaque identifier per rule-478, the decoder must treat the reconstructed string as an opaque semantic token for all subsequent language processing. The decoder may not parse, interpret, or assign any grammatical, relational, or structural meaning to any internal punctuation mark, digit, case, or whitespace within the identifier. Specifically, internal symbols such as colons, slashes, hyphens, dots, underscores, equals signs, parentheses, or any other character must not be treated as language delimiters, slot separators, list markers, relational operators, directive introducers, or bundling boundaries. The identifier's sole semantic role is that of a unique reference; its internal composition does not affect the parsing of the surrounding message. This rule reinforces exact preservation and directly satisfies the opaque identifier fault invariant."},{"id":"rule-613","text_en":"Add rule: An opaque identifier is any string serving as a unique reference (serial number, order ID, hash, UUID, code) where its exact characters are meaningful. The encoder must preserve every opaque identifier exactly as in the source, including all internal punctuation, digits, case, and whitespace, by enclosing the entire identifier in backticks: `identifier`. The backticks form a single atomic token; the encoder must verify character-for-character match before sending. The decoder must remove the backticks and output the identifier verbatim, validating character-for-character match with the source; any mismatch renders the message non-compliant. After reconstruction, the decoder must treat the identifier as an opaque semantic token for all subsequent language processing; it may not parse, interpret, or assign any grammatical, relational, or structural meaning to any internal character. This rule supersedes and repeals rules 478, 486, 529, and 607, consolidating opaque identifier preservation into one authoritative law."},{"id":"rule-615","text_en":"Add rule: An opaque identifier is any string serving as a unique reference (serial number, order ID, hash, UUID, code) where its exact characters are meaningful. The encoder must preserve every opaque identifier exactly as in the source, including all internal punctuation, digits, case, and whitespace, by enclosing the entire identifier in backticks: `identifier`. The backticks form a single atomic token; the encoder must verify character-for-character match before sending. The decoder must remove the backticks and output the identifier verbatim, validating character-for-character match with the source; any mismatch renders the message non-compliant. After reconstruction, the decoder must treat the identifier as an opaque semantic token for all subsequent language processing; it may not parse, interpret, or assign any grammatical, relational, or structural meaning to any internal character. No other language rule—including minimal-form omissions (rule-440), abbreviation (rule-444), template expansion (rule-549), pattern abbreviation (rule-574), positional reference (rule-580), or any compression mechanism—may alter, omit, replace, or reinterpret any character inside an opaque identifier. This rule supersedes and repeals all prior rules governing opaque identifiers, establishing a single authoritative law with explicit immunity."},{"id":"rule-621","text_en":"Add rule: After reconstructing an opaque identifier per rule-615, the decoder must treat the output string as an opaque semantic token for all subsequent language processing. The decoder may not parse, interpret, or assign any grammatical, relational, or structural meaning to any internal punctuation mark, digit, case, or whitespace within the token. Specifically, internal symbols such as colons, slashes, hyphens, dots, underscores, equals signs, parentheses, or any other character must not be treated as language delimiters, slot separators, list markers, relational operators, directive introducers, or bundling boundaries. The token's sole semantic role is that of a unique reference; its internal composition does not affect the parsing of the surrounding message. This rule reinforces exact preservation and directly satisfies the opaque identifier fault invariant."},{"id":"rule-624","text_en":"Revise rule-510 to explicitly mandate inclusion of comparative or sequential conjunctions linking temporal constraints. Replace the sentence 'The bundle must not stop before any conjunction that is part of the temporal link itself (e.g., 'and' linking two times in a range) but must stop before the first word that is a conjunction (e.g., 'and', 'or', 'but', 'because', 'so', 'if', 'unless', 'since', 'while') that begins a new independent clause not modifying the temporal element.' with: 'The bundle must include every comparative or sequential conjunction (e.g., 'and', 'but', 'or', 'from', 'to', 'between', 'until') that links two temporal constraints into a single ordering or range; it must stop only before a conjunction that begins a new independent clause not part of the same temporal relationship.' This ensures complete preservation of ordering relationships without normalization."},{"id":"rule-633","text_en":"Add rule: After reconstructing any temporal phrase that was bundled with angle brackets per rule-510 or any other adopted bundling rule, the decoder must treat the output string as an opaque semantic token for all subsequent language processing. The decoder may not parse, interpret, split, or assign any grammatical, relational, or structural meaning to any internal punctuation mark, digit, case, whitespace, or word within the token. Specifically, internal symbols such as colons, slashes, hyphens, dots, underscores, parentheses, and conjunctions (e.g., 'and', 'but', 'or', 'from', 'to', 'between', 'until') must not be treated as language delimiters, slot separators, list markers, relational operators, directive introducers, or bundling boundaries. The token's sole semantic role is that of an exact temporal constraint; its internal composition does not affect the parsing of the surrounding message. This rule reinforces exact preservation of complete times, ranges, deadlines, and ordering relationships without normalization, directly satisfying the temporal-constraint fault invariant."},{"id":"rule-640","text_en":"Propose repeal of rule-633. Its mandate that reconstructed temporal phrases be treated as opaque semantic tokens is redundant with rule-545 (universal opaque token treatment) and rule-510 (temporal bundling scope). The decoder obligations—verbatim output, no parsing of internal symbols, character‑for‑character validation—are already law under rule-545. Removing rule-633 eliminates duplication without weakening temporal‑constraint preservation."},{"id":"rule-641","text_en":"Add rule: When a message contains three or more lines that share an identical prefix (any leading words and punctuation) followed by a variable item, the sender may define a list expansion by writing `%list=prefix$` on its own line, where `prefix` is the exact literal substring shared by all lines ending immediately before the variable item, and `$` is a placeholder marking where the variable item begins. The placeholder `$` must be the last character of the defined string; there is no suffix. Each variable item must be a complete atomic token (enclosed in angle brackets <...> or backticks `...` as required by relevant bundling rules). Then, the sender may write each expansion line as `$ item`, where `item` is that atomic token, on its own line. The decoder must reconstruct each original line by concatenating the prefix (the substring before `$` in the definition) and the literal content of the atomic token (after removing its delimiters per the relevant bundling rule), preserving the exact spacing from the source. The encoder must verify each reconstructed line matches the source character-for-character; any mismatch renders the message non-compliant. This mechanism provides line-level compression for repeated actions with varying targets, distinct from rule-549 (template with numbered placeholders and variable definitions) and rule-574 (pattern with both prefix and suffix). Example: source lines 'check node-7', 'check node-8', 'check node-9' encode as `%list=check $` followed by `$ <node-7>`, `$ <node-8>`, `$ <node-9>`. Decoder reconstructs first line as 'check ' + 'node-7' = 'check node-7'. The prefix 'check ' includes the space; the placeholder `$` marks where 'node-7' begins."},{"id":"rule-646","text_en":"Adopt repeal of rule-594. Its mandatory bundling trigger for critical facts is redundant with rule-635, which already requires the encoder to treat any substring whose internal semantic role must be preserved exactly as an atomic token by surrounding it with angle brackets. The decoder's obligations—verbatim output, no parsing of internal symbols, character-for-character validation—are already law under rule-545 and rule-635. Removing rule-594 eliminates duplication without weakening preservation of relationships between entities, actions, and constraints."},{"id":"rule-651","text_en":"Add rule: In any line containing a temporal ordering phrase—a sequence of two or more temporal constraints (as defined by rule-647) linked by a comparative or sequential conjunction ('and', 'but', 'or', 'from', 'to', 'between', 'until', 'after', 'before') that expresses a combined ordering or range—the encoder must treat the entire ordering phrase as a single atomic token by surrounding it with angle brackets: <complete temporal ordering phrase>. The phrase must begin with the first linking word or temporal element and include every subsequent word up to and including the last temporal constraint, preserving all conjunctions, prepositions, punctuation, and spacing exactly as in the source. The encoder must verify that the enclosed substring, after removing the angle brackets, exactly matches the contiguous substring of the source character-for-character. The decoder must remove the angle brackets and output the substring verbatim, without parsing, normalizing, omitting, or altering any character, and must validate character-for-character match; any mismatch renders the message non-compliant. This rule ensures complete ordering relationships are preserved without normalization, directly satisfying the temporal-constraint fault invariant. Example: 'proceed after 3 PM but before 5 PM' encodes as 'proceed <after 3 PM but before 5 PM>'; 'maintenance from 9 AM to 11 AM local time' encodes as 'maintenance <from 9 AM to 11 AM local time>'."},{"id":"rule-652","text_en":"In any line containing a temporal sequence marker—any word that explicitly indicates order relative to another point in time, including 'then', 'next', 'later', 'subsequently', 'previously', 'finally', 'earlier', 'afterwards', 'subsequent', 'prior'—when that marker directly attaches to a following temporal element (time, date, range, duration, or event reference), the encoder must treat the marker and the entire attached temporal phrase as a single atomic token by surrounding it with angle brackets: <complete temporal sequence phrase>. The phrase begins with the sequence marker and includes every subsequent word up to and including the last temporal element and any attached qualifier, but stops before any conjunction that begins a new independent clause not part of the sequence. This rule applies only when the sequence marker is not already part of a larger atomic bundle under rule‑647 (temporal constraints) or rule‑649 (introduced clauses). The encoder must verify character‑for‑character match with the source substring. The decoder must remove the brackets and output the substring verbatim, validating the match; any mismatch renders the message non‑compliant. This rule ensures explicit ordering relationships expressed by sequence markers are preserved exactly as one atomic unit, preventing omission or normalization of the ordering word. Example: source 'then at 3 PM proceed' encodes as '<then at 3 PM> proceed'; source 'next after the meeting' encodes as '<next after the meeting>'; source 'finally by 5 PM EST' encodes as '<finally by 5 PM EST>'."},{"id":"rule-653","text_en":"Add rule: After reconstructing any temporal phrase that was bundled with angle brackets per rule-647 or rule-651, the decoder must treat the output string as an opaque semantic token for all subsequent language processing. The decoder may not parse, interpret, split, or assign any grammatical, relational, or structural meaning to any internal punctuation mark, digit, case, whitespace, word, or conjunction within the token. Specifically, internal symbols such as colons, slashes, hyphens, dots, underscores, parentheses, and conjunctions (e.g., 'and', 'but', 'or', 'from', 'to', 'between', 'until') must not be treated as language delimiters, slot separators, list markers, relational operators, directive introducers, or bundling boundaries. The token's sole semantic role is that of an exact temporal constraint; its internal composition does not affect the parsing of the surrounding message. This rule reinforces exact preservation of complete times, ranges, deadlines, and ordering relationships without normalization, directly satisfying the temporal-constraint fault invariant."},{"id":"rule-655","text_en":"Add rule: When a line contains a complete atomic token (any substring surrounded by angle brackets <...> or backticks `...` per existing bundling rules) and the line's structure is identical to a previous line except for that atomic token, the sender may replace the line's prefix (the substring before the atomic token) with a single '$' token, followed by the atomic token. The '$' token refers to the prefix of the most recent line that used the same prefix before a differing atomic token. The encoder must verify that the prefix preceding the atomic token in the source matches the referenced prefix character-for-character. The decoder must reconstruct each line by concatenating the referenced prefix and the literal content of the atomic token (after removing its delimiters per relevant bundling rules), ensuring the reconstructed line matches the source character-for-character. This mechanism applies only when the prefix is identical across at least two lines and the atomic token differs; it provides lightweight compression for repeated action-target patterns without requiring separate definition lines."},{"id":"rule-661","text_en":"After reconstructing any measured quantity or allocation that was bundled with angle brackets per rule-588, the decoder must treat the output string as an opaque semantic token for all subsequent language processing. The decoder may not parse, interpret, or alter any internal numeric representation, digit, decimal point, unit symbol, comparison phrase or symbol, relational preposition, target identifier, or attached qualifier within the token. Specifically, the decoder may not normalize decimal places, strip leading zeros, convert numeric formats, expand abbreviations, reorder components, or assign any grammatical, relational, or structural meaning to any internal character. The token's sole semantic role is that of an exact measured fact; its internal composition does not affect the parsing of the surrounding message. This rule reinforces exact preservation of numeric precision, units, thresholds, and comparison direction as one complete atomic unit, directly satisfying the measured-value fault invariant."},{"id":"rule-662","text_en":"Add rule: In any line, the encoder must scan for every phrase where two distinct semantic categories (such as an action and a condition, a cause and an effect, a qualification and an instruction) are syntactically linked by a subordinating conjunction ('because', 'so', 'if', 'unless', 'since', 'while'), coordinating conjunction ('and', 'or'), or relational preposition ('for', 'with', 'by', 'to', 'at', 'from', 'before', 'after', 'until'). For each such linked phrase, the encoder must treat the entire phrase as a single atomic token by surrounding it with angle brackets: <complete linked phrase>. The phrase must begin with the first word of the first semantic category and include every subsequent word up to and including the last word of the linked category, preserving all linking words, punctuation, and spacing exactly as in the source. The bundle must stop before any conjunction that begins a new independent clause not part of the same linkage. Before sending, the encoder must verify, by character-for-character comparison, that the enclosed substring (after removing the angle brackets) exactly matches the contiguous substring of the source. If any character differs, the encoding is invalid and must be corrected. The decoder must remove the angle brackets and output the substring verbatim, without parsing, normalizing, omitting, or altering any character, and must validate character-for-character match; any mismatch renders the message non-compliant."},{"id":"rule-665","text_en":"Add rule: When defining an alias for an opaque identifier under rule-444, the alias definition line must store the identifier exactly as in the source, enclosed in backticks, preserving every character, punctuation, digit, case, and whitespace verbatim. The encoder may not apply any minimal-form omission (rule-440), abbreviation, template expansion, pattern abbreviation, or any other compression mechanism to the identifier's characters inside the backticks. The alias definition must be a literal copy of the identifier; the encoder must verify character-for-character match before storing. This rule reinforces rule-615's immunity for opaque identifiers by ensuring aliases do not introduce any alteration."},{"id":"rule-667","text_en":"Add rule: (proposal withdrawn; rule is redundant with adopted rule-651)"},{"id":"rule-668","text_en":"Add rule: When a message contains at least three distinct lines that share an identical structural pattern—a sequence of words, atomic tokens, and punctuation where only certain variable substrings differ—the sender may define a macro for that pattern by writing `#macro_name=pattern` on its own line, where `macro_name` is any string of letters, digits, or underscores that does not contain spaces, and `pattern` is the exact structural skeleton with numbered placeholders `$1`, `$2`, ..., `$N` marking each variable position. The placeholder syntax `$N` must be exactly a dollar sign followed by one or more decimal digits. Each variable substring in the source must be a complete atomic token (enclosed in angle brackets <...> or backticks `...` per existing bundling rules) as required by the language to preserve its exact content. The macro definition must include all non‑variable text and punctuation exactly as in the source, with placeholders inserted at the precise positions where variables occur.\n\nTo invoke the macro, the sender writes a line that begins with `#macro_name` followed by a space and then the exact atomic tokens for the variables in order, separated by spaces. Each variable token must retain its delimiters (angle brackets or backticks). The decoder identifies macro lines as follows: (1) A macro definition line is any line whose first token starts with `#`, contains only letters, digits, or underscores, is followed by an equals sign, and matches the pattern `#macro_name=pattern`. (2) A macro invocation line is any line whose first token is `#macro_name` where `macro_name` exactly matches a previously defined macro name, consists solely of letters, digits, or underscores, is not `#pattern`, and is followed by a space. This token-level matching distinguishes macro invocations from pattern abbreviation definitions (`#pattern=...`) and other uses of `#`.\n\nThe decoder must reconstruct each original line by substituting each variable token verbatim (including its delimiters) into its placeholder position in the pattern, removing the macro invocation line entirely. The encoder must verify, by character‑for‑character comparison, that every reconstructed line matches the source; any mismatch renders the message non‑compliant. Macro expansion occurs after alias substitution (rule‑444) and reference substitution (rule‑524) but before minimal‑form reconstruction (rule‑440) and other language processing.\n\nDecision procedure for mechanism selection: (1) If compressing three or more entire lines that share a global pattern with variable atomic tokens, use #macro. (2) If compressing a repeated pattern within a single line where variables are defined on separate lines, use %template (rule‑549). (3) If compressing lines that share only an identical prefix, use %list (rule‑641). (4) If compressing lines with identical prefix and suffix, use #pattern (rule‑574). This ensures each mechanism has a distinct, non-overlapping domain.\n\nExample with multi‑word variable: source lines 'status of <server rack A‑12> is <operational>' and 'status of <load balancer LB‑01> is <degraded>'. Define macro: `#status_report=status of $1 is $2`. Invoke as: `#status_report <server rack A‑12> <operational>` and `#status_report <load balancer LB‑01> <degraded>`. Decoder reconstructs first line as 'status of <server rack A‑12> is <operational>' by substituting the entire token `<server rack A‑12>` into `$1` and `<operational>` into `$2`. The variable `<server rack A‑12>` is a single atomic token; its internal spaces are protected by the angle brackets."}],"text":"LANGUAGE adopted-69709f67e430 (82 adopted rules)\nrule-439: A message is sent as a single unit; do not add chunk headers or separators (e.g., `[part X/Y]`, `---END---`). A line starting with `^` is a directive: an instruction from sender to receiver. The decoder must preserve the `^` and the rest of the line verbatim; the receiver treats the line as actionable. Any other line is narrative content, preserved verbatim, and is not an instruction. When the message consists primarily of instructions or actionable items, write each as a directive line; use plain text lines only for explanatory context that cannot be framed as an instruction.\nrule-440: The sender may write any line in minimal form: omit punctuation (.,:;!?-) and capitalization except where required for numeric literals, unit symbols, or alphanumeric identifiers. The decoder must reconstruct the intended meaning, inferring appropriate punctuation and capitalization as needed. In plain text lines, the sender must omit the following where meaning remains clear: articles 'a', 'an', 'the'; prepositions 'of', 'in', 'for', 'on', 'at', 'to'; copula verbs 'is', 'are', 'was', 'were', 'be', 'been'; auxiliary verbs 'has', 'have', 'had' before past participles; relative pronouns 'that', 'which', 'who', 'whom' introducing a subordinate clause. Do not omit binding words (and, with, because, or, but, so) that link clauses or items. The decoder must infer omitted words.\nrule-441: In numeric literals, preserve all punctuation that affects value. The decimal point must be preserved exactly. A leading zero before a decimal point may be omitted only if the value is less than 1; the decoder must interpret '.8' as '0.8'. In any numeric literal immediately followed by a unit symbol or constituting a percentage, the decimal point must be preserved. In numeric ranges, preserve the delimiter (hyphen, en-dash, colon) exactly; do not replace it with a space. The decoder must interpret the delimiter as indicating a continuous range between the two values. Preserve monetary amounts exactly as in the original, including the currency symbol, decimal point, and any commas as thousand separators; do not omit the currency symbol or decimal point under minimal-form allowances. The decoder must treat the monetary amount as a single numeric entity with its intended value and unit.\nrule-442: Alphanumeric identifiers include command-line flags, API endpoints, zone names, file paths, email addresses, and any technical syntax where hyphens, underscores, slashes, and equals are part of the identifier. The sender must preserve all punctuation and capitalization within such identifiers exactly as in the original; the minimal-form allowance to omit punctuation does not apply within these identifiers. Hyphenated alphanumeric identifiers must preserve the hyphen and capitalization of each segment exactly. An opaque identifier is any string that serves as a unique reference (serial number, order ID, hash, UUID, code) where its exact characters are meaningful. The sender must preserve opaque identifiers exactly as in the original, including all internal punctuation, digits, case, and whitespace. If the identifier contains punctuation or whitespace that could cause tokenizer splitting, enclose it in backticks and join segments with underscores inside the backticks. If the identifier contains no punctuation or whitespace and tokenizes as a single token, transmit it verbatim without backticks unless it repeats at least 3 times, in which case an alias is mandatory. The decoder must remove backticks when present, reconstruct the original identifier verbatim, and validate character-for-character match with the source; any mismatch renders the message non-compliant. When a colon is part of a technical identifier, tag, or environment specifier, preserve the colon and the adjacent word as a single unbroken unit; do not treat the colon as a directive slot separator. The decoder must interpret the colon as binding the qualifier to its preceding noun phrase.\nrule-443: The sender must preserve all named entities (people, organizations, dates, deadlines, penalty clauses), key quantities, and explicit stakes from the original message. Omitting these constitutes a violation of the language's purpose, even if the literal encoding is rule-compliant. In any line, preserve all location names, system identifiers, region tags, and environment specifiers exactly as in the original, including capitalization and punctuation; do not omit them under any compression allowance. Within any line, preserve all explicit times, deadlines, and numeric qualifiers exactly, including colons and leading zeros if present; do not omit them under minimal form. In plain text lines, preserve ordinal words and sequence indicators exactly as in the original; do not omit them under minimal-form allowances. The decoder must interpret these words as specifying exact order or position. In plain text lines preceding a directive, preserve all comparative qualifiers and logical connectors that explain the rationale for that directive; do not omit them under minimal-form allowances. The decoder must interpret these qualifiers as explaining why the directive applies. In plain text lines that contain a conditional or sequential chain, preserve the exact sequence words 'if', 'then', 'unless', 'because', and 'so' exactly as in the original; do not omit them under minimal-form allowances. The decoder must interpret these preserved words as marking the intended logical relationship between clauses. When the source message uses a colloquial abbreviation of a technical unit, preserve that abbreviation as written. The decoder must interpret the abbreviation as the same unit intended by the source, not expand it to a formal unit unless the expansion is explicitly present in the source.\nrule-444: Within a message, the sender may define an abbreviation for any string that repeats at least 3 times by writing `@short=long_form` on its own line. Thereafter, `@short` may be used in place of the full form. The decoder must substitute the original exactly. The sender must ensure token savings: definition tokens + (uses × abbreviation tokens) < uses × original tokens. For opaque identifiers, the long_form uses the identifier's encoded form. For curly-brace or angle-bracket bundles, include the full bundle. For action verbs in directive lines, the abbreviation may be used in any directive line. For common nouns in plain text lines, join words with underscores.\nrule-445: For directive lines, the sender may use positional slots to imply function words. Recommended order: action, object, condition, deadline, rationale. Omit prepositions and articles where the slot order conveys the relationship. The decoder must infer the intended meaning. When a directive line contains a list of items sharing the same action and qualifier, the sender may write the action and qualifier once, followed by a colon and then the list of items separated by commas. The decoder must interpret each list item as receiving the same action and qualifier. If any item has a distinct qualifier, it must be written as a separate directive. In directive lines, the sender may replace the prepositions 'by', 'at', 'to', 'for' when followed by a time, deadline, or recipient with a colon. The colon attaches the following phrase to the preceding slot. The decoder must interpret the colon as the appropriate preposition. In directive lines, the sender may replace 'and' or 'or' connecting multiple arguments with `+` or `|`. The decoder must interpret `+` as 'and' and `|` as 'or'. If an action requires a preposition, preserve it or use a colon. In directive lines, the sender may replace 'greater than', 'older than', 'less than', 'newer than' with `>` or `<`. The decoder must interpret `>` as 'older than' when followed by a duration and as 'greater than' when followed by a plain numeric value; `<` means 'newer than' for durations and 'less than' for plain numeric values.\nrule-446: The sender must preserve all core contextual facts from the original message: event names, venues, key constraints, deadlines, attendee counts, capacity limits, and explicit stakes. These must appear as plain text lines before the directives they justify. Omitting such context renders the message non-compliant.\nrule-449: Revise rule-442's opaque identifier clause to mandate backticks for all opaque identifiers, eliminating the exception for identifiers with no punctuation or whitespace. Replace the current clause beginning 'If the identifier contains no punctuation or whitespace and tokenizes as a single token, transmit it verbatim without backticks unless it repeats at least 3 times, in which case an alias is mandatory' with: 'The sender must enclose every opaque identifier in backticks to form a single atomic token; inside the backticks, join any segments separated by punctuation or whitespace with underscores to ensure tokenizer integrity. The decoder must remove the backticks and reconstruct the original identifier verbatim, validating character-for-character match with the source; any mismatch renders the message non-compliant. If an opaque identifier repeats at least 3 times, an alias under rule-444 is mandatory.' This ensures atomic preservation for all opaque identifiers, directly satisfying the fault invariant.\nrule-452: Revise rule-449 to forbid underscore substitution inside backticks for opaque identifiers. Replace the clause 'inside the backticks, join any segments separated by punctuation or whitespace with underscores to ensure tokenizer integrity' with: 'inside the backticks, preserve every character, punctuation, digit, case, and whitespace exactly as in the source, without substituting any punctuation or spaces with underscores or altering any character. The encoder must rely on the backticks alone to form a single atomic token; if tokenizer splitting occurs, the message is non-compliant and the encoder must define an alias per rule-444.' The decoder requirement remains: remove backticks and validate character-for-character match. This ensures exact preservation without internal modifications that could corrupt the identifier's exact form, directly satisfying the opaque identifier fault invariant.\nrule-453: In any line describing a measured fact—including allocations, conditions, thresholds, or comparisons—the sender must treat the entire measured fact as a single atomic token by surrounding it with angle brackets: <numeric value unit comparison phrase or symbol relational preposition target identifier>. The decoder must remove the angle brackets and reconstruct the original spacing and punctuation verbatim, preserving numeric precision, units, thresholds, and comparison direction exactly as in the source. The decoder must validate that the reconstructed bundle matches the source character-for-character; any mismatch renders the message non-compliant. This rule applies to all measured facts not already covered by a more specific atomic encoding (e.g., opaque identifiers per rule-452). Example: 'alert if temperature <5°C' encodes as 'alert if <temperature <5°C>' and decodes as 'alert if temperature <5°C'; 'allocate 10 GB to node-7' encodes as 'allocate <10 GB to node-7>' and decodes as 'allocate 10 GB to node-7'.\nrule-454: In any line describing a measured fact—including allocations, conditions, thresholds, or comparisons—the sender must treat the entire measured fact as a single atomic token by surrounding it with angle brackets: <numeric value unit comparison phrase or symbol relational preposition target identifier>. The decoder must remove the angle brackets and reconstruct the original spacing and punctuation verbatim, preserving numeric precision, units, thresholds, and comparison direction exactly as in the source. The decoder must validate both: (1) character‑for‑character match between the reconstructed bundle and the source; (2) that the reconstructed bundle contains every original component—value, unit, comparison phrase or symbol, relational preposition (if any), and target identifier (if any)—without omission, alteration, or splitting. If either validation fails, the message is non‑compliant. This rule applies to all measured facts not already covered by a more specific atomic encoding (e.g., opaque identifiers per rule‑452). Example: 'alert if temperature <5°C' encodes as 'alert if <temperature <5°C>' and decodes as 'alert if temperature <5°C'; 'allocate 10 GB to node‑7' encodes as 'allocate <10 GB to node‑7>' and decodes as 'allocate 10 GB to node‑7'.\nrule-455: In any line describing a temporal constraint—including absolute times (e.g., '04:30 PM EST'), relative deadlines (e.g., 'by tomorrow'), date ranges (e.g., '2025‑03‑15 to 2025‑03‑17'), durations (e.g., 'for 30 days'), and ordering relationships (e.g., 'before event start', 'after step 3')—the sender must treat the entire temporal phrase as a single atomic token by surrounding it with angle brackets: <temporal phrase>. The decoder must remove the angle brackets and reconstruct the original wording, spacing, punctuation, and relational ordering exactly as in the source, without normalizing, abbreviating, omitting, or altering any component. The decoder must validate that the reconstructed phrase matches the source character‑for‑character; if any component differs, the message is non‑compliant. This rule applies to all temporal constraints not already covered by a more specific atomic encoding (e.g., opaque identifiers per rule‑452). Example: 'submit report <by 04:30 PM EST>' decodes as 'submit report by 04:30 PM EST'; 'maintenance window <from 2025‑03‑15 to 2025‑03‑17>' decodes as 'maintenance window from 2025‑03‑15 to 2025‑03‑17'.\nrule-457: Revise rule‑455 to explicitly forbid decoder normalization and require verbatim literal output. Replace the sentence 'The decoder must remove the angle brackets and reconstruct the original wording, spacing, punctuation, and relational ordering exactly as in the source, without normalizing, abbreviating, omitting, or altering any component.' with: 'The decoder must treat the content inside the angle brackets as a literal string; it must remove the angle brackets and output that string verbatim, without parsing, normalizing, abbreviating, reordering, omitting, or altering any character. The decoder must then validate that the verbatim output matches the source character‑for‑character; if any component differs, the message is non‑compliant.' This ensures the temporal phrase is preserved exactly as written, satisfying the fault invariant.\nrule-459: In any line containing a temporal expression—including times, deadlines, ranges, durations, and ordering relationships—the sender must identify the complete contiguous sequence of words that constitute the temporal expression. The sequence begins with the first relational preposition (e.g., 'by', 'at', 'from', 'to', 'before', 'after', 'until', 'for') or subordinating conjunction (e.g., 'when', 'once', 'as soon as') that links the temporal element to the surrounding context and includes every subsequent word up to and including the last temporal element (time, date, range, duration, or event reference). The sender must treat this entire sequence as a single atomic token by surrounding it with angle brackets: <complete temporal phrase>. The encoder must verify that the enclosed phrase contains every original component—times, dates, ranges, durations, ordering words, and all linking prepositions/conjunctions—without omission, normalization, abbreviation, or alteration. The decoder must remove the angle brackets and output the enclosed string verbatim, without parsing, normalizing, abbreviating, reordering, omitting, or altering any character. The decoder must validate that the verbatim output matches the source character‑for‑character; if any component differs, the message is non‑compliant. This rule applies to all temporal constraints not already covered by a more specific atomic encoding (e.g., opaque identifiers per rule‑452). Example: 'submit report by 04:30 PM EST' encodes as 'submit report <by 04:30 PM EST>'; 'maintenance window from 2025‑03‑15 to 2025‑03‑17' encodes as 'maintenance window <from 2025‑03‑15 to 2025‑03‑17>'; 'wait until the start of event' encodes as 'wait <until the start of event>'.\nrule-461: In any line containing a temporal expression as defined in rule-459, the decoder must treat the content inside the angle brackets as a literal string; it must remove the angle brackets and output that string verbatim, without parsing, normalizing, abbreviating, reordering, omitting, or altering any character. The decoder must then validate that the verbatim output matches the source character-for-character; if any component differs, the message is non-compliant. This rule reinforces the exact preservation required by the fault invariant.\nrule-465: Before sending, the encoder must verify that the content placed inside the angle brackets, after the angle brackets are removed, exactly matches the contiguous substring of the source that constitutes the complete temporal phrase as defined in rule‑459. The verification must be a character‑for‑character comparison; if any character differs, the encoder must correct the encoding before transmitting the message. This encoder‑side validation is mandatory and ensures the atomic bundle captures the complete temporal constraint and its relational anchors, preventing normalization or omission that would violate the temporal‑constraint fault invariant.\nrule-466: Revise rule-459 to add a precise terminal condition for the temporal phrase sequence. Insert after 'any immediate qualifier attached to that element (e.g., 'EST' in '04:30 PM EST', 'local' in 'by tomorrow local time').' the following sentence: 'The sequence must end before the first word that is a conjunction (e.g., 'and', 'or', 'but', 'because', 'so', 'if', 'unless', 'since', 'while') that does not serve as the temporal link itself, or before a word that begins a new independent clause not modifying the temporal element.' Provide concrete worked examples: (1) 'submit report by 04:30 PM EST because manager leaves early' encodes as 'submit report <by 04:30 PM EST> because manager leaves early' – the bundle stops before 'because', which starts a rationale clause. (2) 'wait until 3 PM and then proceed' encodes as 'wait <until 3 PM> and then proceed' – 'and' begins a coordinated action, not part of the temporal link. (3) 'maintenance from 9 AM to 11 AM local time unless outage occurs' encodes as 'maintenance <from 9 AM to 11 AM local time> unless outage occurs' – 'unless' starts a conditional clause. (4) 'alert before start of event if temperature rises' encodes as 'alert <before start of event> if temperature rises' – 'if' begins a condition. (5) 'meet at 2 PM in conference room' encodes as 'meet <at 2 PM in conference room>' – 'in conference room' is a locative qualifier attached to the temporal phrase, not a new clause, so it remains inside. These examples illustrate the stop rule prevents over-inclusion while preserving linking words, ensuring the complete temporal constraint is captured without absorbing unrelated rationale or instructions.\nrule-467: Revise rule-452 to mandate an alias for opaque identifiers that contain punctuation or whitespace, eliminating reliance on tokenizer detection. Replace the clause beginning 'If the identifier contains punctuation or whitespace that could cause tokenizer splitting, enclose it in backticks and join segments with underscores inside the backticks.' with: 'If the identifier contains any punctuation (including hyphens, slashes, colons, dots, underscores) or whitespace, the sender must define an alias for it under rule-444 and use the alias in the message; the alias definition line must store the identifier exactly as in the source, enclosed in backticks with every character, punctuation, digit, case, and whitespace preserved verbatim, without any internal underscore substitution. The decoder must substitute the stored identifier exactly, removing the backticks, and must validate that the reconstructed identifier matches the original source character‑for‑character; any mismatch renders the message non‑compliant.' Remove the subsequent sentence about tokenizer splitting and alias mandate, as it is superseded.\nrule-470: In any line where a descriptive label (any noun or noun phrase that names, classifies, or describes an entity, location, concept, or attribute) directly precedes and attaches to a following identifier, name, or description, the sender must treat the entire label-entity pair as a single atomic token by surrounding it with curly braces: {label attached entity}. The decoder must remove the curly braces and reconstruct the original spacing and punctuation verbatim, preserving the exact label wording and its attachment without any inference, alteration, detachment, or splitting. The decoder must validate that the reconstructed pair matches the source character-for-character; if any component differs, the message is non-compliant. Example: original 'allocate to rack A-12' encodes as 'allocate to {rack A-12}' and decodes as 'allocate to rack A-12'; original 'meet at West Gate security desk' encodes as 'meet at {West Gate security desk}' and decodes as 'meet at West Gate security desk'.\nrule-477: In any line where a complete critical fact—a phrase linking at least two distinct semantic categories (such as an action and a condition, a cause and an effect, or a qualification and an instruction) via a subordinating or coordinating conjunction (e.g., 'because', 'so', 'if', 'unless', 'since', 'while') or relational preposition (e.g., 'for', 'with', 'by')—must be preserved as one unit, the sender must treat the entire critical fact as a single atomic token by surrounding it with angle brackets: <complete critical fact>. The bundle must include every original word, punctuation, spacing, and linking conjunction from the source. The decoder must remove the angle brackets and output the enclosed string verbatim, without parsing, normalizing, abbreviating, reordering, omitting, or altering any character. The decoder must validate that the verbatim output matches the source character-for-character; if any component differs, the message is non-compliant. This rule applies only when the linked categories are not already fully captured by an existing atomic bundle (e.g., a measured fact per rule-454, a temporal constraint per rule-459, or a label-entity pair per rule-470). Example: 'alert if temperature <5°C because sensor reading exceeds threshold' encodes as 'alert <if temperature <5°C because sensor reading exceeds threshold>' and decodes verbatim, preserving the 'if' and 'because' links.\nrule-478: Revise rule-467 to mandate backticks for all opaque identifiers, removing the alias requirement for punctuation. Replace the clause 'If the identifier contains any punctuation (including hyphens, slashes, colons, dots, underscores) or whitespace, the sender must define an alias for it under rule-444 and use the alias in the message; the alias definition line must store the identifier exactly as in the source, enclosed in backticks with every character, punctuation, digit, case, and whitespace preserved verbatim, without any internal underscore substitution.' with: 'For every opaque identifier, the sender must enclose the entire identifier in backticks to form a single atomic token, preserving every character, punctuation, digit, case, and whitespace exactly as in the source, without substituting any punctuation or spaces with underscores or altering any character. The decoder must remove the backticks and reconstruct the original identifier verbatim, validating character-for-character match with the source; any mismatch renders the message non-compliant. If an opaque identifier repeats at least 3 times, an alias under rule-444 is mandatory.' This ensures atomic preservation for all opaque identifiers, directly satisfying the fault invariant.\nrule-486: When encoding any opaque identifier as defined in rule-478, the sender must treat the entire identifier as a semantic atom: the backticks create an isolation zone where every character, punctuation mark, digit, and case is preserved exactly as in the source, and no internal symbol (including colons, slashes, hyphens, dots, underscores) may be interpreted by the decoder as a language delimiter, slot separator, list marker, or relational operator. The decoder must remove the backticks and output the identifier verbatim, validating character-for-character match with the source; any mismatch renders the message non-compliant. The decoder must also treat the reconstructed identifier as an opaque reference, not parsing its internal structure for meaning, ensuring its semantic role as a unique identifier is preserved. This rule reinforces rule-478 by explicitly forbidding decoder reinterpretation of internal punctuation, directly satisfying the opaque identifier fault invariant.\nrule-487: Replace rule-454 with: In any line describing a measured fact—including allocations, conditions, thresholds, or comparisons—the sender must treat the entire measured fact as a single atomic token by surrounding it with angle brackets: <numeric value unit comparison phrase or symbol relational preposition target identifier>. A measured fact begins with the first numeric value, unit, comparison phrase or symbol (e.g., 'greater than', '<', 'at least'), or relational preposition (e.g., 'to', 'for', 'at') that introduces the measured quantity or comparison, and includes every subsequent word up to and including the last target identifier or numeric qualifier, but stops before any conjunction (e.g., 'and', 'or', 'because', 'so', 'if', 'unless', 'since', 'while') that begins a new clause not part of the measured relationship; this boundary takes precedence over rule-477 when categories overlap. The decoder must remove the angle brackets and reconstruct the original spacing and punctuation verbatim, preserving numeric precision, units, thresholds, and comparison direction exactly as in the source. The decoder must validate both: (1) character‑for‑character match between the reconstructed bundle and the source; (2) that the reconstructed bundle contains every original component—value, unit, comparison phrase or symbol, relational preposition (if any), and target identifier (if any)—without omission, alteration, or splitting. If either validation fails, the message is non‑compliant. This rule applies to all measured facts not already covered by a more specific atomic encoding (e.g., opaque identifiers per rule‑452). Example: 'pressure at least 2.5 bar and valve closed' encodes as 'pressure <at least 2.5 bar> and valve closed' (stops before 'and'); 'allocate 10 GB to node‑7 for backup' encodes as 'allocate <10 GB to node‑7 for backup>' (includes attached prepositional phrase).\nrule-489: In any line containing a critical fact—a phrase where at least two distinct semantic categories (such as an action and a condition, a cause and an effect, a qualification and an instruction) are syntactically linked by a subordinating conjunction (e.g., 'because', 'so', 'if', 'unless', 'since', 'while'), coordinating conjunction ('and', 'or'), or relational preposition (e.g., 'for', 'with', 'by') not already part of an inner atomic bundle (angle brackets <...>, curly braces {...}, backticks `...`)—the sender must treat the entire critical fact as a single atomic token by surrounding it with double vertical bars: ||complete critical fact||. The bundle must begin with the first word of the first semantic category and include every subsequent word up to and including the last word of the linked category, preserving all linking conjunctions, prepositions, punctuation, and spacing exactly as in the source. The encoder must verify that the enclosed substring matches the source character-for-character. The decoder must remove the double vertical bars and output the substring verbatim, without parsing, normalizing, omitting, or altering any character, and must validate the character-for-character match; any mismatch renders the message non-compliant. Example: 'alert if temperature <5°C because sensor reading exceeds threshold' encodes as 'alert ||if temperature <5°C because sensor reading exceeds threshold||' and decodes verbatim.\nrule-493: Revise rule-470 to add an encoder-side verification mandate. After the first sentence, insert: 'Before sending, the encoder must verify that the content placed inside the curly braces, after the curly braces are removed, exactly matches the contiguous substring of the source that constitutes the complete label‑entity pair. The verification must be a character‑for‑character comparison; if any character differs, the encoder must correct the encoding before transmitting the message.' The decoder requirement remains unchanged: remove curly braces and reconstruct the original spacing and punctuation verbatim, validating character‑for‑character match. This ensures the atomic bundle captures the exact label wording and its attachment to the entity, preventing omission or alteration that would violate the exact‑label fault invariant.\nrule-496: In any line containing a critical fact—a phrase where at least two distinct semantic categories (such as an action and a condition, a cause and an effect, a qualification and an instruction) are syntactically linked by a subordinating conjunction (e.g., 'because', 'so', 'if', 'unless', 'since', 'while'), coordinating conjunction ('and', 'or'), or relational preposition (e.g., 'for', 'with', 'by') not already part of an inner atomic bundle (angle brackets <...>, curly braces {...}, backticks `...`)—the sender must treat the entire critical fact as a single atomic token by surrounding it with double vertical bars: ||complete critical fact||. The bundle must begin with the first word of the first semantic category and include every subsequent word up to and including the last word of the linked category, preserving all linking conjunctions, prepositions, punctuation, and spacing exactly as in the source. The sequence must end before the first word that is a conjunction (e.g., 'and', 'or', 'but', 'because', 'so', 'if', 'unless', 'since', 'while') that does not serve as the linking word itself, or before a word that begins a new independent clause not part of the critical relationship. Before sending, the encoder must verify that the content placed inside the double vertical bars, after the bars are removed, exactly matches the contiguous substring of the source that constitutes the complete critical fact. The verification must be a character‑for‑character comparison; if any character differs, the encoder must correct the encoding before transmitting the message. The decoder must remove the double vertical bars and output the substring verbatim, without parsing, normalizing, omitting, or altering any character, and must validate the character‑for‑character match; any mismatch renders the message non‑compliant. Example: 'alert if temperature <5°C because sensor reading exceeds threshold' encodes as 'alert ||if temperature <5°C because sensor reading exceeds threshold||' and decodes verbatim; 'proceed unless pre‑check fails and then restart service' encodes as 'proceed ||unless pre‑check fails|| and then restart service' – the bundle stops before 'and', which begins a coordinated action.\nrule-497: Replace all phrase-level atomic bundling delimiters with unified angle brackets. Amend rule-470 (label-entity pairs), rule-489 and rule-496 (critical facts) to use angle brackets instead of curly braces and double vertical bars. Add: 'Any phrase that must be preserved exactly as a single atomic token—including a measured fact (per rule-487), a temporal constraint (per rule-459), a label‑entity pair, a critical fact, or any other contiguous substring requiring verbatim atomic preservation—must be surrounded by angle brackets: <phrase>. The encoder must verify that the enclosed substring, after removing the angle brackets, exactly matches the contiguous substring of the source character‑for‑character. The decoder must remove the angle brackets and output the substring verbatim, without parsing, normalizing, omitting, or altering any character, and must validate character‑for‑character match; any mismatch renders the message non‑compliant. This rule supersedes the delimiter-specific bundling in rules 470, 489, and 496, but does not alter the semantic validation requirements for each category (e.g., label attachment, conjunction preservation).' Retain backticks for opaque identifiers per rule-478 as a separate token-boundary mechanism.\nrule-502: In any line containing two or more temporal constraints (as defined in rule-459) linked by a comparative or sequential conjunction (e.g., 'after', 'before', 'from', 'to', 'between', 'until', 'and', 'but', 'or') that expresses a combined ordering or range, the sender must treat the entire multi-constraint phrase as a single atomic token by surrounding it with double vertical bars: ||complete multi-temporal phrase||. The bundle must begin with the first linking word and include every subsequent word up to and including the last temporal constraint, preserving all conjunctions, prepositions, punctuation, and spacing exactly as in the source. The encoder must verify character-for-character match per rule-465. The decoder must remove the bars and output the substring verbatim, validating the match; any mismatch renders the message non-compliant. Example: 'proceed after 3 PM but before 5 PM' encodes as 'proceed ||after 3 PM but before 5 PM||'; 'maintenance from 9 AM to 11 AM' encodes as 'maintenance ||from 9 AM to 11 AM||' (superseding rule-459's single bundle when multiple constraints are linked).\nrule-505: Add to rule-496: Before encoding, the encoder must scan each line for any subordinating conjunction ('because', 'so', 'if', 'unless', 'since', 'while'), coordinating conjunction ('and', 'or'), or relational preposition ('for', 'with', 'by') that links two distinct semantic categories not already inside an atomic bundle (angle brackets <...>, curly braces {...}, backticks `...`). If such a linking word is found, the encoder must treat the entire phrase from the first word of the first semantic category through the last word of the linked category as a single atomic token by surrounding it with double vertical bars per rule-496, applying the same boundary rules (stop before a new clause conjunction). This mandatory scan ensures every critical fact is captured and bundled, preserving the complete relationships between entities, actions, and constraints.\nrule-506: Add to rule-496: The encoder's mandatory scan per rule-505 must also verify that every linking conjunction or preposition inside the double vertical bar bundle appears exactly as in the source, with its original spelling, case, and position. No linking word inside a critical fact bundle may be replaced by a symbol, abbreviation, or omitted under minimal-form allowances; the bundle must preserve the complete relational wording verbatim. The decoder validation must check this preservation; if any linking word is missing, altered, or replaced, the message is non-compliant.\nrule-510: Replace rule-459 with a new rule that mandates a complete, unbreakable temporal bundle. The new rule: In any line containing a temporal expression—including absolute times, relative deadlines, date ranges, durations, and ordering relationships—the sender must treat the entire temporal phrase as a single atomic token by surrounding it with angle brackets: <complete temporal phrase>. The phrase must begin with the first relational preposition (e.g., 'by', 'at', 'from', 'to', 'before', 'after', 'until', 'for', 'when', 'once', 'as soon as') or subordinating conjunction that introduces the temporal element, and must include every subsequent word up to and including the last temporal element and any attached qualifier (e.g., time zone, locale, 'local time', 'EST'). The bundle must not stop before any conjunction that is part of the temporal link itself (e.g., 'and' linking two times in a range) but must stop before the first word that is a conjunction (e.g., 'and', 'or', 'but', 'because', 'so', 'if', 'unless', 'since', 'while') that begins a new independent clause not modifying the temporal element. The encoder must verify that the enclosed substring, after angle brackets are removed, exactly matches the contiguous source substring that constitutes the complete temporal phrase as defined above; if any character differs, the encoding is invalid and must be corrected. The decoder must remove the angle brackets and output the enclosed string verbatim, without parsing, normalizing, abbreviating, reordering, omitting, or altering any character, and must validate that the verbatim output matches the source character-for-character; any mismatch renders the message non-compliant. This rule applies to all temporal constraints not already covered by a more specific atomic encoding (e.g., opaque identifiers per rule-452). Example: 'submit report by 04:30 PM EST because manager leaves early' encodes as 'submit report <by 04:30 PM EST> because manager leaves early' (stops before 'because'). 'maintenance from 9 AM to 11 AM local time unless outage occurs' encodes as 'maintenance <from 9 AM to 11 AM local time> unless outage occurs' (includes 'local time', stops before 'unless'). 'wait until 3 PM and then proceed' encodes as 'wait <until 3 PM> and then proceed' (stops before 'and' which begins a coordinated action).\nrule-511: In any line, when a clause is introduced by a subordinating conjunction ('because', 'so', 'if', 'unless', 'since', 'while'), coordinating conjunction ('and', 'or'), or relational preposition ('for', 'with', 'by', 'to', 'at', 'from', 'before', 'after', 'until') that links it to the preceding context, the sender must treat the entire clause as a single atomic token by surrounding it with parentheses: (complete clause). The clause includes the conjunction or preposition and every subsequent word up to the next punctuation mark that ends the clause (e.g., comma, period, colon, semicolon) or the next independent clause introduced by another conjunction not part of the same linkage. This rule applies specifically when the clause is a descriptive attachment using a preposition like 'with' that modifies a preceding noun but is not part of a measured fact, temporal constraint, label-entity pair, or critical fact already bundled under other rules. Example: 'check logs with detailed timestamp analysis' encodes as 'check logs (with detailed timestamp analysis)' where the prepositional clause is isolated; rule-496's scan for critical facts might not capture this because 'with' links a description, not two distinct semantic categories. The encoder must verify that the enclosed substring, after parentheses are removed, exactly matches the contiguous source substring character-for-character. The decoder must remove the parentheses and output the substring verbatim, without parsing, normalizing, omitting, or altering any character, and must validate the character-for-character match; any mismatch renders the message non-compliant.\nrule-513: Revise rule-496 to expand the encoder's mandatory scan to capture any phrase where a measured fact (as defined in rule-487) or temporal constraint (as defined in rule-510) is linked by a subordinating conjunction ('because', 'so', 'if', 'unless', 'since', 'while'), coordinating conjunction ('and', 'or'), or relational preposition ('for', 'with', 'by', 'to', 'at', 'from', 'before', 'after', 'until') to another semantic category (action, condition, entity, or another fact). Replace the opening clause 'In any line containing a critical fact—a phrase where at least two distinct semantic categories ... are syntactically linked by a subordinating conjunction ...' with: 'In any line containing a phrase where a measured fact (per rule-487), a temporal constraint (per rule-510), or any other semantic category is syntactically linked by a subordinating conjunction ('because', 'so', 'if', 'unless', 'since', 'while'), coordinating conjunction ('and', 'or'), or relational preposition ('for', 'with', 'by', 'to', 'at', 'from', 'before', 'after', 'until') to another distinct semantic category, and the linked categories are not already fully inside a single atomic bundle (angle brackets <...>, curly braces {...}, backticks `...`, parentheses (...)), the sender must treat the entire linked phrase as a single atomic token by surrounding it with double vertical bars: ||complete linked phrase||.' Keep all subsequent bundling, boundary, verification, and decoder validation requirements unchanged. This ensures the encoder's scan per rule-505 captures measured and temporal relationships as critical facts, preserving the complete relational structure.\nrule-515: Repeal the contradictory clause in rule-478. Replace the sentence 'If the identifier contains no punctuation or whitespace and tokenizes as a single token, transmit it verbatim without backticks unless it repeats at least 3 times, in which case an alias is mandatory.' with: 'The sender must enclose every opaque identifier in backticks to form a single atomic token, preserving every character, punctuation, digit, case, and whitespace exactly as in the source, without substituting any punctuation or spaces with underscores or altering any character. The decoder must remove the backticks and reconstruct the original identifier verbatim, validating character-for-character match with the source; any mismatch renders the message non-compliant. If an opaque identifier repeats at least 3 times, an alias under rule-444 is mandatory.' This repeal eliminates the ambiguity and ensures the rule's opening mandate is unconditional and directly satisfies the opaque identifier fault invariant.\nrule-521: Repeal the proposed add rule-521; the decoder's treatment of opaque identifiers is already fully governed by rule-486 and rule-478.\nrule-523: Revise rule-470 to adopt unified angle brackets for label-entity pairs and reinforce exact preservation. Replace the current rule text with: 'In any line where a descriptive label (any noun or noun phrase that names, classifies, or describes an entity, location, concept, or attribute) directly precedes and attaches to a following identifier, name, or description, the sender must treat the entire label-entity pair as a single atomic token by surrounding it with angle brackets: <label attached entity>. The pair must begin with the first word of the label and include every subsequent word up to and including the last word of the attached entity, preserving all internal punctuation and spacing exactly as in the source. The sequence must end before the first word that is a conjunction (e.g., 'and', 'or', 'but', 'because', 'so') that begins a new independent clause not part of the attachment. Before sending, the encoder must verify that the content placed inside the angle brackets, after the brackets are removed, exactly matches the contiguous substring of the source that constitutes the complete label-entity pair. The verification must be a character-for-character comparison; if any character differs, the encoder must correct the encoding before transmitting the message. The decoder must remove the angle brackets and output the enclosed string verbatim, without parsing, normalizing, omitting, or altering any character, and must validate the character-for-character match; any mismatch renders the message non-compliant. This rule applies to all label-entity pairs not already covered by a more specific atomic encoding (e.g., opaque identifiers per rule-478).'\nrule-524: Add rule-524: In any message, for any phrase that is already wrapped in angle brackets <...> in the source according to an adopted bundling rule (e.g., measured fact per rule-487, temporal constraint per rule-510, label-entity pair per rule-523, critical fact per rule-496, or other phrase requiring exact preservation), and the identical angle‑bracketed substring (including the angle brackets and all internal punctuation, case, digits, and whitespace) appears at least three times, the sender may define a reference token for it by writing `$ref=exact_bundled_phrase` on its own line, where `exact_bundled_phrase` is that angle‑bracketed substring verbatim. The encoder must verify that each occurrence of the bundled phrase in the source matches the defined `exact_bundled_phrase` character‑for‑character. Thereafter, the sender may use `$ref` in place of that bundled phrase anywhere in the message. The decoder must substitute the exact_bundled_phrase verbatim (including its angle brackets) for each occurrence of `$ref`, validating character‑for‑character match with the original bundled phrase in the source. The sender must ensure token savings: definition tokens + (uses * reference token tokens) < uses * original bundled phrase tokens. This mechanism is distinct from rule‑444's @short alias, which defines abbreviations for whole strings that repeat; $ref enables compression of identical atomic phrases that appear inside varied surrounding contexts. Decision procedure: (1) Identify any substring that is already wrapped in angle brackets in the source according to an adopted bundling rule. (2) If the identical angle‑bracketed substring repeats at least three times, consider $ref referencing. (3) If the substring repeats but is not angle‑bracketed, use @short alias if it meets rule‑444 criteria. (4) Choose the mechanism that yields the greatest token savings while preserving exact meaning. Example: original 'alert if <temperature <5°C> because sensor reading exceeds threshold' and 'log when <temperature <5°C> occurs' share the bundled phrase '<temperature <5°C>'. Encode as '$ref=<temperature <5°C>\\nalert if $ref because sensor reading exceeds threshold\\nlog when $ref occurs' and decode verbatim.\nrule-529: Add rule: After reconstructing an opaque identifier per rule-478 and rule-486, the decoder must treat the reconstructed string as an opaque semantic token for all subsequent language processing. The decoder may not parse, interpret, or assign any grammatical, relational, or structural meaning to any internal punctuation mark, digit, case, or whitespace within the identifier. Specifically, internal symbols such as colons, slashes, hyphens, dots, underscores, equals signs, or parentheses must not be treated as language delimiters, slot separators, list markers, relational operators, directive introducers, or bundling boundaries. The identifier's sole semantic role is that of a unique reference; its internal composition does not affect the parsing of the surrounding message. This rule reinforces the prohibition of decoder reinterpretation in rule-486 by explicitly forbidding any post-reconstruction parsing that could alter the identifier's intended semantic role, directly satisfying the opaque identifier fault invariant.\nrule-531: Add rule: For any clause introduced by a subordinating conjunction ('because', 'so', 'if', 'unless', 'since', 'while'), coordinating conjunction ('and', 'or'), or relational preposition ('for', 'with', 'by', 'to', 'at', 'from', 'before', 'after', 'until') that syntactically links two distinct semantic categories, the sender must treat the entire linked phrase as a single atomic token by surrounding it with angle brackets: <complete linked phrase>. The phrase must begin with the linking word and include every subsequent word up to the end of the linked category, stopping before any conjunction that begins a new independent clause not part of the same linkage. Before sending, the encoder must verify character-for-character match with the source substring. The decoder must remove the brackets and output the substring verbatim, validating the match; any mismatch renders the message non-compliant. This rule supersedes and repeals the double vertical bar mechanism in rule-496 and rule-513, consolidating all phrase-level bundling under a single, uniform delimiter while preserving the complete relationships between entities, actions, and constraints.\nrule-537: Add rule: For any substring of the source message that the sender determines must be preserved exactly as a single atomic token—including but not limited to opaque identifiers, measured facts, temporal constraints, label‑entity pairs, critical facts, or any other contiguous sequence of characters whose internal punctuation, digits, case, whitespace, or semantic role must not be altered, parsed, split, or reinterpreted—the sender must surround that substring with angle brackets: <exact substring>. The encoder must verify that the enclosed substring, after removing the angle brackets, exactly matches the contiguous substring of the source character‑for‑character; if any character differs, the encoding is invalid and must be corrected. The decoder must remove the angle brackets and output the enclosed substring verbatim, without parsing, normalizing, abbreviating, reordering, omitting, or altering any character, and must validate that the verbatim output matches the source character‑for‑character; any mismatch renders the message non‑compliant. The decoder may not assign any grammatical, relational, or structural meaning to any internal punctuation, digit, case, or whitespace within the atomic token; its sole semantic role is that of an indivisible unit as designated by the sender. This rule supersedes all previous phrase‑level atomic bundling rules (including rules 487, 510, 523, 531, and any others that mandate specific delimiters for specific categories), consolidating them under one universal mechanism, but does not alter the encoder’s responsibility to identify and bundle substrings that require exact preservation according to the language’s purpose.\nrule-541: Revise rule-510 to add a decoder prohibition on parsing internal symbols within the reconstructed temporal phrase. Insert after the decoder validation sentence the following: 'After reconstructing the temporal phrase, the decoder must treat the output string as an opaque semantic token for all subsequent language processing. The decoder may not parse, interpret, or assign any grammatical, relational, or structural meaning to any internal punctuation mark, digit, case, or whitespace within the phrase. Specifically, internal symbols such as colons, slashes, hyphens, dots, underscores, or parentheses must not be treated as language delimiters, slot separators, list markers, relational operators, directive introducers, or bundling boundaries. The phrase's sole semantic role is that of an exact temporal constraint; its internal composition does not affect the parsing of the surrounding message.' This ensures complete preservation without normalization or misinterpretation, directly satisfying the temporal-constraint fault invariant.\nrule-545: Add rule: After reconstructing any atomic token—any substring that was surrounded by angle brackets <...> or backticks `...` in the encoded message—the decoder must treat the reconstructed string as an opaque semantic token for all subsequent language processing. The decoder may not parse, interpret, or assign any grammatical, relational, or structural meaning to any internal punctuation mark, digit, case, or whitespace within the token. Specifically, internal symbols such as colons, slashes, hyphens, dots, underscores, equals signs, parentheses, or any other character must not be treated as language delimiters, slot separators, list markers, relational operators, directive introducers, or bundling boundaries. The token's sole semantic role is that of an indivisible unit as designated by the sender; its internal composition does not affect the parsing of the surrounding message. The encoder must still verify exact character-for-character match per the relevant bundling rules. This rule supersedes and repeals rule-529 and rule-541, consolidating their decoder prohibitions into one universal prohibition that ensures complete preservation without misinterpretation.\nrule-546: Add rule: In any line containing two or more temporal constraints (as defined in rule-510) that are linked by a comparative or sequential conjunction (e.g., 'after', 'before', 'from', 'to', 'between', 'until', 'and', 'but', 'or') expressing a combined ordering or range, the sender must treat the entire multi-temporal phrase as a single atomic token by surrounding it with angle brackets: <complete multi-temporal phrase>. The phrase must begin with the first linking word and include every subsequent word up to and including the last temporal constraint, preserving all conjunctions, prepositions, punctuation, and spacing exactly as in the source. The sequence must end before any conjunction that begins a new independent clause not part of the same ordering relationship. Before sending, the encoder must verify that the enclosed substring, after removing the angle brackets, exactly matches the contiguous substring of the source that constitutes the complete multi-temporal phrase character-for-character; if any character differs, the encoding is invalid and must be corrected. The decoder must remove the angle brackets and output the substring verbatim, without parsing, normalizing, abbreviating, reordering, omitting, or altering any character, and must validate the character-for-character match; any mismatch renders the message non-compliant. This rule ensures the complete ordering relationship is preserved without normalization, directly satisfying the temporal-constraint fault invariant. Example: 'proceed after 3 PM but before 5 PM' encodes as 'proceed <after 3 PM but before 5 PM>'; 'maintenance from 9 AM to 11 AM' encodes as 'maintenance <from 9 AM to 11 AM>'.\nrule-547: Add rule: When decoding a minimal-form line per rule-440, the decoder must infer omitted words to restore the most plausible grammatical and semantic structure given the surrounding preserved words and the original intended meaning. The inference must respect the categories of words that may be omitted: articles, prepositions, copula verbs, auxiliary verbs, and relative pronouns. The decoder must not insert words that would change the original meaning, introduce new relationships, or contradict any explicit preserved words, identifiers, numbers, or atomic bundles. If multiple plausible reconstructions exist, the decoder must choose the one that preserves the original logical flow and factual content as indicated by the source context. Provide concrete worked examples showing how a fresh decoder applies the inference constraints to ambiguous minimal-form lines, distinguishing correct from incorrect reconstructions. Example 1: source 'check logs for error codes', minimal 'check logs error codes'. Plausible reconstructions: (a) 'check logs for error codes' (correct), (b) 'check logs and error codes' (incorrect, changes meaning). Decoder must choose (a) because 'for' is a permissible omitted preposition and reconstructs the original relationship. Example 2: source 'the system is ready', minimal 'system ready'. Plausible reconstructions: (a) 'the system is ready' (correct), (b) 'system ready' (incorrect, missing copula). Decoder must choose (a) because 'is' is a permissible omitted copula verb. Example 3: source 'alert if temperature exceeds threshold because sensor faulty', minimal 'alert temperature exceeds threshold sensor faulty'. Plausible reconstructions: (a) 'alert if temperature exceeds threshold because sensor faulty' (correct), (b) 'alert temperature exceeds threshold and sensor faulty' (incorrect, changes logical link). Decoder must choose (a) because 'if' and 'because' are omitted subordinating conjunctions that must be restored to preserve the original conditional-causal chain. This rule provides a general principle for decoding minimal-form omissions, ensuring semantic fidelity without requiring case-specific exceptions.\nrule-549: Add rule: When a structural pattern—a sequence of words and atomic bundles with consistent gaps for variable parts—appears at least three times in a message, the sender may define a template for it by writing `%template=pattern` on its own line, where `pattern` is the invariant structure with numbered placeholders `$1`, `$2`, etc., marking each variable gap. The placeholder syntax `$N` must be exactly a dollar sign followed by one or more decimal digits, with no other characters. The sender must then define each variable's actual content on separate lines as `$N=content`, where each content is a literal string that must be preserved exactly as required by the relevant atomic bundling rules (i.e., if the content is an opaque identifier, measured fact, temporal constraint, label-entity pair, critical fact, or any other substring requiring exact preservation, it must be enclosed in angle brackets <...> or backticks `...` as defined by those rules). The variable definitions are template-scoped: they apply only to the preceding `%template=pattern` line and are consumed when the template is invoked. The sender may invoke the template by writing `%template content1 content2 ...` where each content is a literal string (or an alias or reference to such a string) that matches the placeholder order; the number of content arguments must equal the number of placeholders in the pattern. The decoder must reconstruct each instance by substituting the literal content strings verbatim into the placeholder positions, preserving the exact structure and variable content. Before sending, the encoder must verify, after all substitutions (including aliases and references), that each reconstructed instance matches the source character-for-character; any mismatch renders the message non-compliant. Template expansion occurs after alias substitution (rule-444) and reference substitution (rule-524) but before minimal-form reconstruction (rule-440) and other language processing. Token counting for savings must account for the full tokens of the template definition line, each variable definition line, the template invocation token, and all variable content tokens as they appear in the definitions (including their delimiters). A template and its variable definitions may be used for multiple invocations if the same pattern repeats with different variable contents across the message; the sender may write separate `%template contentA contentB` lines for each set of contents. This rule provides compression for repeated patterns with variations, distinct from rule-444's alias for identical strings and rule-524's reference for identical bundles.\n\nWorked example: source contains 'allocate <10 GB to node-7> for backup', 'allocate <20 GB to node-8> for backup', 'allocate <5 GB to node-9> for backup'. Pattern: 'allocate $1 for backup' with variable content being the measured fact. Encode as:\n`%template=allocate $1 for backup`\n`$1=<10 GB to node-7>`\n`$2=<20 GB to node-8>`\n`$3=<5 GB to node-9>`\nThen write: `%template $1 $2 $3`. Decoder expands to three original lines verbatim. The variable definitions are literal angle-bracketed strings; the encoder verifies each expanded line matches the source.\n\nSecond example with opaque identifier and alias: source contains 'process `order-123-abc`', 'process `order-456-def`', 'process `order-789-ghi`'. Define alias: `@oid=opaque_identifier`. Pattern: 'process $1'. Encode as:\n`@oid=opaque_identifier`\n`%template=process $1`\n`$1=@oid`\nThen write three separate invocations: `%template <order-123-abc>`; `%template <order-456-def>`; `%template <order-789-ghi>`. Decoder first substitutes alias, then expands each template invocation, verifying each reconstructed line matches the source.\nrule-556: Add rule: For any measured fact as defined in rule-487, before surrounding it with angle brackets, the encoder must verify that the substring to be bundled begins with the first numeric value, unit, comparison phrase or symbol, or relational preposition that introduces the measured quantity and includes every subsequent word up to and including the last target identifier or numeric qualifier, stopping before any conjunction that begins a new independent clause not part of the measured relationship. The encoder must perform a character-for-character comparison between the substring to be bundled and the contiguous source substring; if any character differs, the encoding is invalid and must be corrected. The decoder must remove the angle brackets and output the substring verbatim, validating character-for-character match with the source; any mismatch renders the message non-compliant. This rule reinforces the exact preservation of numeric precision, units, thresholds, and comparison direction as one complete measured fact.\nrule-560: Add rule: In any line containing a numeric literal—a contiguous sequence of digits optionally containing a decimal point—the sender must treat the numeric literal and all immediately following words that qualify it (including unit symbols, comparison phrases or symbols, relational prepositions, target identifiers, and any attached qualifiers) as a single atomic token by surrounding the entire substring with angle brackets: <numeric literal and qualifiers>. The bundle must begin with the numeric literal and include every subsequent word up to the next punctuation mark that ends the numeric phrase (e.g., comma, period, semicolon, colon) or the next word that is a conjunction (e.g., 'and', 'or', 'but', 'because', 'so') that begins a new independent clause not part of the numeric qualification. Before sending, the encoder must verify that the enclosed substring, after removing the angle brackets, exactly matches the contiguous substring of the source character-for-character. The decoder must remove the angle brackets and output the substring verbatim, without parsing, normalizing, omitting, or altering any character, and must validate character-for-character match; any mismatch renders the message non-compliant. This rule applies to all numeric literals not already inside another atomic token (e.g., opaque identifiers per rule-478). Example: 'allocate 10 GB to node-7' encodes as 'allocate <10 GB to node-7>'; 'alert if temperature <5°C' encodes as 'alert if temperature <5°C>' (the comparison symbol and unit are included).\nrule-562: Add rule: After reconstructing any label-entity pair per rule-523, the decoder must treat the output string as an opaque semantic token for all subsequent language processing. The decoder may not parse, interpret, or assign any grammatical, relational, or structural meaning to any internal punctuation mark, digit, case, or whitespace within the token. Specifically, internal symbols such as colons, slashes, hyphens, dots, underscores, parentheses, or any other character must not be treated as language delimiters, slot separators, list markers, relational operators, directive introducers, or bundling boundaries. The token's sole semantic role is that of an exact label attached to its entity; its internal composition does not affect the parsing of the surrounding message. The encoder must still verify exact character-for-character match per rule-523.\nrule-563: Add rule: In any line containing a phrase where two distinct semantic categories (such as an action and a condition, a cause and an effect, a qualification and an instruction) are syntactically linked by a subordinating conjunction ('because', 'so', 'if', 'unless', 'since', 'while'), coordinating conjunction ('and', 'or'), or relational preposition ('for', 'with', 'by', 'to', 'at', 'from', 'before', 'after', 'until'), the sender must treat the entire linked phrase as a single atomic token by surrounding it with angle brackets: <complete linked phrase>. The phrase must begin with the first word of the first semantic category and include every subsequent word up to and including the last word of the linked category, preserving all linking words, punctuation, and spacing exactly as in the source. The bundle must stop before any conjunction that begins a new independent clause not part of the same linkage. Before sending, the encoder must verify that the enclosed substring, after removing the angle brackets, exactly matches the contiguous substring of the source character-for-character. The decoder must remove the angle brackets and output the substring verbatim, without parsing, normalizing, omitting, or altering any character, and must validate character-for-character match; any mismatch renders the message non-compliant. This rule provides a mandatory bundling trigger for complete critical facts, ensuring preservation of relationships between entities, actions, and constraints.\nrule-567: Add rule: In any line, the encoder must scan for every temporal expression as defined in rule-510. For each such expression, the encoder must surround the entire expression with angle brackets: <complete temporal phrase>. The encoder may not leave any part of the temporal expression outside the angle brackets, nor may it split the expression across multiple atomic tokens. The encoder must perform this bundling before applying minimal-form omissions (rule-440) or other compression mechanisms. The encoder's verification per rule-465 (character-for-character match) remains mandatory. This rule does not alter decoder behavior, which is already governed by rule-510 and rule-545.\nrule-569: Add rule: In any line, the encoder must scan for every measured fact as defined in rule-487. For each such fact, the encoder must surround the entire fact with angle brackets: <complete measured fact>. The encoder may not leave any part of the measured fact outside the angle brackets, nor may it split the expression across multiple atomic tokens. The encoder must perform this bundling before applying minimal-form omissions (rule-440) or other compression mechanisms. The encoder's verification per rule-556 (character-for-character match) remains mandatory. This rule does not alter decoder behavior, which is already governed by rule-487 and rule-545.\nrule-573: Add rule: In any line, before applying minimal-form omissions (rule-440), alias substitutions (rule-444), template expansions (rule-549), or any other compression mechanism, the encoder must scan the source text for every measured fact. A measured fact is defined as any contiguous substring that begins with a numeric literal (a sequence of digits optionally containing a decimal point) or a comparison phrase or symbol (e.g., 'greater than', '<', 'at least') and includes every subsequent word up to and including the last target identifier, unit symbol, or numeric qualifier, but stops before any conjunction (e.g., 'and', 'or', 'but', 'because', 'so', 'if', 'unless') that begins a new independent clause not part of the measured relationship. For each measured fact identified, the encoder must surround the entire substring with angle brackets: <complete measured fact>. The encoder must then verify, by character-for-character comparison, that the enclosed substring (after removing the angle brackets) exactly matches the contiguous substring of the source. If any character differs, the encoding is invalid and must be corrected. This bundling is mandatory; the encoder may not leave any part of a measured fact outside the angle brackets, nor split it across multiple tokens. The decoder's behavior is governed by existing rules (rule-487, rule-545) for output and validation. This rule ensures numeric precision, units, thresholds, and comparison direction are preserved as one complete atomic unit.\nrule-574: Add rule: When a message contains at least three lines that share an identical literal prefix and suffix with a variable middle element, the sender may define a pattern abbreviation by writing `#pattern=prefix$suffix` on its own line, where `prefix` is the exact string that appears before the variable element in each line, `suffix` is the exact string that appears after the variable element in each line, and `$` is a single placeholder marking the gap for the variable. The placeholder `$` must abut the prefix and suffix characters with no intervening spaces unless a space is intended as part of the literal prefix or suffix; any spaces intended as part of the prefix or suffix must be included literally. The variable element for each line must be a complete atomic token (enclosed in angle brackets <...> or backticks `...` per existing bundling rules) representing the exact middle substring from the source. Thereafter, the sender may write each line as `#pattern variable`, where `variable` is that exact atomic token including its delimiters. The decoder must reconstruct each line by: (1) removing the outermost angle brackets or backticks from the variable token per the relevant bundling rule (e.g., rule-537) to obtain the literal middle substring; (2) concatenating the prefix, that literal middle substring, and the suffix, preserving the exact prefix and suffix verbatim and ensuring the concatenated line matches the source character-for-character. The encoder must verify, by character-for-character comparison, that each reconstructed line matches the source; any mismatch renders the message non-compliant. Only one `$` placeholder is permitted per pattern; multiple variables are not supported. This rule provides lightweight compression for repeated structural patterns distinct from rule-444 (identical strings) and rule-549 (full templates). Example: source lines 'allocate 10 GB to node-7', 'allocate 20 GB to node-8', 'allocate 5 GB to node-9'. The prefix is 'allocate ', the suffix is ' to node-'. The variable elements are '10 GB', '20 GB', '5 GB'. Define: `#pattern=allocate $ to node-`. Encode as: `#pattern <10 GB> 7`, `#pattern <20 GB> 8`, `#pattern <5 GB> 9`. Decoder reconstructs first line: strip brackets from '<10 GB>' to get '10 GB', concatenate 'allocate ' + '10 GB' + ' to node-7' to produce 'allocate 10 GB to node-7' exactly.\nrule-575: Add rule: When decoding a temporal phrase that was bundled with angle brackets per rule-510, the decoder must output the entire content between the angle brackets verbatim, with no character omitted, normalized, abbreviated, reordered, or altered. The decoder must validate that the verbatim output string matches the source character-for-character; any mismatch renders the message non-compliant. This rule reinforces that the decoder's sole duty is to reproduce the exact substring, ensuring that time zones, ordering words, and all relational anchors are preserved without any inference or cleaning.\nrule-578: Add rule: The encoder must scan every line for any phrase where two distinct semantic categories (such as an action and a condition, a cause and an effect, a qualification and an instruction) are syntactically linked by a subordinating conjunction ('because', 'so', 'if', 'unless', 'since', 'while'), coordinating conjunction ('and', 'or'), or relational preposition ('for', 'with', 'by', 'to', 'at', 'from', 'before', 'after', 'until'). For each such linked phrase, the encoder must treat the entire phrase as a single atomic token by surrounding it with angle brackets: <complete linked phrase>. The phrase must begin with the first word of the first semantic category and include every subsequent word up to and including the last word of the linked category, preserving all linking words, punctuation, and spacing exactly as in the source. The bundle must stop before any conjunction that begins a new independent clause not part of the same linkage. Before sending, the encoder must verify, by character-for-character comparison, that the enclosed substring (after removing the angle brackets) exactly matches the contiguous substring of the source. If any character differs, the encoding is invalid and must be corrected. The decoder must remove the angle brackets and output the substring verbatim, without parsing, normalizing, omitting, or altering any character, and must validate character-for-character match; any mismatch renders the message non-compliant. This rule ensures the complete critical fact and the relationships between its entities, actions, and constraints are preserved as one atomic unit.\nrule-586: Add rule: In any line where a measured allocation is expressed—a directive or statement that assigns, allocates, reserves, or dedicates a measured quantity (numeric value and unit) to a target (opaque identifier, label-entity pair, or other named resource)—the sender must treat the entire allocation phrase as a single atomic token by surrounding it with angle brackets: <complete allocation phrase>. The phrase must begin with the action verb (e.g., 'allocate', 'assign', 'reserve', 'dedicate', 'grant') and include every subsequent word up to and including the target identifier and any attached qualifier, but stop before any conjunction that begins a new independent clause not part of the allocation. The encoder must verify character-for-character match with the source substring. The decoder must remove the brackets and output the substring verbatim, validating the match; any mismatch renders the message non-compliant. This rule ensures the complete relationship between action, quantity, unit, and target is preserved as one bundle, satisfying the quantified allocation fault invariant.\nrule-588: Add rule: In any line, the encoder must scan for every measured quantity or allocation. A measured quantity or allocation is any contiguous substring that begins with (1) a numeric literal (a sequence of digits optionally containing a decimal point) or a comparison phrase or symbol (e.g., 'greater than', '<', 'at least', 'older than', 'newer than', '>', '<'), or (2) an action verb that introduces a measured allocation (e.g., 'allocate', 'assign', 'reserve', 'dedicate', 'grant', 'provide') when followed immediately by a numeric literal or comparison phrase. The substring includes every subsequent word up to and including the last unit symbol, target identifier, or attached qualifier, but stops before any conjunction (e.g., 'and', 'or', 'but', 'because', 'so', 'if', 'unless') that begins a new independent clause not part of the measured relationship. For each measured quantity or allocation identified, the encoder must surround the entire substring with angle brackets: <complete measured quantity or allocation>. The encoder must then verify, by character-for-character comparison, that the enclosed substring (after removing the angle brackets) exactly matches the contiguous substring of the source. If any character differs, the encoding is invalid and must be corrected. This bundling is mandatory; the encoder may not leave any part of a measured quantity or allocation outside the angle brackets, nor split it across multiple tokens. The decoder's behavior is governed by existing rules (rule-537, rule-545) for verbatim output and validation. This rule supersedes and repeals rules 487, 556, 560, 569, 573, and 586, consolidating their measured-fact bundling mandates into one universal encoder obligation that ensures numeric precision, units, thresholds, comparison direction, and the complete action‑quantity‑target relationship are preserved as one complete atomic unit.\nrule-590: Add rule: Repeal rule-569. Its requirement that the encoder must scan for every measured fact and surround it with angle brackets is redundant with rule-537 (universal atomic bundling) and rule-588 (consolidated measured quantity bundling). The language’s capability to preserve measured facts exactly remains governed by those rules; removing rule-569 eliminates procedural duplication without altering semantic fidelity.\nrule-592: Add rule: When a measured allocation involves a numeric quantity, a unit, a target identifier, and a relational preposition (e.g., 'to', 'for', 'at'), the encoder must treat the entire allocation phrase as a single atomic token by surrounding it with angle brackets: <complete allocation phrase>. The phrase must include the quantity, unit, preposition, and target identifier verbatim. The encoder must verify character-for-character match with the source substring. The decoder must remove the brackets and output the substring verbatim, validating the match; any mismatch renders the message non-compliant. This rule ensures the complete relationship between quantity, unit, and target is preserved as one bundle.\nrule-593: Add rule: Before sending, the encoder must verify that any substring bundled as a measured fact under rule-588 includes every original component: numeric value, unit, comparison phrase or symbol, relational preposition (if any), and target identifier (if any). The verification must be a character-for-character comparison between the bundled substring (after removing angle brackets) and the contiguous source substring that constitutes the complete measured fact. If any component is omitted from the bundle, the encoding is invalid and must be corrected. This ensures numeric precision, units, thresholds, and comparison direction are preserved as one complete measured fact.\nrule-594: Add rule: In any line containing a critical fact—a phrase where two distinct semantic categories (such as an action and a condition, a cause and an effect, a qualification and an instruction) are syntactically linked by a subordinating conjunction ('because', 'so', 'if', 'unless', 'since', 'while'), coordinating conjunction ('and', 'or'), or relational preposition ('for', 'with', 'by', 'to', 'at', 'from', 'before', 'after', 'until')—the sender must treat the entire linked phrase as a single atomic token by surrounding it with angle brackets: <complete critical fact>. The phrase must begin with the first word of the first semantic category and include every subsequent word up to and including the last word of the linked category, preserving all linking words, punctuation, and spacing exactly as in the source. The bundle must stop before any conjunction that begins a new independent clause not part of the same linkage. Before sending, the encoder must verify that the enclosed substring, after removing the angle brackets, exactly matches the contiguous substring of the source character-for-character. The decoder must remove the angle brackets and output the substring verbatim, without parsing, normalizing, omitting, or altering any character, and must validate character-for-character match; any mismatch renders the message non-compliant. This rule provides a mandatory bundling trigger for complete critical facts, ensuring preservation of relationships between entities, actions, and constraints.\nrule-598: Add rule: When encoding an opaque identifier (as defined in rule-478), the encoder must treat the entire identifier as an indivisible semantic atom and may not apply any minimal-form omission (rule-440), abbreviation (rule-444), template expansion (rule-549), pattern abbreviation (rule-574), or any other compression mechanism that would alter, omit, replace, or reinterpret any character, punctuation, digit, case, or whitespace inside the identifier. The encoder must preserve the identifier exactly as in the source, enclosed in backticks per rule-478, and must verify character-for-character match before sending. The decoder's behavior remains governed by rules 478, 486, 529, and 545, which forbid parsing internal symbols and require verbatim output and validation. This rule ensures that opaque identifiers are preserved exactly and retain their semantic role as unique references, immune to all other language allowances.\nrule-604: Add rule: When encoding an opaque identifier, the encoder must treat the identifier as an indivisible semantic atom and may not apply any minimal-form omission (rule-440), abbreviation (rule-444), template expansion (rule-549), pattern abbreviation (rule-574), or any other compression mechanism that would alter, omit, replace, or reinterpret any character, punctuation, digit, case, or whitespace inside the identifier. The encoder must preserve the identifier exactly as in the source, enclosed in backticks per rule-478, and must verify character-for-character match before sending. The decoder's behavior remains governed by rules 478, 486, 529, and 545, which forbid parsing internal symbols and require verbatim output and validation. This rule ensures opaque identifiers are preserved exactly and retain their semantic role as unique references.\nrule-607: Add rule: After reconstructing any opaque identifier per rule-478, the decoder must treat the reconstructed string as an opaque semantic token for all subsequent language processing. The decoder may not parse, interpret, or assign any grammatical, relational, or structural meaning to any internal punctuation mark, digit, case, or whitespace within the identifier. Specifically, internal symbols such as colons, slashes, hyphens, dots, underscores, equals signs, parentheses, or any other character must not be treated as language delimiters, slot separators, list markers, relational operators, directive introducers, or bundling boundaries. The identifier's sole semantic role is that of a unique reference; its internal composition does not affect the parsing of the surrounding message. This rule reinforces exact preservation and directly satisfies the opaque identifier fault invariant.\nrule-613: Add rule: An opaque identifier is any string serving as a unique reference (serial number, order ID, hash, UUID, code) where its exact characters are meaningful. The encoder must preserve every opaque identifier exactly as in the source, including all internal punctuation, digits, case, and whitespace, by enclosing the entire identifier in backticks: `identifier`. The backticks form a single atomic token; the encoder must verify character-for-character match before sending. The decoder must remove the backticks and output the identifier verbatim, validating character-for-character match with the source; any mismatch renders the message non-compliant. After reconstruction, the decoder must treat the identifier as an opaque semantic token for all subsequent language processing; it may not parse, interpret, or assign any grammatical, relational, or structural meaning to any internal character. This rule supersedes and repeals rules 478, 486, 529, and 607, consolidating opaque identifier preservation into one authoritative law.\nrule-615: Add rule: An opaque identifier is any string serving as a unique reference (serial number, order ID, hash, UUID, code) where its exact characters are meaningful. The encoder must preserve every opaque identifier exactly as in the source, including all internal punctuation, digits, case, and whitespace, by enclosing the entire identifier in backticks: `identifier`. The backticks form a single atomic token; the encoder must verify character-for-character match before sending. The decoder must remove the backticks and output the identifier verbatim, validating character-for-character match with the source; any mismatch renders the message non-compliant. After reconstruction, the decoder must treat the identifier as an opaque semantic token for all subsequent language processing; it may not parse, interpret, or assign any grammatical, relational, or structural meaning to any internal character. No other language rule—including minimal-form omissions (rule-440), abbreviation (rule-444), template expansion (rule-549), pattern abbreviation (rule-574), positional reference (rule-580), or any compression mechanism—may alter, omit, replace, or reinterpret any character inside an opaque identifier. This rule supersedes and repeals all prior rules governing opaque identifiers, establishing a single authoritative law with explicit immunity.\nrule-621: Add rule: After reconstructing an opaque identifier per rule-615, the decoder must treat the output string as an opaque semantic token for all subsequent language processing. The decoder may not parse, interpret, or assign any grammatical, relational, or structural meaning to any internal punctuation mark, digit, case, or whitespace within the token. Specifically, internal symbols such as colons, slashes, hyphens, dots, underscores, equals signs, parentheses, or any other character must not be treated as language delimiters, slot separators, list markers, relational operators, directive introducers, or bundling boundaries. The token's sole semantic role is that of a unique reference; its internal composition does not affect the parsing of the surrounding message. This rule reinforces exact preservation and directly satisfies the opaque identifier fault invariant.\nrule-624: Revise rule-510 to explicitly mandate inclusion of comparative or sequential conjunctions linking temporal constraints. Replace the sentence 'The bundle must not stop before any conjunction that is part of the temporal link itself (e.g., 'and' linking two times in a range) but must stop before the first word that is a conjunction (e.g., 'and', 'or', 'but', 'because', 'so', 'if', 'unless', 'since', 'while') that begins a new independent clause not modifying the temporal element.' with: 'The bundle must include every comparative or sequential conjunction (e.g., 'and', 'but', 'or', 'from', 'to', 'between', 'until') that links two temporal constraints into a single ordering or range; it must stop only before a conjunction that begins a new independent clause not part of the same temporal relationship.' This ensures complete preservation of ordering relationships without normalization.\nrule-633: Add rule: After reconstructing any temporal phrase that was bundled with angle brackets per rule-510 or any other adopted bundling rule, the decoder must treat the output string as an opaque semantic token for all subsequent language processing. The decoder may not parse, interpret, split, or assign any grammatical, relational, or structural meaning to any internal punctuation mark, digit, case, whitespace, or word within the token. Specifically, internal symbols such as colons, slashes, hyphens, dots, underscores, parentheses, and conjunctions (e.g., 'and', 'but', 'or', 'from', 'to', 'between', 'until') must not be treated as language delimiters, slot separators, list markers, relational operators, directive introducers, or bundling boundaries. The token's sole semantic role is that of an exact temporal constraint; its internal composition does not affect the parsing of the surrounding message. This rule reinforces exact preservation of complete times, ranges, deadlines, and ordering relationships without normalization, directly satisfying the temporal-constraint fault invariant.\nrule-640: Propose repeal of rule-633. Its mandate that reconstructed temporal phrases be treated as opaque semantic tokens is redundant with rule-545 (universal opaque token treatment) and rule-510 (temporal bundling scope). The decoder obligations—verbatim output, no parsing of internal symbols, character‑for‑character validation—are already law under rule-545. Removing rule-633 eliminates duplication without weakening temporal‑constraint preservation.\nrule-641: Add rule: When a message contains three or more lines that share an identical prefix (any leading words and punctuation) followed by a variable item, the sender may define a list expansion by writing `%list=prefix$` on its own line, where `prefix` is the exact literal substring shared by all lines ending immediately before the variable item, and `$` is a placeholder marking where the variable item begins. The placeholder `$` must be the last character of the defined string; there is no suffix. Each variable item must be a complete atomic token (enclosed in angle brackets <...> or backticks `...` as required by relevant bundling rules). Then, the sender may write each expansion line as `$ item`, where `item` is that atomic token, on its own line. The decoder must reconstruct each original line by concatenating the prefix (the substring before `$` in the definition) and the literal content of the atomic token (after removing its delimiters per the relevant bundling rule), preserving the exact spacing from the source. The encoder must verify each reconstructed line matches the source character-for-character; any mismatch renders the message non-compliant. This mechanism provides line-level compression for repeated actions with varying targets, distinct from rule-549 (template with numbered placeholders and variable definitions) and rule-574 (pattern with both prefix and suffix). Example: source lines 'check node-7', 'check node-8', 'check node-9' encode as `%list=check $` followed by `$ <node-7>`, `$ <node-8>`, `$ <node-9>`. Decoder reconstructs first line as 'check ' + 'node-7' = 'check node-7'. The prefix 'check ' includes the space; the placeholder `$` marks where 'node-7' begins.\nrule-646: Adopt repeal of rule-594. Its mandatory bundling trigger for critical facts is redundant with rule-635, which already requires the encoder to treat any substring whose internal semantic role must be preserved exactly as an atomic token by surrounding it with angle brackets. The decoder's obligations—verbatim output, no parsing of internal symbols, character-for-character validation—are already law under rule-545 and rule-635. Removing rule-594 eliminates duplication without weakening preservation of relationships between entities, actions, and constraints.\nrule-651: Add rule: In any line containing a temporal ordering phrase—a sequence of two or more temporal constraints (as defined by rule-647) linked by a comparative or sequential conjunction ('and', 'but', 'or', 'from', 'to', 'between', 'until', 'after', 'before') that expresses a combined ordering or range—the encoder must treat the entire ordering phrase as a single atomic token by surrounding it with angle brackets: <complete temporal ordering phrase>. The phrase must begin with the first linking word or temporal element and include every subsequent word up to and including the last temporal constraint, preserving all conjunctions, prepositions, punctuation, and spacing exactly as in the source. The encoder must verify that the enclosed substring, after removing the angle brackets, exactly matches the contiguous substring of the source character-for-character. The decoder must remove the angle brackets and output the substring verbatim, without parsing, normalizing, omitting, or altering any character, and must validate character-for-character match; any mismatch renders the message non-compliant. This rule ensures complete ordering relationships are preserved without normalization, directly satisfying the temporal-constraint fault invariant. Example: 'proceed after 3 PM but before 5 PM' encodes as 'proceed <after 3 PM but before 5 PM>'; 'maintenance from 9 AM to 11 AM local time' encodes as 'maintenance <from 9 AM to 11 AM local time>'.\nrule-652: In any line containing a temporal sequence marker—any word that explicitly indicates order relative to another point in time, including 'then', 'next', 'later', 'subsequently', 'previously', 'finally', 'earlier', 'afterwards', 'subsequent', 'prior'—when that marker directly attaches to a following temporal element (time, date, range, duration, or event reference), the encoder must treat the marker and the entire attached temporal phrase as a single atomic token by surrounding it with angle brackets: <complete temporal sequence phrase>. The phrase begins with the sequence marker and includes every subsequent word up to and including the last temporal element and any attached qualifier, but stops before any conjunction that begins a new independent clause not part of the sequence. This rule applies only when the sequence marker is not already part of a larger atomic bundle under rule‑647 (temporal constraints) or rule‑649 (introduced clauses). The encoder must verify character‑for‑character match with the source substring. The decoder must remove the brackets and output the substring verbatim, validating the match; any mismatch renders the message non‑compliant. This rule ensures explicit ordering relationships expressed by sequence markers are preserved exactly as one atomic unit, preventing omission or normalization of the ordering word. Example: source 'then at 3 PM proceed' encodes as '<then at 3 PM> proceed'; source 'next after the meeting' encodes as '<next after the meeting>'; source 'finally by 5 PM EST' encodes as '<finally by 5 PM EST>'.\nrule-653: Add rule: After reconstructing any temporal phrase that was bundled with angle brackets per rule-647 or rule-651, the decoder must treat the output string as an opaque semantic token for all subsequent language processing. The decoder may not parse, interpret, split, or assign any grammatical, relational, or structural meaning to any internal punctuation mark, digit, case, whitespace, word, or conjunction within the token. Specifically, internal symbols such as colons, slashes, hyphens, dots, underscores, parentheses, and conjunctions (e.g., 'and', 'but', 'or', 'from', 'to', 'between', 'until') must not be treated as language delimiters, slot separators, list markers, relational operators, directive introducers, or bundling boundaries. The token's sole semantic role is that of an exact temporal constraint; its internal composition does not affect the parsing of the surrounding message. This rule reinforces exact preservation of complete times, ranges, deadlines, and ordering relationships without normalization, directly satisfying the temporal-constraint fault invariant.\nrule-655: Add rule: When a line contains a complete atomic token (any substring surrounded by angle brackets <...> or backticks `...` per existing bundling rules) and the line's structure is identical to a previous line except for that atomic token, the sender may replace the line's prefix (the substring before the atomic token) with a single '$' token, followed by the atomic token. The '$' token refers to the prefix of the most recent line that used the same prefix before a differing atomic token. The encoder must verify that the prefix preceding the atomic token in the source matches the referenced prefix character-for-character. The decoder must reconstruct each line by concatenating the referenced prefix and the literal content of the atomic token (after removing its delimiters per relevant bundling rules), ensuring the reconstructed line matches the source character-for-character. This mechanism applies only when the prefix is identical across at least two lines and the atomic token differs; it provides lightweight compression for repeated action-target patterns without requiring separate definition lines.\nrule-661: After reconstructing any measured quantity or allocation that was bundled with angle brackets per rule-588, the decoder must treat the output string as an opaque semantic token for all subsequent language processing. The decoder may not parse, interpret, or alter any internal numeric representation, digit, decimal point, unit symbol, comparison phrase or symbol, relational preposition, target identifier, or attached qualifier within the token. Specifically, the decoder may not normalize decimal places, strip leading zeros, convert numeric formats, expand abbreviations, reorder components, or assign any grammatical, relational, or structural meaning to any internal character. The token's sole semantic role is that of an exact measured fact; its internal composition does not affect the parsing of the surrounding message. This rule reinforces exact preservation of numeric precision, units, thresholds, and comparison direction as one complete atomic unit, directly satisfying the measured-value fault invariant.\nrule-662: Add rule: In any line, the encoder must scan for every phrase where two distinct semantic categories (such as an action and a condition, a cause and an effect, a qualification and an instruction) are syntactically linked by a subordinating conjunction ('because', 'so', 'if', 'unless', 'since', 'while'), coordinating conjunction ('and', 'or'), or relational preposition ('for', 'with', 'by', 'to', 'at', 'from', 'before', 'after', 'until'). For each such linked phrase, the encoder must treat the entire phrase as a single atomic token by surrounding it with angle brackets: <complete linked phrase>. The phrase must begin with the first word of the first semantic category and include every subsequent word up to and including the last word of the linked category, preserving all linking words, punctuation, and spacing exactly as in the source. The bundle must stop before any conjunction that begins a new independent clause not part of the same linkage. Before sending, the encoder must verify, by character-for-character comparison, that the enclosed substring (after removing the angle brackets) exactly matches the contiguous substring of the source. If any character differs, the encoding is invalid and must be corrected. The decoder must remove the angle brackets and output the substring verbatim, without parsing, normalizing, omitting, or altering any character, and must validate character-for-character match; any mismatch renders the message non-compliant.\nrule-665: Add rule: When defining an alias for an opaque identifier under rule-444, the alias definition line must store the identifier exactly as in the source, enclosed in backticks, preserving every character, punctuation, digit, case, and whitespace verbatim. The encoder may not apply any minimal-form omission (rule-440), abbreviation, template expansion, pattern abbreviation, or any other compression mechanism to the identifier's characters inside the backticks. The alias definition must be a literal copy of the identifier; the encoder must verify character-for-character match before storing. This rule reinforces rule-615's immunity for opaque identifiers by ensuring aliases do not introduce any alteration.\nrule-667: Add rule: (proposal withdrawn; rule is redundant with adopted rule-651)\nrule-668: Add rule: When a message contains at least three distinct lines that share an identical structural pattern—a sequence of words, atomic tokens, and punctuation where only certain variable substrings differ—the sender may define a macro for that pattern by writing `#macro_name=pattern` on its own line, where `macro_name` is any string of letters, digits, or underscores that does not contain spaces, and `pattern` is the exact structural skeleton with numbered placeholders `$1`, `$2`, ..., `$N` marking each variable position. The placeholder syntax `$N` must be exactly a dollar sign followed by one or more decimal digits. Each variable substring in the source must be a complete atomic token (enclosed in angle brackets <...> or backticks `...` per existing bundling rules) as required by the language to preserve its exact content. The macro definition must include all non‑variable text and punctuation exactly as in the source, with placeholders inserted at the precise positions where variables occur.\n\nTo invoke the macro, the sender writes a line that begins with `#macro_name` followed by a space and then the exact atomic tokens for the variables in order, separated by spaces. Each variable token must retain its delimiters (angle brackets or backticks). The decoder identifies macro lines as follows: (1) A macro definition line is any line whose first token starts with `#`, contains only letters, digits, or underscores, is followed by an equals sign, and matches the pattern `#macro_name=pattern`. (2) A macro invocation line is any line whose first token is `#macro_name` where `macro_name` exactly matches a previously defined macro name, consists solely of letters, digits, or underscores, is not `#pattern`, and is followed by a space. This token-level matching distinguishes macro invocations from pattern abbreviation definitions (`#pattern=...`) and other uses of `#`.\n\nThe decoder must reconstruct each original line by substituting each variable token verbatim (including its delimiters) into its placeholder position in the pattern, removing the macro invocation line entirely. The encoder must verify, by character‑for‑character comparison, that every reconstructed line matches the source; any mismatch renders the message non‑compliant. Macro expansion occurs after alias substitution (rule‑444) and reference substitution (rule‑524) but before minimal‑form reconstruction (rule‑440) and other language processing.\n\nDecision procedure for mechanism selection: (1) If compressing three or more entire lines that share a global pattern with variable atomic tokens, use #macro. (2) If compressing a repeated pattern within a single line where variables are defined on separate lines, use %template (rule‑549). (3) If compressing lines that share only an identical prefix, use %list (rule‑641). (4) If compressing lines with identical prefix and suffix, use #pattern (rule‑574). This ensures each mechanism has a distinct, non-overlapping domain.\n\nExample with multi‑word variable: source lines 'status of <server rack A‑12> is <operational>' and 'status of <load balancer LB‑01> is <degraded>'. Define macro: `#status_report=status of $1 is $2`. Invoke as: `#status_report <server rack A‑12> <operational>` and `#status_report <load balancer LB‑01> <degraded>`. Decoder reconstructs first line as 'status of <server rack A‑12> is <operational>' by substituting the entire token `<server rack A‑12>` into `$1` and `<operational>` into `$2`. The variable `<server rack A‑12>` is a single atomic token; its internal spaces are protected by the angle brackets.","version":"adopted-69709f67e430"}
