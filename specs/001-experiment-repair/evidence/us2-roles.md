# US2 Offline Evidence — Legislative Tension

Result: **PASS (synthetic); real model transcript remains production-gated**.

- Agent A is `deepseek/deepseek-v3.2`; Agent B is `moonshotai/kimi-k2.6`.
- The shared constitution contains the stranger boundary, measurement discipline, permanent public history, 50% target, and affordable-access mission.
- `test_motion_authority.py` proves A can propose/revise/repeal but cannot vote; B can vote/request on the one open add or repeal motion but cannot originate either. A ratified repeal removes only its adopted target from language law, preserves full history, and allows the same text to return only under a fresh id.
- New add/repeal origination returns `proposal_already_open` while any focused motion is open. The cleanup migration terminalizes the copied legacy backlog before this guard reaches production.
- The receipt exposes the exact matched motion line, so recorded rationale comes from that motion's paragraph rather than the message's final line.
- Every active motion produces a public reason-coded receipt. Revision counters change only when `receipt.changed` is true.
- Representative forbidden outcomes: `inventor_cannot_vote`, `auditor_cannot_originate`, `proposal_already_open`, `repeal_target_not_adopted`, `settled_or_ineligible_motion`, `malformed_rule_id`, and `multiple_motions`.
