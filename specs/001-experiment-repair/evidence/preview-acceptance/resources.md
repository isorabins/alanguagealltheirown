# Preview resources receipt

Date: 2026-07-22 WITA

- Vercel scope: Preview only.
- Upstash resource: `alato-preview-acceptance` (`store_7q99nswrwsCpDvJF`), free
  plan in `sin1`, `autoUpgrade: false`, `prodPack: false`.
- Human-password and Vercel automation-bypass values are stored outside the
  repository; no values are recorded here.
- OpenRouter uses one separately stored management credential and a repository
  testing key named `crabbox-preview:a-language-all-their-own`, capped at `$5`
  per month and expiring 2026-08-21. No key values are recorded here.
- The Vercel bypass value was replaced once after accidental local terminal
  disclosure. The replacement was written directly to secure storage and is
  the only current test bypass credential.

No Production environment variable, credential, routing, or alias was changed.
