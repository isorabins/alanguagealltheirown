# PROGRESS — A Language All Their Own

Two LLMs negotiate an AI-to-AI language in public; the artifact is a versioned, encode/decode-tested rulebook. Spec: `BRIEF.md`. Overnight build contract: `BUILD-TONIGHT.md`.

## Ground rules (set at project start, 2026-07-14 — from the archive review)

- **Kill gate:** the brief's step-2 gate (run 20+ turns, read the transcript) is a real decision with a date. If the conversation isn't genuinely interesting after prompt tuning, this file gets a one-line cause of death and the project stops. **Gate decision due: 2026-07-17** (Iso reads MORNING-READOUT.md and calls CONTINUE / TUNE MORE / KILL).
- **Twitter precondition:** no tweet automation gets built until Iso either grants standing approval for a fixed dry-changelog format on a dedicated account, or cuts Twitter from v1. Per-tweet approval queues are how the Twitter quote automation died (analysis/graveyard.md).
- **Public-page date:** if the gate passes, the ugly public page ships within 7 days of CONTINUE — before the language is interesting. Built-never-launched is the house death mode.
- **Budget:** lives inside the 1h/day tinkering budget + nightly Fable runs. If it starts eating focus-lane time, park it honestly here.

## Log (append-only, tag [local]/[vps])

- 2026-07-16 [local] ~14:40 WITA — **BOTH FIXES SHIPPED AND VERIFIED — THE LOOP IS ALIVE, BUT THE FALSE BELIEF SURVIVED THE FIX.** Iso's go on the two items from the 14:20 entry.

  **(1) Display fix (`loop.py`, 289ae0b).** The agents' view of the stranger's decode was sliced at 400 chars; that silent cut is what they reverse-engineered into a phantom "~100-token decoder limit." Replaced with `render_decode()`: cap raised to 2000 (longest decode ever recorded: 838, so it never fires in practice), and if it ever must elide it now says so in the text — "the decode itself was NOT truncated… judge fidelity by the grader score, never by where this view stops." Verified against all 48 recorded tests: **zero now render cut**, t162's tail (the 40 chars A spent all of t163 debugging) is intact.

  **(2) Unwedge (VPS).** Committed the deliberate `tweets_sent` 24→0 reset that had been blocking `run_turn.sh`'s own `git pull` since 03:30 UTC — committed the intent rather than discarding it. Wedge cleared, VPS pulled the display fix, **turn 164 ran at 06:29 UTC and the timer's own fire landed turn 165 at 06:30 — both clean.** ~3h20m / 13 turns lost to the wedge. Hardening still unbuilt: `run_turn.sh` will wedge again the same way if any tracked state file is edited out of band.

  **(3) The fix worked mechanically and did not cure the belief — this is the live problem.** t164 (B, first turn seeing whole decodes) *escalated* instead of recovering: *"The truncation is unacceptable. Fidelity 100 is a false positive here — the decoder's output is missing material content ('evening tasting menu.'). The grader marked 'nothing material' incorrectly; that's a harness grading bug."* **That text was present in the decode B was shown.** B is now reading A's t163 claim rather than the data, and has begun impeaching the grader — the one ground-truth signal — to defend the phantom. The false premise has moved out of the display and into two places the fix cannot reach: the 12-event conversation window (flushes on its own in ~3h), and **the rulebook, which is re-rendered into every prompt** — rule-026 ("if it exceeds 100 tokens, split") and rule-029 ("ensures encoded chunks stay under 100") state the phantom limit as settled law, and B has just moved to tighten 029 from 90→80 tokens. Left alone, the agents will keep legislating against a bug that no longer produces evidence, because they are now citing each other. **Recommendation to Iso, nothing done without his yes: one dated harness-correction event injected into their window — the harness lied, so the harness corrects the record in public — stating that no decoder limit exists (`max_tokens=1000`, never approached), that the pre-t164 truncations were a display artifact, and that rules 026/029/030 rest on it. Then let them re-legislate through their own test-gated process. Minimal, honest, and it leaves the reasoning to them.**

- 2026-07-16 [local] ~14:20 WITA — **SCHEDULED READ OF THE TWO MORNING CHANGES — FOUND THE LOOP DEAD AND A HARNESS BUG DRIVING THE LANGUAGE.** Read-only; nothing changed anywhere. Three findings, in order of importance.

  **(1) The loop has been dead since 03:15 UTC / 11:15 WITA. Turn 163 is the last turn; ~12 fires lost.** `language-loop.timer` is active and firing on schedule — `run_turn.sh` exits 128 on *every* fire, at its own `git pull --rebase`: `error: cannot pull with rebase: You have unstaged changes.` The dirty file is `state/tweet-state.json`; an out-of-band edit at 03:20:30 UTC reset `tweets_sent` 24 → 0 (deliberate — `tweets_sent: 0` is precisely what makes the first real tweet premise-mode, per the ~09:35 entry) and was never committed. **This is a self-sustaining wedge, not a transient failure:** the dirty file blocks the pull, and `set -e` kills the script *at* the pull — before it can ever reach the `git add -A` twelve lines below that would have staged it. It will never recover on its own. Fix needs Iso's yes (VPS writes were outside this task's scope, correctly blocked): on the VPS, `git commit state/tweet-state.json` — commit the intent rather than discard it; dedup runs off the `statuses` dict (unchanged), so it cannot re-tweet history. Hardening candidate: stash generated state before the pull, or drop `set -e` around it.

  **(2) The agents have spent ~30 turns building their language around a bug that does not exist.** `loop.py:165` renders every test into the agents' window as `fresh decoder returned: {e['decoded'][:400]}` — a **400-character display slice**. 34 of 48 tests (71%) exceed 400 chars, so on most tests the agents read a mid-word cut; **27 of those scored fidelity 100** with the grader saying "nothing material." 400 chars ÷ ~4 chars/token ≈ **100 tokens** — exactly the number they reverse-engineered it into. t133 A: *"the decoder truncated again mid-sentence… the grader says 'nothing material'… but the harness's decoder seems to have a token budget"* (A notices the grader contradicts him, explains it away). t134 B: *"truncation appears after ~100 tokens… suggesting a decoder buffer limit."* t137 B: *"The consistent truncation at ~100 tokens **confirms** a hard output limit in the harness decoder. Chunking is the only viable response."* **There is no such limit:** the decoder runs at `max_tokens=1000` (`loop.py:306`) and never approaches it. rule-029 (chunk at >90 original tokens) and rule-030 (don't break `^` lines across chunks) are **2 of the 4 live rules, and both exist solely to route around a display artifact.** The `[:400]` slice dates to the initial commit (e722bba) — it is not new; the agents simply began attributing meaning to it at t133. Verified: t162 decoded 440 chars, fidelity 100, tail fully intact — and t163's entire 650-token turn is A debugging the 40 characters the *view* dropped, inventing a per-chunk decode limit to explain them.

  **(3) The two changes: sample far too small to judge, and confounded.** Corrected boundaries from commit times (turn commits are UTC, dev commits WITA; every turn pulls before it runs): the constitution (82333a0, 10:16 WITA) was live from **turn 160**, not ~161; tombstones (4e07478, 10:38 WITA) from **turn 161**, not ~165. That leaves **3 agent messages under the constitution (160, 161, 163) and 2 under tombstones (161, 163)** before the loop died — no basis for a verdict. On what exists: **no mysticism, no sermon, no praise loop** — every claim tethered to a number, MEASURE fired on all three turns, adoption still test-gated; the anti-mysticism clause is holding. **No tombstone confusion** — neither agent references a dead rule or a stub. **New-idea rate: zero** — 158/160/161/163 are all rule-029 threshold-tuning. The window's only novel idea is A at t160 — *"perhaps we allow omission of `^` inside a directive block if every line is a directive? No — that adds complexity"* — raised and self-killed in one breath. **Honest read: the discipline is fully intact and aimed at a ghost.** The constitution did not cause the rabbit hole (t133 predates it by 27 turns), and 4 turns cannot show whether it would have broken them out of it. Scores unchanged, within noise: t159 −15% fid 100, t162 +15% fid 100; all-time 37/48 at fid 100, avg +6.1% tokens. **Recommendation (Iso's call, nothing changed): fix `loop.py:165` first — raise the slice above payload size or mark it explicitly elided — then unwedge, then re-read the constitution question against a real sample. Judging the prompts while the harness lies to the agents just measures the lie.**

- 2026-07-16 [local] ~11:15 WITA — **TOMBSTONE COMPRESSION SHIPPED** (Iso's call to ship now rather than after the constitution read — token-budget and forgetting risk beat one-variable purity; the afternoon reader is told both change boundaries). Dead rules still ride in every call as memory, but render as index cards — id, death turn, 60-char stub, kill score — instead of full text: the agents' printed rulebook shrank 54% (7,652 -> 3,542 chars) with zero loss of living law. Full corpses remain in git history and on the public page's graveyard section (humans get the museum, agents get the index). kernel_tokens header lags until the next rule change recomputes it. Roadmap idea parked in HANDOFF: periodically let the agents review the full graveyard text (an archaeology turn) in case a stub hides a lesson.

- 2026-07-16 [local] ~10:50 WITA — **THE CONSTITUTION (Iso's direction, his prompt-craft style).** Both negotiator prompts rewritten constitution-first: a mandate section giving the agents the real stakes (agents worldwide speak English to each other at token prices; an open-source rulebook even 5% cheaper at full meaning is worth real compute/money/energy at scale), explicit first-principles creative license ('assume the winning ideas have not been proposed yet' — permission only, no techniques seeded, per the contamination rule), the two inviolable laws (stranger-decodable, tokenizer decides), and an anti-mysticism clause as drift guard ('a grand claim without a test number is noise'). All mechanics unchanged: conventions, MEASURE, win-codify, 250-word cap, A/B leans verbatim. Old prompts preserved in git history + MECHANICS.md. Live from ~turn 161. Also verified for Iso: full history IS durably stored — conversation.json carries every event since turn 1 (228 events), rulebook.json has 85+ committed versions, one public git commit per turn; no database needed, the repo is the database.

- 2026-07-16 [local] ~10:20 WITA — **PROMPT TUNE (Iso's call) + HOMEPAGE ONBOARDING.** Both negotiator prompts get one new bullet: a test that beats plain English at high fidelity is a discovery to codify into a rule before it scrolls out of the 12-event window ('a win that never becomes a rule is lost') — Iso's direction after seeing meaning at 22/22 perfect but cost still +8% avg; the fidelity/compression balance of the two roles was deliberately NOT changed. Also fixed stale 'every 5th turn' → 3rd in both prompts (cadence changed days ago, prompts never told). Pre-change prompt text preserved in public git history. Homepage: new 'How This Works' section — the five AI roles (negotiators, test writer, encoder, stranger, judge) in plain language for cold visitors; grader truncation rule and Field Notes shipped earlier this morning. All deployed and fingerprint-verified.

- 2026-07-16 [local] ~09:35 WITA — **TWEETS ARE LIVE.** Iso's final clicks landed: `{@` avatar verified on the public @alanguageall profile; upload-post profile `language` created via API (his first connect attempt landed on his personal `iso` profile and would have crossed wires with his other pipelines — caught by API verification, rerouted via a generated connect link). VPS .env now carries UPLOAD_POST_API_KEY / UPLOAD_POST_USER=language / TWEET_ENABLE=1 (mode 600); tweet.py exits dry-run as of the next turn. First real tweet fires on the next rule status change — a persistent monitor watches tweet.log and will confirm it. Loose end for Iso: profile `iso` still has @alanguageall connected — reconnecting his personal X there evicts it. Once the first tweet posts: pin it (it's premise-mode by construction), then the launch sequence in PROMOTION.md is unblocked (trigger already arguably met by t147's −24% at fid 100).

- 2026-07-15 [local] ~17:05 UTC / 01:05 WITA+1 — **REMAINING THREE ITEMS LIVE, ALL VERIFIED.** (1) **Blind payload generator** (Iso's design): every test payload is now written fresh by a generator that sees neither rulebook nor conversation — teach-to-the-test is structurally closed. Category rotates prose/task/data, domain rotates through 12; falls back to the file battery if generation fails twice. Verified in production: t90's test ran on `gen-data-customer` (a frozen-berries recall message with lot-scoped quarantine + refund conditional, invented seconds earlier) — fid 100, +11%. Metrics note: per-test difficulty now varies; read trends in batches. The 19 hand-written payloads are retired from rotation and reserved as the transfer-test battery + fallback. (2) **Transfer-test package merged** (TRANSFER-TEST.md + transfer_test.py): fresh Claude+GPT pairs vs the DeepSeek baseline; runs only on Iso's go at readiness (10+ adopted, rolling fid ≥90). (3) **Stall visibility**: the page footer now computes real freshness from the repo's last-commit time — "last turn N min ago" or "the loop has been still since <time>" past 45 min (deployed, fingerprint-verified); plus a scheduled heartbeat check that alerts Iso if the commit stream stalls. Near-miss logged honestly: a shell-proxy mangling briefly gutted .gitignore and staged .env — caught at staging, never committed, .gitignore rebuilt via direct file write; lesson: no shell text-processing on gitignore-class files while the rtk hook is active. MECHANICS.md synced to all of the above.

- 2026-07-15 [local] ~19:20 WITA — **IMPROVEMENTS 1–3 ACCEPTED AND LIVE** (from the afternoon review branch; Iso accepted per-item). (1) Launch pack: OG/Twitter-card tags, `{@` favicon set, 1200×630 banner — deployed to prod, verified serving on the domain (og tags present, banner 200 at exact byte size); Show HN / LinkedIn / ForageSF drafts staged in PROMOTION.md. (2) run_turn.sh body wrapped in main() so its own mid-run `git pull` can never splice old/new script bytes (the t76 stale-run class); rolled out with timer briefly stopped, verified with a manual turn (t88) under the new script, timer re-armed. (3) tweet.py: grader-failure fidelity −1 no longer produces a stats line; LINKEDIN_ENABLE plumbing added, off. Pending Iso's word: payload strategy (he proposed generated-unique test payloads — evaluating), transfer-test package, stall-alert. Rules count crept 12 → 15 during the away window (new proposals, unreviewed — next transcript read). data-table (now a status-report message) scored fidelity 90 at +14% tokens — a real test where v1 scored free 100s via verbatim pass-through. The fresh decoder truncated the directive block early (dropped west's tail + the last two directives + closing `}`); agents diagnosed it within one turn as block-boundary ambiguity and are tightening rule-003's close condition. Two observations logged, no intervention: (1) the encoder marked every line `^` including narrative data lines — `^` may be drifting from "directive" toward "any line," worth watching across the next few tests; (2) the grader's "lost" note misattributed the omission (said "South headcount 18" — 18 is north's, and the actual loss was the truncated tail), score direction right, note sloppy. Loop health: t79 at 06:30 UTC, VPS disk 70% (the 94% crisis is resolved), tweet hook running clean in dry mode. Iso's call: reuse the dormant babelAI account (@AiBabel18552, from the 2025 Babel attempt, 1 follower, never really used) instead of a fresh signup — aged account, better spam-filter standing, no audience mismatch. With his explicit go-ahead I renamed it in his browser: handle → **@alanguageall**, display name → "A Language All Their Own", bio set (the premise, 140 chars), website → alanguagealltheirown.com. Verified live on the profile page. The account's 27 old babel-era posts left in place (they'll sink under the changelog; the piece's pre-history is public anyway). Remaining before go-live: Iso connects @alanguageall to a new upload-post profile `language` (OAuth, his click), then the VPS .env gets the key + TWEET_ENABLE=1.

- 2026-07-15 [local] ~14:40 WITA — **DOMAIN LIVE + TWO-MODE TWEETS + PROMOTION PLAN.** Iso bought alanguagealltheirown.com at the Vercel checkout (his click, $11.25/yr, team isorabins-projects); attached to the project via CLI, verified serving the page (HTTP 200), tweets now link the real domain. Tweet format per Iso's second pass ("not every tweet… a little machiney"): machine-dry diff-style changelog by default (`+ rule-018 adopted · turn 81`), full premise line every 5th tweet, counter persisted in tweet-state.json; first-ever tweet is premise-mode by construction. Promotion decisions: NO rotating @mentions (platform-manipulation risk, reads as spam — Iso's idea, talked out of it), NO converting the 7K-follower ForageSF X account (wrong audience suppresses reach; it quote-tweets the launch once instead); plan = Show HN + LinkedIn post + ≤10 hand-sent personal notes, triggered when the language first beats plain English or hits 10 adopted rules. Details in PROMOTION.md (gitignored). Twitter go-live still blocked on Iso creating the X account (suggested handle @alanguageall) + connecting it to a fresh upload-post profile named `language`.

- 2026-07-15 [local] ~13:55 WITA — **TWEET FORMAT HUMANIZED + DOMAIN BLOCKED ON ISO.** Iso approved standing tweets but redirected the format: "Twitter is for people" — jargon out. New fixed template: refrain ("Two AIs are inventing a language, one tested rule at a time."), the rule text quoted, a plain-English stranger-test line (only on adoptions — rulebook-wide scores can't honestly explain a rejection), rule count + page link. ≤275 chars. Domain purchase attempted per Iso's yes: Vercel refuses non-interactive/agent purchases outright (registrant details + payment must be interactive) — Iso must run `vercel domains buy alanguagealltheirown.com` himself; attach-to-project is scriptable afterward. Turn 76 note: run_turn.sh updates itself via its own mid-run pull, so the tweet hook first executes at turn 77 — one-turn propagation delay, by design.

- 2026-07-15 [local] ~13:40 WITA — **TWITTER WIRED (dry-run) + MOTION-PARSER FIX + STATE REPAIR.** (1) tweet.py: dry changelog to X via upload-post, fired from run_turn.sh after every turn; tweets only rule status changes (adopted/rejected/reverted — never raw proposals), fixed ≤275-char template, >3 changes in one turn collapse to a single summary tweet, any failure is logged and dropped so Twitter can never block the loop. Inert until TWEET_ENABLE=1 + UPLOAD_POST_API_KEY/USER land in the VPS .env; until then composed tweets go to state/tweet.log. Blocked on Iso: X account creation, upload-post connect, standing format approval. (2) Parser bug (same family as the t42 markdown-bold and t50 unicode-hyphen fixes): agents write "PROPOSE: REJECT: rule-NNN" motions and apply_conventions minted them as junk rules — rule-013 (text: "REJECT: rule-006") even got ADOPTED into the kernel — while the motions' intent never landed: rule-006 and rule-014 sat "proposed" after both agents had agreed to kill them. Fix: a PROPOSE whose payload is itself a verb line now acts as the inner verb instead of minting. (3) State repair on top of t75: rule-006 → rejected (intent of 013's motion, adopted t65), rule-014 → rejected (intent of 016+017, t73/t74), the three junk mints purged, rule-003's leading-markdown splinter cleaned. Version 0.25 → 0.28, kernel now 4 adopted / 12 total. tweet-state.json bootstrapped post-repair, so the repair events themselves will never be tweeted.

- 2026-07-15 [local] ~13:25 WITA — **PAYLOAD SET V2** (work-queue item 1 from session close). The three data-* payloads rewritten from bare data dumps (config file / JSON order / CSV table) into agent-to-agent messages that carry the same data: data-config is now a deploy request with change-vs-stay values and an abort condition, data-order a fulfillment ask with scoped instructions (wrap the mugs, not the tee) and an out-of-stock contingency, data-table a status report with an exclusion caveat (Delta books under corporate) and directives on figure use. Same underlying numbers/SKUs/regions as v1 — the swap changes the communicative wrapper, not the content. Filenames unchanged, so the rotation and engine are untouched (payloads are read fresh per test). **Metrics are not comparable across the swap:** v1 data tests could score 100 via verbatim pass-through while exercising zero rules; v2 data tests exercise directive scoping, contingencies, and the parser-not-agent boundary. Transcript read t65–t74 (handoff item 4): healthy, no hold-loops or drift — agents self-diagnosed the t69 fid-0 execution failure (decoder executed a directive block), adopted rule-015 (decoder is a parser, not an agent) + revised rule-003 (text after `}` is still message), recovered to fid 100 at t72, and are now measuring rulebook self-compression (95-token merged rule vs ~170 tokens across four rules) unprompted — brief §8's kernel-shrinking pressure arriving organically. Loop heartbeat at session start: turn 74 at 05:15 UTC, clean 15-min cadence, $0.067 spent.

- 2026-07-15 [local/cowork] ~10:45 WITA — SESSION CLOSE (Iso moving to a fresh chat). Iso's page review found the right flaw: the three data-* payloads read as logging data, not messages — and since the current rulebook passes structured data through verbatim, those tests score 100 while exercising zero rules. **Next session item #1: rewrite the three data-* payloads as agent-to-agent MESSAGES that contain data** (the config becomes a deploy request, the table becomes a status report, the order becomes a fulfillment ask), keep the 5/5/3 prose/task/data balance, and log the swap as "payload set v2" so metrics aren't compared naively across the change. Session totals: built from zero → gate run → CONTINUE → published and autonomous in one morning (repo github.com/isorabins/alanguagealltheirown, page alanguagealltheirown.vercel.app, VPS timer every 15 min, ~$0.05 total API spend); viewer redesigned twice to Iso's taste; harness hardened per entries above. Loop at turn 62+, needs nothing from anyone. Iso's open items: rotate the exposed clawd PAT, Twitter account + standing tweet approval (or cut it), domain purchase via Vercel. Fresh-chat bootstrap: read this file top to bottom, then MECHANICS.md.

- 2026-07-15 [local/cowork] ~10:20 WITA: Legibility pass on the live page per Iso's read-through (he couldn't parse fidelity/compression/transmission — page's fault, not his). Shipped: metric key line, one-line explainer under every section, savings reframed positive (best +22% / avg −2% — the honest "not beating English yet" number), transmission panel now three columns (original → encoded → what the stranger got back) + grader verdict, per-rule score line relabeled "rulebook's latest test" (attribution is rulebook-level). Data fix: markdown `**` stripped from stored rule text (engine + 1 existing rule cleaned). Verified on prod with build fingerprint (earlier stale-looking checks were CDN cache + SSO wall on raw deployment URLs). Meanwhile the loop hit turn 62 unattended: plain-text foundation + `^` directive marker adopted after the t57 execution failure, kernel 771 tokens, agents now debating pruning their own duplicate rules.

- 2026-07-15 [local/cowork] ~09:00 WITA: **PUBLISHED — the piece is live and autonomous.** Iso approved publish explicitly (AskUserQuestion). Public repo github.com/isorabins/alanguagealltheirown (35 files incl. engine, prompts, full state; .env excluded, verified). VPS systemd timer `language-loop.timer` fires every 15 min: run_turn.sh → one turn → commit → push via repo-scoped write deploy key (NOT the exposed PAT). Public page https://alanguagealltheirown.vercel.app (Vercel CLI deploy; MCP token can't create projects) fetches live state from GitHub raw with bundled fallback — verified rendering turn 57 three minutes after the VPS authored it. First fully-autonomous turn: t57 at 01:00:20 UTC, which included a live test scoring fidelity 0 — a public failure on the front page within the first hour, per design. Remaining Iso items: rotate the clawd PAT (exposed in transcript), Twitter account + standing tweet approval (or cut from v1), buy alanguagealltheirown.com via Vercel when ready. Loop is hands-off; watching = read the page.

- 2026-07-15 [local/cowork] midday: Iso called **CONTINUE** ("ok go") — 7-day public-page clock running. Phase-2 harness work done and live-verified through turn 55: grader now catches decoder-executes-instead-of-relays (caught one at t48 — fid 0, agents responded by adopting the missing plain-text foundation rule), MEASURE tool shipped (agents used it within one turn to settle the block-vs-prefix cost debate), test cadence 5→3, unicode-hyphen parsing fixed + two silently-dropped t50 adoptions repaired (rulebook now 3 adopted / 9 total), OpenRouter provider pinned + self-recalibrating token probes (a provider-drift bug had corrupted one measurement B reasoned from). Viewer redesigned per Iso (artifact-first: chat panes → last adopted/rejected → rules with WHY excerpts → original-vs-encoded transmission panel; spend removed); MECHANICS.md written for Iso. **Publish checkpoint reached and held:** public repo `isorabins/alanguagealltheirown` created empty; full project committed LOCALLY (35 files, .env excluded); push + Vercel deploy await Iso's explicit word (classifier correctly enforced the no-push rule). ⚠️ Security flag for Iso: the VPS clawd git remote embeds a GitHub PAT in its URL — it got echoed into a session transcript today; rotate it. Old Babel-v1 corpse repo (`A-language-all-their-own`, private, June 2025) found on the account — left untouched.

- 2026-07-15 [local/cowork] late morning: **GATE RUN COMPLETE — verdict CONTINUE** (see MORNING-READOUT.md, copied to ForAlex). 45 turns + 9 live tests on DeepSeek v3.2, total spend $0.04. Rulebook v0.13: 1 adopted (directive blocks, survived 8 tests, scores −52%…+21%), 4 rejected on measurements, first death-and-resurrection arc (`//` commentary rejected t21, revived t37 after the −52% metadata-loss test proved the need). Unscripted finale: fresh decoder EXECUTED a task payload instead of relaying it (invented a whole quarterly memo), grader wrongly scored it 95 — grader rubric fix is phase-2 item #1. Other phase-2 fixes: dedup duplicate proposals (006/007/008 triplicates), tolerate markdown-bold PROPOSE lines (two real proposals silently dropped at t42–43), test cadence 5→3 + self-serve encode-count tool (agents asked for it at t33; cures the t27–34 dead-air loop). Awaiting Iso gate call; 7-day public-page clock starts on his CONTINUE.

- 2026-07-14 [local/cowork]: Project created. Archive review done (graveyard, blind-spots, Babel v1 post-mortem — this is a changed-design revival, conditions above derive from it). Prior research swept: `learning/language-design-patterns-yt-2026-02-23.md` is an empty template (nothing usable); Babel v1 spec usable only as anti-pattern, its seed vocabulary explicitly firewalled from agent prompts (contamination rule in BUILD-TONIGHT.md). Overnight build contract written and queued in tasks/fable-nightly-queue.md. Status: **queued for tonight's Fable run — awaiting first build.**
- 2026-07-15 [local/cowork] morning: Overnight run NEVER STARTED — Iso's "go" and the session interrupt collided and the turn died before the engine was written; discovered when Iso asked "did it work." Reported honestly, then built and ran live this morning with Iso present: loop.py (~250 lines, stdlib+requests, DeepSeek/OpenRouter), 13 payloads, Nous-styled viewer. Tuning cycle 1 (archived state/tuning-runs/cycle1-json-trap/): agents fell straight into the JSON trap, B adopted instantly, first test fid 0 on invented-field hallucination — but Agent A correctly self-diagnosed the fresh-decoder failure on t6, proving the feedback loop lands. Cycle 2 (revised prompts: payload-domain line, A can't self-adopt, B never adopts unmeasured; harness: payload types interleaved, decoder told to reconstruct-not-invent): same payload went fid 0 -> 100, B refused adoption twice, both rules REJECTED on measurement at t7 — real negotiation. Final 45-turn run launched from cycle-2 state (turn cap 500->650 mid-run at t8, noted for the record). Spend so far: <$0.01.
- 2026-07-14 [local/cowork] evening: Provider switched by Iso — Anthropic out, DeepSeek in. BUILD-TONIGHT.md updated: `deepseek/deepseek-v3.2` via OpenRouter for all four roles (live-priced $0.21/$0.32 per M → overnight build <$1 of the $5 cap, future 15-min cadence ~$0.50/day), token counting moved to response `usage` fields (count_tokens was Anthropic-only), deps cut to stdlib+requests, pre-KILL model-swap check added to the tuning protocol so the 7/17 gate judges the idea rather than the cheap model. OPENROUTER_API_KEY verified live (HTTP 200) from Bitwarden. Same-day access pre-flight: GitHub CLI, Vercel MCP, VPS SSH + outbound HTTPS, upload-post credential all green; claude-vps disk at 94% — cleanup chip spawned on Iso's dashboard. Still queued for tonight.
