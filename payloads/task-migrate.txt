Migrate the analytics cron off the old box before it is decommissioned on the 28th. Steps: copy /etc/cron.d/analytics and the two scripts it calls to the new host, install postgres client 15 (not 16 — the dump format differs), run one manual cycle and diff its output against yesterday's run on the old box. Only disable the old cron after the diff comes back clean two days in a row. If the diff never comes clean, stop and report — don't force it.
