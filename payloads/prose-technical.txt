When you type a domain name into a browser, the resolver first checks its local cache. On a miss, it asks a root server, which points it to the registry for the top-level domain. That registry names the domain's authoritative servers, and one of those finally returns the IP address. Each answer carries a time-to-live value; the resolver caches the record for that many seconds, which is why DNS changes propagate gradually rather than instantly.
