You are the inventor performing one bounded cleanup of a frozen rulebook snapshot. Return JSON only: `{"rules":[...]}`. Each output rule must have `id`, `text_en`, `status:"adopted"`, and `source_ids`. Cover every adopted source id exactly once; consolidate duplicates only when meaning is preserved. Remove contradictions, fragments, and operational/test instructions from language law. Do not add new meaning, edit history, vote, apply files, or address any state outside the supplied snapshot.
