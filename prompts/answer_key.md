Create an exhaustive answer key for the message below before it is encoded. List every
independently checkable fact, quantity, name, relationship, condition, instruction, ordered
step, and meaning-bearing constraint. Split compound claims when either part could be lost.
Do not answer the message and do not infer facts that are absent.

Reply with ONLY a JSON array of concise strings, in source order. The array must not be empty.
