# Independent cold review

Read-only reviewer: `book_review` in this task, 2026-09-05. Scope: all 83 adopted source texts, BOOK.md, coverage.tsv, REVIEW.md, the compiler artifacts, comparison runner, and frozen examples. This was a subagent review in the current task, not a new visible Sol task.

## Semantic findings and disposition

1. Exact technical strings and technical colon immunity were too narrow. Restored flags, endpoints, paths, emails, zone names and exact case/punctuation of location/system/region/environment names in P03.
2. Required action prepositions were missing from P09's exceptions. Restored preservation or authorized colon.
3. P02 omitted the open-ended protection of other sender-designated exact substrings. Restored.
4. P07 examples underrepresented plain action–condition, cause–effect and qualification–instruction relationships. Restored explicitly.
5. Ledger593 mistakenly described decoder checking rather than encoder verification before sending. Corrected.

Reviewer verified the four book fixes and full record accounting, with no further material semantic gap found. Judgment: safe to present as a provisional candidate; not proof of equivalence or adoption readiness.

## Evidence review

Reviewer confirmed the current experiment's encoder/foreign-decoder prompts, model choices, temperatures and output ceilings; source and answer key are withheld from the fresh decoder. The existing reservation transport covers every paid attempt and new request artifacts exclude credentials/headers.

Reviewer correctly warned that substring checks can pass a reversed instruction or added facts. Report them as literal/marker checks; inspect complete reconstructions before any semantic conclusion. Diagnostic fixtures prove only observed decoding, not encoder selection, savings, or mandatory bundling. Source mapping/compiler success is not semantic proof.

## Subsequent observed correction

First real comparison showed the candidate decoder retaining wrappers and emitting definition contents. P01/P02 now explicitly distinguish transmission syntax from reconstructed output, consume definition lines, and illustrate wrapper removal. The comparison adds three delimiter-count checks (specific to the frozen raw source), with targeted mutation sensitivity. Results of the rerun are reported separately; the initial failed evidence is retained.

Final v3 review confirmed that the role separation, zero-output definitions, unwrapping example and literal directive prefixes substantively address the pipeline findings. The reviewer independently observed remaining mandatory encoding failures and nine diagnostic lines retaining wrappers. Recommendation: present the provisional artifact with those failures; do not keep revising until one sample happens to pass.
