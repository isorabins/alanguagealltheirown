# Bounded live restart — 2026-09-06

Iso approved restoring the real experiment as it stands, then improving language
quality later. This includes integration, live runner/public viewer release and
successive real-turn verification. Existing shared API cap remains $1 with
$0.80077017417 spent until a further budget answer arrives. No X publication is
part of this release.

## Defining behavior

R1: A/B continue their existing legislative and development-exam cycle; negative
compression results are honest feedback, not a reason to stop the experiment.
R2: C uses Astra through the host's existing Codex ChatGPT login. B independently
reviews the exact candidate. Admission still needs <=4500 tokens and all meaning
exams valid and passing; a failed cleanup preserves the existing language.
R3: An admitted structured book and exactly three nonoperative ideas reach both
roles through the existing durable delivery path.
R4: Invalid authored judge evidence gets at most one model correction on the
same original/encoded/decoded material, preserving both attempts. Valid negative
judgments never get retried. Validators and meaning thresholds stay unchanged.
R5: Every paid dispatch reserves against one persistent approved session ledger;
expiry, uncertain cost or insufficient funds prevents further spending. Astra's
subscription use is separate. Research tool fees cannot be reserved by the text
pricing guard and are refused through the existing research-failure path.

## Verified preflight

Server `/root/alanguagealltheirown`, main 493a4643, clean. language-loop.timer is
inactive/disabled since September 4. Last recorded failure: legislative prompt
exceeded deterministic size budget. Existing server Codex 0.133.0 reports ChatGPT
login. Public remote main matches 493a4643. Separate X timer is active, untouched.
Local branch contains six prior architecture/cleanup commits, plus in-progress
full-book and restart fixes. Full real local run06 previously passed exact Kimi
review but failed admission on three invalid judgments. Source unchanged.

## Delivery and rollback

Review/test the entire branch, commit, integrate through a PR, then fast-forward
server code while preserving canonical state. Install a pinned ALATO-only CLI,
configure the bounded session, verify Astra on host and real successive turns,
and verify the browser's current public snapshot. Resume the timer only inside
the approved budget envelope. Rollback: stop loop timer, preserve all new state
and receipts, revert code through Git; never reset canonical experiment history.

## Coverage ledger

- Judge: native exam path, valid pass/fail, malformed and persistently invalid
  evidence, one repaired negative outcome, same messages, one cursor advancement,
  old baseline protection; offline tests and real provider follow-up required.
- Runtime: Astra routes before API-key lookup; paid dispatch persists costs
  across processes; cap/expiry/auxiliary fees stop before dispatch. Tests plus
  actual host login/model check and real scheduled caller required.
- Existing cleanup admission/delivery: preserve default-path integration tests;
  actual failure must continue normal turns without applying a candidate.
- Release: remote revision, host service, successive canonical turns and public
  viewer evidence remain required before a live completion claim.
