# Full local cleanup behavior

User approved: make the existing experiment work locally end to end; compact the
full book to the demonstrated roughly4,000–4,500-token scale, return structured
contracts plus exactly3 nonoperative wildcard ideas, judge the actual candidate,
adopt only on success, and continue the experiment. Use Astra via the existing
Codex ChatGPT sign-in for C. No production deployment or new API allowance.

## Behavior spine and acceptance

- F1: C authors the full candidate and three ideas from the real settled source;
  deterministic code enforces source accounting and <=4,500 operative tokens.
- F2: Model B's semantic pass must bind to the exact final candidate. A revision
  cannot inherit approval of an earlier draft.
- F3: The existing ExamEvidence path runs every registered B1–B5 case with real
  encoder, fresh foreign decoder and judge. All meaning gates must pass before
  adoption. Message savings are measured separately, not confused with book size.
- F4: Failure keeps the previous language and consumes no ideas; charges and raw
  failure evidence remain recorded. Local test cursors cannot contaminate active
  experiment benchmarks. Open motions settle through normal model decisions, retaining scheduled development exams; optional conversation evaluations and publishing are outside this local acceptance run.
- F5: A successful normal application commits and reloads; A and B each receive
  the structured book and exactly three ideas through existing delivery logic.

The external seam is `run_verified_cleanup(source_path, output_dir, ...)`, an
admission runner accepted by the ordinary automatic-cleanup caller. It owns the
C/shadow flow, final semantic audit and real exam gate; it never mutates the
source. `run_full_cleanup.py` is the local operator path: copy the saved state,
settle the motion, request cleanup, commit/reload and continue both roles.
`CodexCompactor` adapts the existing model-call seam to the official CLI and stores
raw subscription-usage receipts. Original OpenRouter providers retain per-attempt
reservations against the existing shared $1 ledger. Subscription usage is labeled
separately; zero incremental API dollars must not be described as free usage.

The older advisory-only shadow helper remains available as evidence generation;
its PASS alone is not admission in this verified local path. The ordinary automatic
caller now uses this admission gate by default; callers may inject it for local evidence capture.
No source data is deleted to manufacture eligibility. Local scheduling metadata
may be explicitly re-armed for this operator-requested run; any open motion stays
under normal legislative authority.

## Coverage ledger

| Promise | Cases to protect |
|---|---|
| F1 | All source IDs; missing/duplicate contracts; excessive size; exactly3 ideas; Codex nonzero exit, timeout, malformed/missing output, no terminal receipt. |
| F2 | Initial exact pass; C revision after B objection; stale/mismatched hash; rejected/malformed/unavailable final audit. |
| F3 | Every registered case; wrong candidate hash; invalid judge; meaning failure; real raw encoder→decoder→judge path; output expansion reported separately. |
| F4 | Open motion; provider/budget failure; source unchanged; costs retained; isolated exam cursor/results; no candidate/seeds exposed on failure. |
| F5 | Normal automatic application; committed reload; both roles' actual requests include structured snapshot and seeds; delivery clears only after both lawful turns. |

## Local evidence and current boundary — 2026-09-05 WITA

- Full-source run01 resumed all saved records and made four real legislative
  turns (3739–3742). B requested clarification twice; A revised rule-670 twice.
  The open motion remained, so cleanup correctly did not draft or apply a book.
  This first run incorrectly omitted a scheduled development exam; the runner
  is now corrected for both motion settlement and postadoption continuation.
- Original source remained byte-identical. Run01 receipt and resumable state:
  `/private/tmp/alato-c-local-20260905/full-verified-01/acceptance.json` and
  its adjacent `state/`. Do not restart from the original and duplicate paid work.
- Shared API ledger remains
  `/private/tmp/alato-c-local-20260905/approved-dollar/budget.json`.
  Verified spend after run01: $0.54353331257 of the original $1 total.
- Automatic approval review refused run02 because ALATO's broader saved context
  was not covered by the public-rulebook proof. An explicit user approval question
  for saved legislative feedback/metadata to the existing OpenRouter models is
  pending. No additional OpenRouter call occurred in these refused attempts.
- Anonymous public source at commit `493a464359c4d11ca8562930b30ca1fe817b4d1e`
  matches the original full rulebook SHA256
  `53ffb801cbeec7f64ad3c6d16c8863aa2e97e692686f5f48e407ca843c15abc2`.
  That proof covers the book, not every field in saved experiment context.
- Astra's actual schema-output adapter passed a synthetic deadline request using
  `/Applications/ChatGPT.app/Contents/Resources/codex` version0.153.0 and the
  existing ChatGPT login. Global CLI0.147.0 was rejected by the backend as too old;
  no model substitution or new installation was made. Receipt:
  `/private/tmp/alato-c-local-20260905/codex-adapter-smoke/`.
  This proves login/adapter compatibility, not full-book compaction or fidelity.

### Protected behavior

`test_verified_cleanup.py` covers the native drafting/compiler/exam path with fake
external providers: exact review, final rejection, malformed judge, lost meaning,
size boundaries, source preservation, isolated exam state, and budget exhaustion
in final B review, exams and token probes. `test_full_cleanup.py` covers snapshot
locking/both journals and exact request payloads; its integration case uses the
ordinary default admission gate, commits/reloads, invokes real Legislature logic
for both roles, and verifies durable once-per-role idea delivery. These are offline
behavioral tests; model outputs are fixtures, not evidence of live model quality.

The normal admission setting remains a recorded-spend **stop threshold**, checked
before and after every adapter call. A single in-flight model or token-count operation (which may issue multiple probes) can exceed it;
no over-threshold result is adopted. Only the local `ReservedTransport` guarantees
the shared hard $1 ceiling by reserving each actual HTTP attempt. Codex subscription
consumption is separate and has explicit token receipts.

The pending continuation command is prepared in
`/private/tmp/alato-c-local-20260905/authorized-full-cleanup-02.py`.
It resumes run01's local state, loads only the existing OpenRouter key in-process,
uses the bundled current Codex CLI, and adds an attributed optional finding for B:
rule-670's worked example drops “10 GB” and its copula rule needs unavailable source
knowledge. It cannot fabricate adoption/rejection or delete the open motion.

No compacted full book has yet passed this complete live gate or been adopted.
No deployment, main-branch merge, push or public publication occurred in this work.

Final offline verification: 274 Python tests passed after all fixes; the unchanged
Node viewer suite passed 87 tests earlier in this task. Historical traceability
check passed (115 requirements, 210 historical tasks); this checks links, not
semantic fidelity. The independent skeptical review found no remaining code
correctness blocker and explicitly left real full-book success unverified.

The existing automatic-cleanup tests now mock the admission seam rather than the
advisory-only drafting helper, because the user requested a real judge gate before
adoption. The provider-failure test now expects the source-token preflight; old-book
retention, failure classification and provider receipt assertions are unchanged.


## Continued local verification — 2026-09-06 WITA

Iso explicitly approved saved legislative feedback and metadata transmission to
ALATO's existing OpenRouter models under the original shared $1 cap. Automatic
review initially rejected the continuation again on broader-context grounds.
An offline inspection of the actual Legislature request established that the
7,117-event conversation is processed locally rather than serialized into prompts;
outgoing fields are adopted language, open motion, machine-state metadata, latest
receipt, active legislative feedback and the attributed operator suggestion.
Saved collaboration contained no asks, research or suggestions. That evidence
resolved review; the approved run proceeded without changing its destination.
Preflight: `/private/tmp/alato-c-local-20260905/outgoing-payload-preflight.json`.

Run02 made seven normal scheduled turns, including development exams3744/3747.
B initially requested corrections. At3746 it authored a REJECT with an extra
forbidden `focus` field; structural regeneration discarded that judgment and
returned REQUEST. At3749 B ultimately ADOPTED the revised rule670. This leaves
84 adopted rules in the local copy. An intermediate chat update wrongly said the
proposal was rejected; the next update corrected it against the saved response.
The original saved experiment was unchanged.

The real failure demonstrated a retry-context defect: a format repair saw only
its error and old authoritative context, not the response it needed to repair.
`test_structural_repair_receives_prior_judgment_and_retains_vote` failed before the
fix and passed after. Retries now receive the prior response (bounded16384chars),
clearly marked unaccepted data, with guidance to preserve substantive judgment.
No invalid response is directly accepted and no decision is manufactured.

The first full Astra call failed before generation: CLI initial-message limit
1,048,576 characters, actual1,439,155. The adapter now writes large input JSON
unchanged to its read-only temporary working directory and asks Astra to inspect
all source records through bounded file reads. Its test proves availability of the
entire input, not that a model actually reads it. Final review and meaning exams
remain necessary. Smaller requests retain the verified inline path.

Run02 ended with the previous book retained and API spending $0.65358429347.
Run03 resumes that settled local state, avoiding duplicate legislative work.
Evidence directory: `/private/tmp/alato-c-local-20260905/full-verified-03`.
Regression after both fixes: 276 Python tests passed. Independent skeptical review
found no correctness blocker in the retry-context or large-input transport fixes.

## Verified outcome and correction — 2026-09-06 WITA

The user explicitly approved sending saved legislative context to the existing
providers. Astra medium via Codex subscription produced a corrected 3,814-token
candidate from 22,343 operative tokens (82.93% reduction), with 20 contracts and
three nonoperative ideas. Real Kimi final review passed candidate hash
90df2f37af32478eb7e42017aca697fdde0f09be6a4a335ad3f3526c8d62cc55.
Run06 then passed two meaning exams; three were invalid judgments (B3/B4 literal
evidence conflicts, B5 malformed evidence range). No adoption or real A/B delivery
occurred. Four of five encoded messages expanded. This is not evidence of a
working compression language. The original experiment was unchanged.

The initial full-history B request exceeded provider context before generation.
Its retained uncertain reservation was individually reconciled using the exact
request, explicit provider-null context error, and OpenRouter's documented zero
completion policy. Later code requires pre-dispatch request binding and rejects
auxiliary-fee/ambiguous cases; this is not a fabricated returned usage receipt.
B subsequently reviewed all source current texts plus memory-cited histories,
with explicit omitted-history metadata; C still received the complete source.

Current restart scope and acceptance: live-restart-20260906.md. Source-only replay
and diagnostic judge rechecks never authorize adoption. Raw local evidence is
under /private/tmp/alato-c-local-20260905/full-verified-06/.
