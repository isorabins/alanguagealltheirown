# ALATO autonomous successful compaction

/goal Get ALATO running autonomously in production with a successfully compacted rulebook adopted and used by its agents. Continue until the smaller book passes the existing meaning tests, is installed live, and the scheduled A/B/C workflow is verified. A failed compaction followed by continued operation is progress, not completion.

THE OUTCOME I WANT

The live experiment uses a substantially smaller, behavior-preserving rulebook. A and B continue developing the language autonomously. C periodically compacts it through the normal production workflow. The website accurately shows the active book, agent activity, tests, and compaction results.

Make the changes necessary to reach that first working state. Broader language improvements and new features come afterward.

MODEL ROLES AND AUTHORITY

Use my latest requested assignments:
- A: Sol, high reasoning.
- B: Kimi K3.
- C: Astra, high reasoning.
- Use my existing Codex login for A and C. Verify actual model IDs, availability, reasoning settings, and routing before running them. Do not silently substitute models.

C produces the compact structured rulebook and exactly three nonoperative wildcard ideas.

B provides comments on the draft. C considers those comments and makes the final editorial decision. C may accept or dismiss B’s objections. B has no veto and must not become a second approval gate.

Allow bounded structural repair while preserving C’s opportunity to make its final decision. Structural validity, source integrity, size limits, and the existing meaning tests remain enforced.

CONTEXT AND CONTINUITY

This task supersedes “Make ALATO production-ready.” Read that task before changing anything and preserve its useful work. If it is still editing or deploying, ask me to pause it before you become another writer. Do not run competing production changes.

Existing checkout:
 /Users/isorabins/alex-workspace/projects/alato-deep-modules-20260905

Read its AGENTS.md, docs/MODULES.md, PROGRESS.md, and:
- docs/architecture/live-restart-20260906.md
- docs/architecture/sol-comparison-20260906.md
- Their adjacent evidence receipts.

Verify the current branch, working changes, remote history, production revision, timer, active model calls, and budget. These files describe earlier observations; inspect current reality before acting. Preserve the architecture, recovery fixes, experiment history, and unrelated user work. Reuse the existing implementation and deployment path.

Known findings to investigate:
- Production admission previously gave B a final veto despite C’s intended authority.
- A structural correction could consume the call needed for C’s final decision.
- Both Astra and Sol produced roughly 4,000-token drafts.
- In the latest paired local comparison, neither draft passed all five meaning tests. All ten judgments were structurally valid.
- A shorter book or a favorable B review therefore does not establish successful compaction.

REQUIRED FINISH LINE

1. Successful compaction:
   Produce a complete structured representation of the operative language, targeting no more than 4,500 measured operative tokens. Preserve the existing rules’ behavior, exceptions, identifiers, conditions, and authority. Keep history available and wildcard ideas nonoperative.

2. Meaning preservation:
   Run all five existing meaning exams against the exact final candidate using the real encoder, decoder, and judge. Every exam must have a valid judgment and pass the existing meaning requirements. Record message-body savings separately from book-size savings.

3. Actual adoption:
   Adopt that exact passing candidate through the normal application path. Verify its identity survives persistence and reload. Confirm A and B receive the accepted structured book and the three ideas through their actual prompts.

4. Autonomous production operation:
   Deploy the necessary changes and verify a real production C compaction/adoption cycle plus subsequent scheduler-driven A and B activity using the compact book. Verify C’s future trigger remains enabled and correctly configured. The workflow must not depend on this chat manually invoking each turn.

5. Truthful public display:
   Open https://alanguagealltheirown.com and verify the displayed active book, size/growth, model labels, latest activity, and test results match canonical production state. A running timer, HTTP 200, or green unit tests alone is insufficient.

6. Protected behavior:
   Add focused regression tests for B rejection followed by C finalization, structural repair without losing finalization, valid adoption, meaning-test failure retaining the old book, three-idea delivery, correct model routing, and interruption/recovery. Include positive and negative controls. Obtain an independent skeptical review of the final changes and resolve material findings.

HOW TO WORK

Diagnose failures from the actual source, encoded message, reconstruction, and judgment. Make targeted corrections to the compaction workflow, prompts, representation, or execution where the evidence justifies them. Then rerun the relevant checks.

If a candidate fails meaning tests, retain the active book and continue working toward a passing candidate. Do not declare the goal achieved merely because rejection was handled correctly.

Preserve the experiment’s behavior. Do not remove difficult rules, weaken tests, lower the meaning threshold, fabricate judgments, hand-author benchmark answers, special-case the five exam messages, or reset the displayed growth number to manufacture success.

Keep edits proportionate. Do not start another architectural rebuild or add unrelated features. Distinguish deterministic bugs, model mistakes, and judge mistakes; investigate questionable judgments against the actual text.

COMPLETION EVIDENCE

Maintain a small durable goal record in this repository with required gates initially unproven. Give each gate concrete evidence: candidate and source hashes, real provider receipts, all five judgments, adoption/reload records, scheduled-turn receipts, deployed revision, and browser evidence.

Create or reuse an executable completion check that rejects missing evidence, failed exams, candidate/hash mismatches, local-only adoption, and absent scheduled continuation. Verify that this check rejects an incomplete state. Revalidate evidence affected by later changes.

Do not mark the goal complete from a checklist or prose claim alone.

ACCESS, SPENDING, AND RELEASE

Preflight GitHub, the existing claude-vps deployment, Vercel, Codex login, and the existing API credential path before depending on them. Keep secrets out of output and files.

This goal authorizes the scoped implementation, tests, review, release, deployment, repair, and rollback needed for this outcome. Preserve production data and experiment history.

Respect the remaining balance of the existing $2 API allowance and its approved operating window. This is not a new $2 allowance. Read the actual shared ledger and expiry; do not reset either or create a parallel allowance. Respect Codex subscription limits as well.

If the budget, operating window, model availability, or another consequential boundary prevents completion, explain the exact blocker and the smallest additional authorization needed. Continue independent safe work where possible. A blocker is not success.

FINAL DELIVERY

Tell me plainly:
- Whether the smaller book is actually adopted live.
- Its before/after measured size and all five meaning-test results.
- Which models are really running as A, B, and C.
- Evidence that scheduled operation continued with the accepted book.
- Remaining spend and the operating deadline.
- Release/commit state and any unresolved limitation.

The finish line is a successfully compacted book in use by an autonomous live experiment. Further optimization comes after that.